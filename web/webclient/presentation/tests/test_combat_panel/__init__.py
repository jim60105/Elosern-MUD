"""Exact ``context_actions`` schema and presenter tests (tasks 3.1-3.2).

Package split of the original flat module; each slice module groups
the shipped classes by panel concern. Shared module-level helpers live
in ``_support`` (not a collected test module).
"""
