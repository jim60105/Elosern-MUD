# ==========================================
# Git, CI & Documentation
# ==========================================
.git/
.gitignore
.github/
README.md
docs/
openspec/

# ==========================================
# Local Python / Virtual Environments
# ==========================================
__pycache__/
**/__pycache__/
*.py[cod]
.venv/
venv/
env/

# ==========================================
# Evennia Local Runtime & Databases
# ==========================================
# Do not bake local test databases, logs, or collected files into the image
server/evennia.db3
**/*.db
**/*.db3
**/*.sqlite3
**/*.sqlite3-journal
server/logs/
server/*.log
server/*.pid
server/*.sock
server/.static/
server/.media/
server/.cache/
server/conf/secret_settings.py

# The translation model cache is an operator-seeded named volume at runtime
# (art-prompt-translation): a locally seeded directory is never baked into the
# image, matching how .rembg's runtime-fetched cache is excluded.
server/.translate/

# The seed-art folder is bind-mounted read-only at runtime (gallery-seed-sync);
# bulk seed images are never baked into the image.
art-seed/

# ==========================================
# Environment & Secrets
# ==========================================
.env
.env.*

# ==========================================
# Tooling & System Artifacts
# ==========================================
.agents/
.claude/
tmp/
tests/
