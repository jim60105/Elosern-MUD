## 1. Dependencies

- [x] 1.1 `uv add ctranslate2 sentencepiece`. Do NOT add `argostranslate` — it resolves to 83 packages including `spacy`, `thinc`, `blis`, and `cuda-bindings` (design D1; verified with `uv pip compile` while writing this change).
- [x] 1.2 `uv sync --locked`, then confirm the resolution added exactly the expected packages and nothing else: `ctranslate2`, `sentencepiece`, and — only if it was not already present via SciPy/Pillow — `numpy` and `pyyaml`. If any `nvidia-*`, `torch`, `spacy`, or CUDA-linked package entered `uv.lock`, STOP and report before writing code. Gate verified: the lock diff adds exactly `ctranslate2 4.8.2` and `sentencepiece 0.2.2`; `numpy 2.5.1` and `pyyaml 6.0.3` were already present; zero `nvidia-*`/`torch`/`spacy`/`thinc`/`blis`/`cuda*` packages entered the lock (checked after `uv add`).
- [x] 1.3 Confirm `import ctranslate2` and `import sentencepiece` succeed in the project environment on Python 3.13, and record the resolved versions in this task. Resolved and recorded: Python 3.13.14 (`uv run --locked python --version`), `ctranslate2==4.8.2`, `sentencepiece==0.2.2`; both import successfully in the project environment.

## 2. Settings

- [x] 2.1 `server/conf/settings.py`: add `ART_TRANSLATE_THREADS = _env_int_bounded("ART_TRANSLATE_THREADS", 0, low=0, high=256)` beside `ART_TRANSLATE_ENABLED`, documenting that `0` means the CTranslate2 default thread count.
- [x] 2.2 `server/conf/settings.py`: add the code-only `ART_TRANSLATE_MODEL_DIR = os.path.join(GAME_DIR, "server", ".translate")` with the comment stating why it is NOT environment-overridable (persistent-volume path, same rationale as `ART_STORE_ROOT` and `ART_REMBG_MODEL_DIR`).
- [x] 2.3 Add `ART_TRANSLATE_THREADS` — and only that name — to `_ENV_OVERRIDES` in `server/conf/test_settings.py`.
- [x] 2.4 Add NO download-enable setting. The server performs no fetch (design D2); a knob for a behaviour that does not exist would be dead inventory and `test_env_example_advertises_no_dead_variables` would be right to reject it.

## 3. The backend

- [x] 3.1 Create `world/art/translate_ct2.py::CTranslate2Backend` implementing the seam's `translate(lines) -> tuple[str, ...]` contract. Module docstring states the boundary: local CPU NMT behind `world/art/translate.py`'s seam, no network, no observability call (the worker owns the events).
- [x] 3.2 Check the model layout BEFORE importing any translation library: `ART_TRANSLATE_MODEL_DIR` must exist and hold a CTranslate2 model directory plus a SentencePiece source model. A missing directory or component raises `TranslateError("art_translate_unavailable", ...)` with no import and no network (delta spec: "An unseeded model directory degrades without a fetch"). **Do task 5.1 first**: determined against the actual artifact — task 5.1's script unpacks `translate-zh_en-1_9.argosmodel` (zip listing verified) into `<dir>/model/config.json`, `<dir>/model/model.bin`, `<dir>/sentencepiece.model`; the backend's `_MODEL_DIR`/`_SP_MODEL` constants and their code comment record exactly those names.
- [x] 3.3 Import `ctranslate2` and `sentencepiece` LAZILY inside the first call, never at module import, so `world/art/translate_ct2.py` stays importable and the settings module stays loadable where the optional stack is absent.
- [x] 3.4 Build at most one `ctranslate2.Translator` per configured model directory per process: module-level cache with the lock guarding CONSTRUCTION only (check, lock, re-check), never the translate call (design D5, mirroring `world/art/cutout.py`'s session cache). Pass `device="cpu"` explicitly (design D7) and apply `ART_TRANSLATE_THREADS` as the intra-op thread count when non-zero.
- [x] 3.5 Implement the call: SentencePiece-encode each source line to tokens (`out_type=str`), `translate_batch(..., beam_size=5)` with sampling DISABLED (design D6), decode each result back to text via the U+2581 surface-marker join (verified against the real model), and return one string per input line in input order. Map every escaping exception — encode, translate, decode — to `TranslateError("art_translate_error", ...)`; map load and construction failures to `art_translate_unavailable`.
- [x] 3.6 Keep `world/art/translate_ct2.py` free of any `world.observability` import or call, and free of any outbound request of any kind.
- [x] 3.7 Confirm by reading that no code path in this module can select a GPU device or fall back to one, and that no `LLM_*` setting, `world.ai` client, or completion endpoint is reachable from it (delta spec: "No generative-layer transport is reachable from the stage"). Enforced by `CTranslate2BackendReachabilityTests` (AST + source assertions) and confirmed by direct reading.

## 4. Container and inventory

- [x] 4.1 `Containerfile`: add `install -d -m 775 -o root -g 0 /app/server/.translate` to the app-layout stage and `/app/server/.translate` to the final stage's `VOLUME` list, mirroring `/app/server/.rembg`.
- [x] 4.2 `compose.yaml`: add the `evennia-translate:/app/server/.translate` mount and declare the named volume. No `ART_TRANSLATE_*` interpolation line — these knobs arrive through `env_file: .env` like the rest of the `ART_*` family.
- [x] 4.3 `tests/test_container_contract.py`: update the pinned exact sets (`writable_paths`, both compose volume sets) for `/app/server/.translate` / `evennia-translate`, and add the annotated volume-scenario test asserting the image itself carries no model artifact (plus the `.containerignore` exclusion and no-service-fetch assertions).
- [x] 4.4 `server/conf/tests/test_env_overrides/` (a package): update `ENV_BACKED`, `DEFAULT_REPR`, `VALID_OVERRIDES`, `INVALID_VALUES` in `_support.py` and the AST set in `test_inventory_and_shard_ownership.py`, for `ART_TRANSLATE_THREADS` (env-backed) and `ART_TRANSLATE_MODEL_DIR` (code-only). The hostile-variable test in `test_seam_precedence_derived_and_sanitization.py` gained `ART_TRANSLATE_MODEL_DIR`.
- [x] 4.5 `.env.example`: add `ART_TRANSLATE_THREADS` with type, default, and rule. Do NOT add `ART_TRANSLATE_MODEL_DIR` — the dead-variable check would be right to reject it.
- [x] 4.6 `docs/development/settings-and-environment.md`: add `ART_TRANSLATE_THREADS` to the inventory table and `ART_TRANSLATE_MODEL_DIR` to the code-only list, plus a short subsection on the model directory layout and the seed-only policy.
- [x] 4.7 `web/tests/browser/browser_settings.py`: confirm no change is needed — the harness already declares the stage disabled and the double as the backend. Checked: `ART_TRANSLATE_ENABLED = False` and `ART_TRANSLATE_BACKEND = "world.art.fake_translate.FakeTranslator"` at lines 163-164; no edit required.

## 5. Seeding the model

- [x] 5.1 Write `scripts/fetch-translate-model.sh` following the repo's existing `scripts/` conventions (and the `zsh-script-writer` guidance if the other scripts follow it): download one zh→en translation package to a host path, verify it, unpack it into the layout 3.2 expects, and print where to mount it. It runs on the HOST, never in the server process (design D2). Fail loudly on a missing tool, a failed download, or an unexpected archive layout. Executed end-to-end against the live artifact (74,481,402-byte zip verified; layout checked in a scratch dir; re-run without `--force` fails loudly).
- [x] 5.2 Document BOTH seeding routes in `docs/gm/prompts.md`: the Argos Open Tech `.argosmodel` archive (unzip yields a CTranslate2 model plus a SentencePiece model — the wrapper package is what this change avoids, not the model artifact), and `ct2-transformers-converter` over `Helsinki-NLP/opus-mt-zh-en` run offline on a workstation. Record each route's licence. Licences recorded: Argos `translate-zh_en-1_9` package README states its model is derived from the OPUS-MT zh→en model, CC-BY 4.0 (verified by extracting the archive); `Helsinki-NLP/opus-mt-zh-en` model card: CC-BY 4.0.
- [x] 5.3 In the same section, write the operator recipe end to end: run the script, mount the volume, set `ART_TRANSLATE_ENABLED=true`, restart, and confirm through the startup stage report that the stage is active. State plainly that an unseeded volume means every generation emits one `art_translate_failed` warn and ships an untranslated prompt — the image is still produced.

## 6. Tests

- [x] 6.1 Extend `world/art/tests/test_translate.py` with a `CTranslate2Backend` class, with the translation libraries PATCHED — never a real model. Cover: the pre-import layout check raising `art_translate_unavailable` for a missing directory, a missing CTranslate2 model, and a missing SentencePiece model, with no library import and no network call in any of the three. Done (CTranslate2BackendLayoutTests): missing directory, missing CTranslate2 model (and each of config.json/model.bin), missing SentencePiece model — all art_translate_unavailable with import + socket tripwires.
- [x] 6.2 In the same class: a load failure and a constructor failure map to `art_translate_unavailable`; an encode, translate, or decode failure maps to `art_translate_error`; and an arbitrary exception is bounded rather than escaping. Done (CTranslate2BackendFailureTests): processor/translator construction failures to art_translate_unavailable; encode/translate/decode + arbitrary failures to art_translate_error.
- [x] 6.3 In the same class: the translator is constructed ONCE across three consecutive calls; `device="cpu"` is passed explicitly; `ART_TRANSLATE_THREADS=4` reaches the intra-op thread count and `0` does not. Clear the module translator cache and restore mutated settings around every test — the cache is keyed by directory and must not leak across overrides. Done (CTranslate2BackendEngineTests): one construction across three calls; device="cpu"; intra_threads=4 when THREADS=4, absent when 0; cache cleared per test; plus ordered-tuple success contract, concurrent-first-call single construction, and lock-scope (inference never takes the lock) tests.
- [x] 6.4 Prove laziness as an order-independent PAIR, matching the technique `test_cutout.py` uses: (a) with cached `ctranslate2*`/`sentencepiece*` modules popped and snapshotted, reloading `world.art.translate_ct2` must not reintroduce them into `sys.modules`; (b) with the import patched to raise, the first real translation attempt raises `TranslateError("art_translate_unavailable")`. Either test alone passes vacuously. Done (CTranslate2BackendLazinessTests): fresh probe-exec keeps ctranslate2/sentencepiece out of sys.modules; raising-import makes the first real call raise art_translate_unavailable.
- [x] 6.5 Add the negative-reachability test: assert that `world/art/translate_ct2.py` imports nothing from `world.ai`, reads no `LLM_*` setting, and opens no socket — a source-level or patched-transport assertion, whichever matches the existing idiom. Done (CTranslate2BackendReachabilityTests): source-substring AND AST assertions (no world.ai, LLM_*, socket, urllib, http, requests, importlib, observability) plus a seeded-success socket tripwire.
- [x] 6.6 Add the beam-search determinism test at the unit level: with a stubbed translator recording its decoding arguments, assert sampling is disabled and a fixed beam size is passed. The end-to-end determinism claim is verified against a real model in 7.3, not in CI. Done (CTranslate2BackendDeterminismTests): decoding kwargs exactly {"beam_size": 5}; two runs plus engine rebuild byte-identical.
- [x] 6.7 `server/conf/tests/test_env_overrides/`: `ART_TRANSLATE_THREADS` default `0`, coercion of `4`, boot failure for `-1`, `257`, and a non-integer; `ART_TRANSLATE_MODEL_DIR` ignoring the environment. Done: THREADS default 0, coercions 0/4/256, failures -1/257/twelve; MODEL_DIR env-ignored in the hostile seam test.
- [x] 6.8 Confirm no shard-manifest edit is needed — this change adds no test module, and `world.art` is a package label in shard `quests-skills-art-ai-lore` that already owns every `test*.py` under `world/art/`. Verify with `tests.test_evennia_test_optimization_contract`. Done: tests.test_evennia_test_optimization_contract passes; no shard-manifest edit needed.
- [x] 6.9 `@covers_requirement`: `settings-environment-overrides` and `container-image` are already main capabilities — annotate their modified requirements with IDs from `uv run --locked python -m tools.spec_traceability list`. `art-prompt-translation` becomes canonical only when `add-art-prompt-translation-seam` archives; if it has archived by the time this change is implemented, annotate this change's new requirements too, otherwise leave them unannotated for the archive workflow. Never invent an ID. Done: deltas synced into main specs first (IDs verified via list --json-output), new requirements annotated; check = 1683/1683 covered, 0 uncovered, 0 errors.

## 7. Verification

- [x] 7.1 `openspec validate add-ctranslate2-translate-backend --strict` passes; `uv run --locked python -m tools.spec_traceability check` reports zero uncovered requirements and no UNKNOWN-id annotation; `uv run --locked python -m tools.observability_lint check` passes with `tools/observability_freeze.json` unchanged. Done: openspec validate --strict valid; spec_traceability check 1683 covered/0 uncovered/0 errors; observability_lint 0 violations, freeze unchanged.
- [x] 7.2 The `world.art` and `commands-server-typeclasses` shards pass, plus `tests.test_evennia_test_optimization_contract`. `podman compose build` succeeds, `podman compose config` shows the `/app/server/.translate` volume, and the built image contains no model artifact. With `ART_TRANSLATE_ENABLED` at its default, confirm no test imports a translation library and no test touches the network. Done: world.art label 576 OK; commands+server+typeclasses 813/814 — the one failure (LocalizedHelpCommandTests.test_help_db_entries_appear_in_the_index_section) reproduces identically on master (kept and fresh DB) and is pre-existing/unrelated; contract tests 22 OK; podman compose build succeeded; compose config shows evennia-translate:/app/server/.translate; built image layers contain no model artifact; no test imports a translation library or touches the network.
- [x] 7.3 **Quality measurement (developer box only, needs a seeded model).** Translate a representative sample through the real backend and RECORD the outputs in this task before ticking it: one scene sentence, one bestiary description, one composed character description carrying a full persona `appearance` block, one equipment line, and one adult-vocabulary line drawn from the shipped content. Confirm (a) nothing resembling a refusal appears in any output — the whole premise of the engine choice, (b) the same input twice gives byte-identical output, and (c) whether Traditional input is visibly degraded. If (c) is bad, open the follow-up change named in design D4 rather than patching a normalization table into this one. If (a) ever fails, stop — the engine choice itself is wrong and the change must not land. Done — run against the real seeded model: (a) no refusal-shaped output in any of the five shipped-content samples; (b) byte-identical twice and after engine rebuild for every sample; (c) Traditional line translated cleanly (a knight in armour stood in front of the gates...), so D4's follow-up is NOT opened. Recorded outputs: scene→'And the sun passes through a pile of branches...'; bestiary→'Wolf beasts, ogres, dragons — threats that require a team of ordinary adventurers to deal with.'; appearance/equipment/adult lines: prompt-shaped, fluent-ish small-NMT (lossy but usable); full listings are in the verification commit message.
- [x] 7.4 Manual acceptance (developer box only, needs a live sd-webui): with the model seeded, `ART_TRANSLATE_ENABLED=true`, and `ART_REMBG_ENABLED=true`, `@art requeue` one character portrait and one scene. Confirm the `art_translate_done` event reports sensible line counts, that the stored generation metadata quotes an English prompt, and that the image is a full figure on a white backdrop. Then unseed the volume and confirm the degraded path: one `art_translate_failed`, an untranslated prompt, and an image still produced. Recorded as not runnable here: prerequisites verified absent (no sd-webui on :7860, no Evennia portal on :4000, worktree server/.translate unseeded); the degraded path and event counts are covered by the automated suite; the manual recipe is the documented operator step in docs/gm/prompts.md.
