## 1. Dependencies

- [ ] 1.1 `uv add ctranslate2 sentencepiece`. Do NOT add `argostranslate` — it resolves to 83 packages including `spacy`, `thinc`, `blis`, and `cuda-bindings` (design D1; verified with `uv pip compile` while writing this change).
- [ ] 1.2 `uv sync --locked`, then confirm the resolution added exactly the expected packages and nothing else: `ctranslate2`, `sentencepiece`, and — only if it was not already present via SciPy/Pillow — `numpy` and `pyyaml`. If any `nvidia-*`, `torch`, `spacy`, or CUDA-linked package entered `uv.lock`, STOP and report before writing code.
- [ ] 1.3 Confirm `import ctranslate2` and `import sentencepiece` succeed in the project environment on Python 3.13, and record the resolved versions in this task.

## 2. Settings

- [ ] 2.1 `server/conf/settings.py`: add `ART_TRANSLATE_THREADS = _env_int_bounded("ART_TRANSLATE_THREADS", 0, low=0, high=256)` beside `ART_TRANSLATE_ENABLED`, documenting that `0` means the CTranslate2 default thread count.
- [ ] 2.2 `server/conf/settings.py`: add the code-only `ART_TRANSLATE_MODEL_DIR = os.path.join(GAME_DIR, "server", ".translate")` with the comment stating why it is NOT environment-overridable (persistent-volume path, same rationale as `ART_STORE_ROOT` and `ART_REMBG_MODEL_DIR`).
- [ ] 2.3 Add `ART_TRANSLATE_THREADS` — and only that name — to `_ENV_OVERRIDES` in `server/conf/test_settings.py`.
- [ ] 2.4 Add NO download-enable setting. The server performs no fetch (design D2); a knob for a behaviour that does not exist would be dead inventory and `test_env_example_advertises_no_dead_variables` would be right to reject it.

## 3. The backend

- [ ] 3.1 Create `world/art/translate_ct2.py::CTranslate2Backend` implementing the seam's `translate(lines) -> tuple[str, ...]` contract. Module docstring states the boundary: local CPU NMT behind `world/art/translate.py`'s seam, no network, no observability call (the worker owns the events).
- [ ] 3.2 Check the model layout BEFORE importing any translation library: `ART_TRANSLATE_MODEL_DIR` must exist and hold a CTranslate2 model directory plus a SentencePiece source model. A missing directory or component raises `TranslateError("art_translate_unavailable", ...)` with no import and no network (delta spec: "An unseeded model directory degrades without a fetch"). **Do task 5.1 first**: determine the exact expected filenames against the artifact the fetch script produces and record them in a code comment — do not guess the layout. This is the one out-of-order dependency in this list.
- [ ] 3.3 Import `ctranslate2` and `sentencepiece` LAZILY inside the first call, never at module import, so `world/art/translate_ct2.py` stays importable and the settings module stays loadable where the optional stack is absent.
- [ ] 3.4 Build at most one `ctranslate2.Translator` per configured model directory per process: module-level cache with the lock guarding CONSTRUCTION only (check, lock, re-check), never the translate call (design D5, mirroring `world/art/cutout.py`'s session cache). Pass `device="cpu"` explicitly (design D7) and apply `ART_TRANSLATE_THREADS` as the intra-op thread count when non-zero.
- [ ] 3.5 Implement the call: SentencePiece-encode each source line to tokens, `translate_batch(..., beam_size=<fixed>)` with sampling DISABLED (design D6), decode each result back to text, and return one string per input line in input order. Map every escaping exception — encode, translate, decode — to `TranslateError("art_translate_error", ...)`; map load and construction failures to `art_translate_unavailable`.
- [ ] 3.6 Keep `world/art/translate_ct2.py` free of any `world.observability` import or call, and free of any outbound request of any kind.
- [ ] 3.7 Confirm by reading that no code path in this module can select a GPU device or fall back to one, and that no `LLM_*` setting, `world.ai` client, or completion endpoint is reachable from it (delta spec: "No generative-layer transport is reachable from the stage").

## 4. Container and inventory

- [ ] 4.1 `Containerfile`: add `install -d -m 775 -o root -g 0 /app/server/.translate` to the app-layout stage and `/app/server/.translate` to the final stage's `VOLUME` list, mirroring `/app/server/.rembg`.
- [ ] 4.2 `compose.yaml`: add the `evennia-translate:/app/server/.translate` mount and declare the named volume. No `ART_TRANSLATE_*` interpolation line — these knobs arrive through `env_file: .env` like the rest of the `ART_*` family.
- [ ] 4.3 `tests/test_container_contract.py`: update the pinned exact sets (`writable_paths`, both compose volume sets) for `/app/server/.translate` / `evennia-translate`, and add the annotated volume-scenario test asserting the image itself carries no model artifact.
- [ ] 4.4 `server/conf/tests/test_env_overrides/` (a package): update `ENV_BACKED`, `DEFAULT_REPR`, `VALID_OVERRIDES`, `INVALID_VALUES` in `_support.py` and the AST set in `test_inventory_and_shard_ownership.py`, for `ART_TRANSLATE_THREADS` (env-backed) and `ART_TRANSLATE_MODEL_DIR` (code-only).
- [ ] 4.5 `.env.example`: add `ART_TRANSLATE_THREADS` with type, default, and rule. Do NOT add `ART_TRANSLATE_MODEL_DIR` — the dead-variable check would be right to reject it.
- [ ] 4.6 `docs/development/settings-and-environment.md`: add `ART_TRANSLATE_THREADS` to the inventory table and `ART_TRANSLATE_MODEL_DIR` to the code-only list, plus a short subsection on the model directory layout and the seed-only policy.
- [ ] 4.7 `web/tests/browser/browser_settings.py`: confirm no change is needed — the harness already declares the stage disabled and the double as the backend. State that it was checked.

## 5. Seeding the model

- [ ] 5.1 Write `scripts/fetch-translate-model.sh` following the repo's existing `scripts/` conventions (and the `zsh-script-writer` guidance if the other scripts follow it): download one zh→en translation package to a host path, verify it, unpack it into the layout 3.2 expects, and print where to mount it. It runs on the HOST, never in the server process (design D2). Fail loudly on a missing tool, a failed download, or an unexpected archive layout.
- [ ] 5.2 Document BOTH seeding routes in `docs/gm/prompts.md`: the Argos Open Tech `.argosmodel` archive (unzip yields a CTranslate2 model plus a SentencePiece model — the wrapper package is what this change avoids, not the model artifact), and `ct2-transformers-converter` over `Helsinki-NLP/opus-mt-zh-en` run offline on a workstation. Record each route's licence.
- [ ] 5.3 In the same section, write the operator recipe end to end: run the script, mount the volume, set `ART_TRANSLATE_ENABLED=true`, restart, and confirm through the startup stage report that the stage is active. State plainly that an unseeded volume means every generation emits one `art_translate_failed` warn and ships an untranslated prompt — the image is still produced.

## 6. Tests

- [ ] 6.1 Extend `world/art/tests/test_translate.py` with a `CTranslate2Backend` class, with the translation libraries PATCHED — never a real model. Cover: the pre-import layout check raising `art_translate_unavailable` for a missing directory, a missing CTranslate2 model, and a missing SentencePiece model, with no library import and no network call in any of the three.
- [ ] 6.2 In the same class: a load failure and a constructor failure map to `art_translate_unavailable`; an encode, translate, or decode failure maps to `art_translate_error`; and an arbitrary exception is bounded rather than escaping.
- [ ] 6.3 In the same class: the translator is constructed ONCE across three consecutive calls; `device="cpu"` is passed explicitly; `ART_TRANSLATE_THREADS=4` reaches the intra-op thread count and `0` does not. Clear the module translator cache and restore mutated settings around every test — the cache is keyed by directory and must not leak across overrides.
- [ ] 6.4 Prove laziness as an order-independent PAIR, matching the technique `test_cutout.py` uses: (a) with cached `ctranslate2*`/`sentencepiece*` modules popped and snapshotted, reloading `world.art.translate_ct2` must not reintroduce them into `sys.modules`; (b) with the import patched to raise, the first real translation attempt raises `TranslateError("art_translate_unavailable")`. Either test alone passes vacuously.
- [ ] 6.5 Add the negative-reachability test: assert that `world/art/translate_ct2.py` imports nothing from `world.ai`, reads no `LLM_*` setting, and opens no socket — a source-level or patched-transport assertion, whichever matches the existing idiom.
- [ ] 6.6 Add the beam-search determinism test at the unit level: with a stubbed translator recording its decoding arguments, assert sampling is disabled and a fixed beam size is passed. The end-to-end determinism claim is verified against a real model in 7.3, not in CI.
- [ ] 6.7 `server/conf/tests/test_env_overrides/`: `ART_TRANSLATE_THREADS` default `0`, coercion of `4`, boot failure for `-1`, `257`, and a non-integer; `ART_TRANSLATE_MODEL_DIR` ignoring the environment.
- [ ] 6.8 Confirm no shard-manifest edit is needed — this change adds no test module, and `world.art` is a package label in shard `quests-skills-art-ai-lore` that already owns every `test*.py` under `world/art/`. Verify with `tests.test_evennia_test_optimization_contract`.
- [ ] 6.9 `@covers_requirement`: `settings-environment-overrides` and `container-image` are already main capabilities — annotate their modified requirements with IDs from `uv run --locked python -m tools.spec_traceability list`. `art-prompt-translation` becomes canonical only when `add-art-prompt-translation-seam` archives; if it has archived by the time this change is implemented, annotate this change's new requirements too, otherwise leave them unannotated for the archive workflow. Never invent an ID.

## 7. Verification

- [ ] 7.1 `openspec validate add-ctranslate2-translate-backend --strict` passes; `uv run --locked python -m tools.spec_traceability check` reports zero uncovered requirements and no UNKNOWN-id annotation; `uv run --locked python -m tools.observability_lint check` passes with `tools/observability_freeze.json` unchanged.
- [ ] 7.2 The `world.art` and `commands-server-typeclasses` shards pass, plus `tests.test_evennia_test_optimization_contract`. `podman compose build` succeeds, `podman compose config` shows the `/app/server/.translate` volume, and the built image contains no model artifact. With `ART_TRANSLATE_ENABLED` at its default, confirm no test imports a translation library and no test touches the network.
- [ ] 7.3 **Quality measurement (developer box only, needs a seeded model).** Translate a representative sample through the real backend and RECORD the outputs in this task before ticking it: one scene sentence, one bestiary description, one composed character description carrying a full persona `appearance` block, one equipment line, and one adult-vocabulary line drawn from the shipped content. Confirm (a) nothing resembling a refusal appears in any output — the whole premise of the engine choice, (b) the same input twice gives byte-identical output, and (c) whether Traditional input is visibly degraded. If (c) is bad, open the follow-up change named in design D4 rather than patching a normalization table into this one. If (a) ever fails, stop — the engine choice itself is wrong and the change must not land.
- [ ] 7.4 Manual acceptance (developer box only, needs a live sd-webui): with the model seeded, `ART_TRANSLATE_ENABLED=true`, and `ART_REMBG_ENABLED=true`, `@art requeue` one character portrait and one scene. Confirm the `art_translate_done` event reports sensible line counts, that the stored generation metadata quotes an English prompt, and that the image is a full figure on a white backdrop. Then unseed the volume and confirm the degraded path: one `art_translate_failed`, an untranslated prompt, and an image still produced.
