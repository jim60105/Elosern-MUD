## Batch order (fire wave — integration contract in ./design.md D9)

0. Single-change wave: zero dependencies. Every consumed primitive is SHIPPED and already exercised live at master (the `marker: ground` clause + standing-on-it + exit extinguishment from terrain-marker, the `physical_hit` event + `apply_buff_to_source` + `buff_active` gate from on-hit-counter-damage, `bypass_defense` 處決級 from water/dark, the n-ary DAG + two-parent capstone validator, reverse-edge caps) — this change authors data only over their frozen grammar.

## 1. Registry swap + rulebook data (data only)

- [x] 1.1 Confirm every predecessor contract is shipped and inspect current source/exported references (codegraph/LSP) for the fire registry block, the dev-era buff key (`fire_scorch`), every dev-era registry key (incl. `sacrificial_flame`'s `self_heal` clause), the `marker` clause loader, the `physical_hit`/`apply_buff_to_source`/`buff_active` reaction surface, and `bypass_defense` before editing; re-run the repo-wide census of fire keys and `self_heal`-on-fire references; if any mechanic from the D1 coverage table is missing, STOP and fix it in its owning behavior surface — do not bolt code onto the catalog (the report to the supervisor must justify any resulting split).
- [x] 1.2 Author the 12-node tree exactly per design.md D1 (keys/labels/targets/MP costs/prerequisites/coefficient+policies/effect bindings), deleting the dev-era block wholesale (including `sacrificial_flame`'s off-tree `self_heal`) without alias and adding `scorching_armor`/`crimson_apotheosis`; write the block in the earth block's individually-written `_spell`/`_skill` shape (the `_elemental_spells` builder itself stays for other elements); verify the two-parent capstone (燔祭焰 Lv.10 ＋ 焚世之焰 Lv.10) validates at registry load through the existing lineage validator and tip caps derive unchanged; author `final_blaze` exactly at the shipped `DamagePolicy(bypass_defense=True, predicate=())` shape and the multi-effect policy tuples exactly at D6's sequencing.
- [x] 1.3 Author the fire rulebook rows per D1/D3/D5: re-home `fire_scorch` (rate −5→−8/10 s, duration 300 kept); NEW `fire_ignite` (−8/10 s, 60 s, debuff), `fire_scorching_armor` (empty-modifier self mount, 60 s), `fire_lava` (`marker: ground`, rate −12/10 s, duration 60, tick 10 — D3's authored values), `fire_sacrifice_burn` (−12/10 s, 120 s), `fire_crimson_self` (−20/10 s, 300 s), `fire_crimson_burn` (−40/10 s, 300 s); the ONE reaction rule `scorching_armor_ignite` (`when: {event: physical_hit, buff_active: fire_scorching_armor}`, `then: {apply_buff_to_source: fire_ignite}`) in `state_reactions.yaml` — every row ordinary shipped vocabulary, no new grammar; `fire_mastery` and `progression.yaml` untouched.
- [x] 1.4 Sync `world/rules/rulebook/status_display.yaml` per D5 (`fire_scorch` row kept; add `fire_ignite`/`fire_scorching_armor`/`fire_lava`/`fire_sacrifice_burn`/`fire_crimson_self`/`fire_crimson_burn` rows at the table's label/severity conventions) — the shipped import-time coverage check (`world/rules/status_display.py`) fails closed on any drift between the file and the live buff keys ∪ combat-modifier rule IDs; state-reaction rule ids are NOT in that census (earth-wave verified fact) — the ignite rule id needs no row.
- [x] 1.5 Buff-key census: no dangling `buff_apply:`/`self_buff_apply:` target; `world/rules/tests/test_buffs.py`'s two dev-era `fire_scorch` numeric-pin tests stripped to load/apply/presence/tick-behavior assertions per D7 (behavior kept, NO re-pinned −8/300 values, no row mirroring added); the multi-buff tick-ordering test `test_damaging_ticks_return_ordered_records` is edited in place per D7: keep tick order + record shape + `poisoned`'s own −5 pin + per-record `hp_before` chaining, DROP the fire-inclusive final HP total (`before − 10` goes false at the −8 rung; NEVER re-pin it at −13); `world/rules/tests/test_status_display.py` green (set equality against the post-change displayable set).

## 2. Echo-test retirement and docs hygiene

- [ ] 2.1 Remove the `FIRE_SPELL_CATALOG` table and the echo methods of `FireSpellCatalogTests` (field-equality + effect round-trip against the table) from `world/skills/tests/test_spell_catalogs.py` and the `test_fire_spell_tier_labels_match_the_catalog` tier-label echo from `world/skills/tests/test_cost_tiers.py`, in-file; MOVE (do not retire) `test_elemental_spells_builder_rejects_unknown_element` (builder-shape mechanic) beside the remaining builder users; the bare-`self_heal`-prefix parse test lives in `ClosedVocabularyParseTests` (not the fire class) and stays untouched; UPDATE (not retire, per D5) `FireLineageTreeCatalogTests` in the same file — expected-edge dict to the twelve-edge authored tree (+`scorching_armor`: scorching_wave 3; +`crimson_apotheosis`: sacrificial_flame 10 + final_blaze 10), canopy assertion moved from `sacrificial_flame` to `crimson_apotheosis` (consumed by no edge, consumes two parents), mastery-passives test untouched — matching the RENAMED+MODIFIED `skill-lineage` delta; the synthetic `replace(SKILL_REGISTRY["fire_ball"], cost=…)` band probes in `test_cost_tiers.py` survive untouched (fire_ball re-homes at mp=14); adjust a `tools/test_data_lint.json` per-file entry only where its file stops naming shipped content as that linter's output decides; verify remaining suites' substantive coverage is untouched (no waivers).
- [ ] 2.2 Docs check under the node-data authority rule: `docs/lore/skill-trees/fire.md` untouched; `docs/lore/magic-system.md` §3 fire row edited ONLY if a real contradiction surfaces (none found at authoring — record the check); `docs/development/adding-spells.md` checked for drift only (names no retired key — expected no edit, record the check); `tests/test_command_docs.py` untouched (no command surface changes); the 2026-08-12 design doc is frozen history — never edited.

## 3. Behavioral evidence and integration

- [ ] 3.1 Disposable offline engine scenario exercising each distinct D1 composition through real casts (wave burn −8/10 s × 300 s + expiry; armor ignite on a landed physical hit — attacker carries `fire_ignite` with grant-time attribution — plus magic/miss/tick/sourceless silence, dead-source silence, no chaining off the attacker's own ignition; lava DoT while standing + flee/KO/session sweep + non-marker buffs persisting + expiry; `final_blaze` unconditional-feeling bypass vs high defense (defense subtraction skipped); static 3.4/4.4 settlement; `sacrificial_flame`/`crimson_apotheosis` self-burn landing independent of the damage leg; capstone's three-component settlement incl. enemy −40 vs ally silence vs self −20; branch gates (scorching_wave's three children) + two-parent capstone through the lineage engine; `fire_ball` tier label derives 學徒 unchanged at mp=14; the deleted `self_heal`-on-sacrificial binding rejecting as an unknown-definition-shaped effect id): observe HP/stats/buffs/marker facts/counters/practice and delete only after full proof.
- [ ] 3.2 Synthetic behavior tests for settlement paths not pinned by the sibling suites in a new module `world/rules/tests/test_fire_burn_immolation.py` (ignite reaction composed settlement incl. immunity/dead-source/magic/miss silences and non-chaining; lava marker + exit-extinguishment + expiry settlement; immolation self-burn independence from the damage leg; capstone three-component audience settlement; family branch/capstone gates on fire-shaped synthetic data). No catalog-row equality, key-set, cost/tier table or skill-tree-table echo assertions anywhere (ratified NON-GOAL).
- [ ] 3.3 Finalize `.github/evennia-shards.json` (this change owns the wave's only manifest edit): register `world.rules.tests.test_fire_burn_immolation` in exactly one shard (rules-a, beside `test_earth_terrain_guard`); verify the ownership-contract test.
- [ ] 3.4 Run the focused labels below after editing stops; `tools.observability_lint check` in the same batch; `tools.test_data_lint check`; `tools.spec_traceability check`; `openspec validate fire-spell-catalog --strict`. Canonical IDs via `uv run --locked python -m tools.spec_traceability list` at the separately authorized main-sync; annotate exactly the tests establishing them (the removed 火-element ID `skill-registry::skill-registry-contains-the-full-火-element-spell-set` leaves the ledger with its requirement; the new ID `skill-registry::fire-spell-progression-composes-executable-burn-and-immolation-behavior` is annotated on `test_fire_burn_immolation`; the renamed lineage ID `skill-lineage::the-fire-lineage-ships-as-the-authored-branching-tree-with-a-two-parent-canopy` replaces `skill-lineage::the-fire-lineage-ships-as-the-first-round-linear-tree` in the ledger at the same main-sync (the updated `FireLineageTreeCatalogTests` annotations name the new ID) — defer the annotation to the main-sync if it reads red pre-sync, per the established pattern).

### Focused invocation

```sh
uv run --locked evennia test --settings test_settings.py --keepdb world.skills.tests.test_spell_catalogs world.skills.tests.test_cost_tiers world.rules.tests.test_skill_lineage world.rules.tests.test_fire_burn_immolation world.rules.tests.test_buffs world.rules.tests.test_phase_spell_reactions world.rules.tests.test_damage_state_feedback world.rules.tests.test_on_hit_counter_damage world.rules.tests.test_status_display world.skills.tests.test_skill_lineage world.rules.tests.test_freeform_casting
uv run --locked python -m tools.observability_lint check
uv run --locked python -m tools.test_data_lint check
uv run --locked python -m tools.spec_traceability check
uv run --locked openspec validate fire-spell-catalog --strict
```

Pass `MUD_TEST_SETTINGS=1` through the tool environment, never a shell prefix. `world.rules.tests.test_fire_burn_immolation` is the intended synthetic settlement module owned by this change, not an existing-test claim (tasks 1.1 confirms the shipped reaction-test module names; adjust labels to actual modules, never claiming a non-existent file). No full local suite/browser/aggregate-coverage run; no command above ten minutes. No apply, archive, main-spec sync or merge in the proposal turn.
