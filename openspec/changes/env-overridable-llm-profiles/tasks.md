# tasks — env-overridable-llm-profiles

## 1. Profile field extension (world/ai/profiles.py)

- [ ] 1.1 Add the optional typed fields to `LLMProfile` per design D-A5: `api_key`/`app_title`/`app_url` (`str = ""`, `api_key` via `field(repr=False)`), the seven float/int sampling fields (`| None = None`), `reasoning_enabled: bool | None = None`, `reasoning_effort: str | None = None`, `reasoning_style: str = "openrouter"`, `max_completion_tokens: int | None = None`.
- [ ] 1.2 Align the module's six-layer wording drift with the seven-name `LAYER_NAMES` (docstrings, `UnknownLayerError` message); assert in `test_profiles.py` that all seven layers resolve and that `action_options`/`title_nomination` keep their 320/640 code defaults.
- [ ] 1.3 Extend `validate_profile_values` with every D-A5 bound (finite-float checks first, inclusive/exclusive endpoints per table, closed sets for `reasoning_effort`/`reasoning_style`), raising `ProfileValidationError(layer, field, ...)`.
- [ ] 1.4 Add `default_profiles(defaults: Mapping[str, Mapping[str, Any]] | None = None)` merging injected per-layer overrides over code defaults inside profiles.py; delete the `os.environ`/`OLLAMA_BASE_URL` read and its import; verify with a source-grep test that the module has zero environment reads.
- [ ] 1.5 Annotate the `test_profiles.py` additions with `covers_requirement` for the `llm-profiles` IDs produced by `uv run --locked python -m tools.spec_traceability list` (registry-with-optional-fields, seven-layers/defaults-survive, strict-validation-extension, repr-excludes-key, injected-defaults trio, secret-layer-merge); run `uv run --locked python -m tools.spec_traceability check`.

## 2. Inert knob module + settings resolution

- [ ] 2.1 Create `server/conf/llm_knobs.py` (import-safe, stdlib-only, zero environment reads): `LlmKnob` record type, `LLM_KNOBS` (the 23-row table of design §3.1: profile field, env suffix, kind, bounds/default), and pure `llm_env_names() -> frozenset[str]` generating global `LLM_<SUFFIX>` + all `LLM_<LAYER>_<SUFFIX>` names (184 total) from `LLM_KNOBS × LAYER_NAMES`; unit-test exact set equality (global subset equals the 23 documented names; total equals 23 + 23×7).
- [ ] 2.2 In `settings.py` add `_env_optional` (absent/blank → `None`, invalid → `ImproperlyConfigured`) and `_env_optional_bool` (tri-state over the existing word list) beside the current helpers.
- [ ] 2.3 Replace `LLM_PROFILES = default_profiles()` (~line 239): import the knob module, resolve the raw map by looping `LLM_KNOBS × LAYER_NAMES` (per-layer env > global env > code default, preserving the 320/640 per-layer defaults) through the `_env_*` helpers, snapshot it as `_ENV_RESOLVED_LLM_PROFILES`, and pass it as `default_profiles(defaults=...)` overrides — per design D-A3, only env-resolved fields are injected.
- [ ] 2.4 Implement design D-A7 per-layer secret merge in `settings.py`: before the `secret_settings` star-import keep the snapshot; after it, if the imported namespace defines `LLM_PROFILES`, re-merge per layer (named layers replaced wholesale, others = snapshot entry); keep `build_profiles(LLM_PROFILES)` as the post-import validation (line ~421) untouched.
- [ ] 2.5 Publish `LLM_ENV_NAMES = frozenset(llm_env_names())` from `settings.py` (derived export only).
- [ ] 2.6 `server/conf/test_settings.py`: keep the literal `_ENV_OVERRIDES` tuple (inventory AST extractor unchanged) and add a second pop loop over `server.conf.llm_knobs.llm_env_names()` before the production star-import.
- [ ] 2.7 Server/conf tests, new `server/conf/tests/test_llm_env_overrides.py`: per-knob boot matrix (unset → default, blank-omittable → `None`, valid, boundary, out-of-bounds error naming variable/raw/rule); global-vs-per-layer precedence; tri-state `LLM_REASONING_ENABLED` absent vs false; secret-merge scenarios via the existing subprocess settings-import pattern (global + unrelated per-layer env values + one-layer secret map → unaffected layers keep env values; secret layer wholesale; unknown secret layer fails boot). `@covers_requirement` with `settings-environment-overrides` + `llm-profiles` delta IDs; add the module to exactly one shard of `.github/evennia-shards.json` (append to the `server` label) and verify `tests.test_evennia_test_optimization_contract`.

## 3. Rename OLLAMA_BASE_URL → LLM_BASE_URL (clean cutover)

- [ ] 3.1 `compose.yaml`: evennia-service entry becomes `LLM_BASE_URL: ${LLM_BASE_URL:-http://host.containers.internal:11434}`; delete `OLLAMA_BASE_URL`.
- [ ] 3.2 `tests/test_container_contract.py`: assert `LLM_BASE_URL` present with the host-gateway default and `OLLAMA_BASE_URL` absent from the rendered compose.
- [ ] 3.3 `.env.example`: replace the `OLLAMA_BASE_URL` block with the 23 global `LLM_*` entries in the file's per-knob comment format (type, default, blank-semantics, pointer to the settings reader), plus a worked vLLM block, a worked OpenRouter block (base URL + `LLM_API_KEY` + app title/url placeholders), and the per-layer suffix grammar with two examples; match the file's existing commented-entry convention.
- [ ] 3.4 `server/conf/tests/test_env_overrides.py`: delete the `OLLAMA_BASE_URL` `EXTERNAL_READERS` entry; extend the inventory check — the AST-extracted literal reads keep their current assertion, and LLM names are checked against `llm_knobs.llm_env_names()`: `.env.example` active global `LLM_*` keys set-equal the function's global subset; popped-names extraction updated to the literal tuple ∪ the knob module; update the module docstring.
- [ ] 3.5 Update `world/ai/tests/test_profiles.py` env-patch tests (`test_env_variable_selects_the_compose_host`): injection-based per the new signature; compose-host behaviour asserted through the subprocess settings-import pattern with `LLM_BASE_URL` set.

## 4. Regression and boundary guards

- [ ] 4.1 Byte-compatibility regression: with no `LLM_*` environment set, assert a constructed profile's client body builder output (model/messages/temperature/max_tokens only) is exactly today's shape — guards design D-A6 (serialization untouched until the follow-up change); place in `world/ai/tests/test_client.py` without touching client code.
- [ ] 4.2 `repr`/`str` of a profile constructed with `api_key="super-secret"` contains no key substring (`test_profiles.py`).
- [ ] 4.3 Offline invariant unchanged: `LLM_<LAYER>_ENABLED=off` still short-circuits through the existing stub path; the five `_raw()`/`_profiles()` test helpers that call `default_profiles()` with no args keep compiling (keyword-only default) — no edits unless compile forces them.

## 5. Docs and handoff

- [ ] 5.1 `docs/development/settings-and-environment.md`: new LLM knob table (variable, profile field, type/bounds, default, blank semantics) incl. per-layer suffix grammar, the two-tier env precedence, and the D-A7 per-layer secret-merge rule (a secret entry replaces only the layers it names); rename the old `OLLAMA_BASE_URL` row to `LLM_BASE_URL` with reader `server/conf/settings.py`. Do NOT yet add the API-key policy reversal note (lands with `llm-endpoint-wire-configuration` when the key becomes observable).
- [ ] 5.2 Verify focused labels (`world.ai.tests.test_profiles`, `server.conf.tests.test_llm_env_overrides`, `server.conf.tests.test_env_overrides`, `tests.test_container_contract`) then `MUD_TEST_SETTINGS=1 uv run --locked evennia test --settings test_settings.py --noinput --parallel 16 commands server typeclasses world web.webclient`; `uv run --locked python -m tools.spec_traceability check`; `openspec validate env-overridable-llm-profiles --strict`; `git diff --check` clean.
