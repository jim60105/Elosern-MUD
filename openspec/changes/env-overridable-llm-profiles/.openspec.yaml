schema: spec-driven
created: 2026-09-01
goal: Make every LLM profile field environment-overridable (global + per-layer),
  rename OLLAMA_BASE_URL to LLM_BASE_URL, and add typed optional profile fields
