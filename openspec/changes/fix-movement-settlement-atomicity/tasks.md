## 1. Movement settlement boundary module

- [ ] 1.1 Create `world/rules/movement_settlement.py` with `settle_movement(traversing_object, source_location, *, traverse, wilderness_coordinates=None, wilderness_source_coordinates=None)` and a pre-move snapshot: traverser location and wilderness registration; co-located companion candidate states (same predicate and per-NPC exception isolation as `follow_companions`); the involved wilderness script's `itemcoordinates`, `rooms`, and `unused_rooms` as shallow reference-sharing copies (never `deepcopy` on object-keyed dicts), resolving the script by lookup for gate entry and from the traverser's `ndb.wilderness` for steps; traverser surfaces `map_knowledge`, `guide_progress`, `onboarding_beat`, `first_arrival_seen`, plus `quest_log` and the destination room's `pin_reasons`/`interacted` through the quest module's own helpers (`world/quests/transitions.py:83-98`)
- [ ] 1.2 Consume the clock advance-surface seam: build the merged `build_advance_snapshot_registry` registry plus clock-tick snapshot before the transaction when the `fix-clock-rollback-cache-sync` seam exists, with a documented fallback to the current `_ADVANCE_ENTITY_SURFACES` declaration and a guarded integration test until that change lands
- [ ] 1.3 Implement the transaction shape: `transaction.atomic()` around `traverse()`, with the `except` outside the `with` block so compensation runs after rollback; compensate and re-raise on exception, and also compensate without an exception when `traverse()` returns falsy while the traverser relocated (wilderness lineages only — they return real booleans)
- [ ] 1.4 Implement compensation primitives: hook-free return relocation (`move_to(..., quiet=True, move_hooks=False)`), wilderness entry undo (quiet `move_to` back to the grid room plus `at_post_object_leave`), wilderness step undo (`move_obj` to the source coordinates), wilderness grid-return undo (`move_obj(traverser, wilderness_source_coordinates)`), direct-cache fallback (`location` property assignment plus `contents_cache.init()` on source and destination), shallow restore of the three wilderness bookkeeping attributes, and surface restore through `world.rules.surfaces.restore_attribute_best_effort` plus the quest module's `restore_*` helpers
- [ ] 1.5 Make every compensation step best-effort with `log_warn` and a fixed deterministic order (traverser, companions, wilderness bookkeeping, attributes); compensation failure must not mask the original failure

## 2. Wire the exit lineages

- [ ] 2.1 Add `MovementCostMixin.at_traverse` override in `typeclasses/exits.py` that opens the settlement boundary around `super().at_traverse(...)`; keep `at_post_traverse` and the `after_successful_movement` call unchanged; update the mixin's class docstring to describe the boundary
- [ ] 2.2 Route `WildernessGateExit.at_traverse`'s body (pre-move veto, `enter_wilderness`, messaging, `after_successful_movement`) through the settlement boundary with `wilderness_coordinates=entry.wilderness_xy`
- [ ] 2.3 Route `WildernessReturnExit.at_traverse`'s special-cased return branch through the boundary (with `wilderness_source_coordinates`), and the ordinary `super().at_traverse()` fallback through the boundary (with both wilderness coordinate kwargs)
- [ ] 2.4 Correct the `after_successful_movement` docstring so it no longer claims every helper "never raise[s] from a traversal hook" — the charge step can raise and is now compensated by the boundary

## 3. Tests

- [ ] 3.1 Test (EvenniaTest, `world/rules/tests/test_movement_settlement.py`): clock-charge failure during a plain `MovementCostMixin` exit move returns player and companion to the source; source/destination contents caches, clock tick (in-process value), map-knowledge record, onboarding state, `quest_log`, and destination room `pin_reasons`/`interacted` all unchanged
- [ ] 3.2 Test: a genuine raise from a settlement step after `follow_companions` (patch the room-entry observer to raise) returns every moved companion and the player to their pre-move locations
- [ ] 3.3 Test: clock-charge failure during `WildernessGateExit` entry returns the player to the grid room and restores the wilderness script's full bookkeeping (`itemcoordinates`, `rooms`, `unused_rooms`) with no rolled-back zombie rooms, including the fresh-room entry case
- [ ] 3.4 Test: clock-charge failure during an ordinary wilderness step (`WildernessReturnExit` fallback) returns the player to the source coordinates and re-registers them in the wilderness script
- [ ] 3.5 Test: falsy-return-with-relocation on the wilderness return lineage is compensated as a failure (no clock charge, no knowledge record, bookkeeping restored)
- [ ] 3.6 Test: commit-time cache reconciliation verified by directly invoking the compensation against a deliberately constructed divergent in-process state (locations, contents caches, attributes, tick) and asserting fresh-read equivalence — do NOT use `set_rollback(True)`, which silently rolls back nested savepoints without raising inside Django test cases
- [ ] 3.7 Test (web/webclient/actions/tests/test_exploration_actions.py): WebClient `_move_adapter` returns `move_failed` with the player still at the source after a failing charge
- [ ] 3.8 Test (typeclasses/tests/test_exit_movement_cost.py): source-inspection — `MovementCostMixin.at_traverse` delegates to the settlement boundary and contains no inline `get_world_clock().advance`; `at_post_traverse` still calls `after_successful_movement(...)` with `cost_key=self.movement_cost_key`
- [ ] 3.9 Test: a non-`PlayerCharacter` traverser's successful move passes through the boundary unchanged (no charge, no record, location correct)
- [ ] 3.10 Run the movement, exit, party, wilderness, exploration-action, and clock test packages; keep the existing `movement-cost-charging` traceability annotation green and add the `covers_requirement` annotation for the modified requirement ID to the 3.8 source-inspection test (the ID already exists in the main index)

## 4. Traceability and handoff

- [ ] 4.1 At archive time (after the delta specs are synced into `openspec/specs/`), add `covers_requirement` annotations for the new `movement-settlement-atomicity` requirement IDs to the new tests AND to the existing success-path tests that substantively cover the new scenarios (`test_exit_movement_cost.py::test_player_traversing_plain_exit_advances_clock_by_move`, the wilderness gate/return coverage in `typeclasses/tests/test_exits.py`); do not add them now — the IDs do not exist in the main index and would fail `tools.spec_traceability check` with unknown-requirement-id
- [ ] 4.2 Run `openspec validate --change fix-movement-settlement-atomicity --strict`, `uv run --locked python -m tools.spec_traceability check`, and the affected test domains (non-browser Evennia packages touched; no browser-suite requirement unless exploration UI tests are affected)
- [ ] 4.3 Verify `git diff --check` is clean and no file outside `openspec/changes/fix-movement-settlement-atomicity/` was modified by the change's own artifacts
