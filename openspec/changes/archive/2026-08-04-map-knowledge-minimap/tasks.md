## 1. Map Knowledge Core

- [x] 1.1 Implement `world/rules/map_knowledge.py` with the versioned record writer (`record_arrival`), node-ID `encode`/`decode`/`validate` helpers for `grid:`/`wild:`/`room:` grammar with registered-component bounds, and strict rejection of malformed/out-of-bounds IDs; add pure tests for round-trips, malformed input, unknown prefixes, non-integer coordinates, and unrepresentable locations.
- [x] 1.2 Implement `parse_knowledge(character)` returning a deterministically ordered read view and raising a dedicated error on missing/malformed/unknown-version/invalid-node/non-integer-tick records without mutating the stored value; add tests for ordering, corruption isolation, and untouched storage.
- [x] 1.3 Wire `record_arrival(traversing_object)` into `typeclasses/exits.py::MovementCostMixin.at_post_traverse` after `charge_movement` and add Evennia tests proving a successful grid/interior/instance/Limbo traversal charges exactly once and records the destination node while a locked or vetoed traversal records nothing; add regression tests that direct `move_to(..., move_type="teleport")` and `move_to(..., quiet=True)` calls record no observation.
- [x] 1.4 Wire `record_arrival` into the `WildernessGateExit.at_traverse` and both `WildernessReturnExit.at_traverse` success branches after `charge_movement`, add Evennia tests for entry/ordinary-step/return recording and failed-step non-recording, and update any existing wilderness tests that assert no side effect beyond charging.
- [x] 1.5 Call `record_arrival(character)` in `world/rules/onboarding.py::relocate_to_starting_location` after a successful relocation (no clock advance) and add tests proving activation records `grid:capital_altoria:2:0`, missing-South-Gate and failed-relocation record nothing, and activation rollback never rolls back relocation or knowledge.
- [x] 1.6 Add a test proving `record_arrival` no-ops without raising or resetting history when the pre-existing record is corrupt, so a normal traversal still completes and the stored value is unchanged.

## 2. Reclamation Pruning

- [x] 2.1 Implement `world/rules/map_knowledge.py::prune_reclaimed_room(room_id)` removing `room:<room_id>` from every affected player's record; it must select only `PlayerCharacter`s already carrying the knowledge attribute, strictly parse each record, write back only records containing the target node, snapshot/restore every affected value on failure, return a boolean success indicator, and raise a dedicated `KnowledgePruneError` only on a genuine persistence failure while logging a diagnostic.
- [x] 2.2 Call `prune_reclaimed_room(room.id)` inside `world/maps/instance.py::reclaim_due_instances`'s `transaction.atomic()` reclaim block, **before** `_clear_non_player_entities` and `room.delete()`; on `KnowledgePruneError`, mark the transaction for rollback and append `"instance_reclaim_deferred"` only after leaving the atomic block, never emitting `"instance_reclaimed"` for a rolled-back transaction.
- [x] 2.3 Add Evennia tests for pruning on reclaim, untouched unrelated players (no attribute created or rewritten), a player without a knowledge attribute staying without one, promotion retention, no pruning on promotion, statement order (prune before entity/room mutation), and injected persistence-failure rollback with the room/entities intact and a deferred event returned.

## 3. Presenter and Layer Adapters

- [x] 3.1 Implement the read-only `local_map` presenter (`web/webclient/presentation/local_map.py`) building from `world.rules.map_knowledge.parse_knowledge` and the actor's location, with exact version-1 node/edge/legend/visibility schema, the shared bounds from design D10a (max 64 nodes, 128 edges, 16 legend entries, string/ID/coordinate limits, closed `visibility`/`layer`/`action` values), `current`/`visible_unvisited`/`visible_visited`/`remembered` states, bounded remembered nodes by most-recent `last_seen`, renderer-local node positions, and `PanelUnavailableError` for absent/unrepresentable/corrupt cases; register `local_map` in the production registry and update registry tests that assume a status/combat-only allowlist.
- [x] 3.2 Implement the grid/anchor adapter reading the XYMap node/link model, configured `map_visual_range`/`map_mode` (from the map's `options`), and anchor annotations — never the rendered ANSI string — with edges traversable only when a real `CostedXYZExit`/`Exit` exists, failing closed to the stable unavailable reason on invalid or out-of-range `options`; add presenter tests for visual range, anchor flags, unknown-node omission, and invalid-options fail-closed behavior.
- [x] 3.3 Implement the wilderness adapter reading provider bounds, legal adjacent coordinates, terrain labels, and the visited coordinate set; add presenter tests for bounds, terrain labels, and remembered cells.
- [x] 3.4 Implement the instance/interior adapter resolving the current room and real Exits, rendering current/origin/return/visited adjacent nodes and `visible_unvisited` one-hop destinations labelled `未探索` with withheld room names; add presenter tests for the one-room instance graph, the guild/store interior graph, and coordinate-free edges.
- [x] 3.5 Add exact payload-validation tests for the `local_map` schema (server side) including every bound in D10a, a worst-case serialization test proving the largest legal payload still fits under the 65,536-byte envelope, deterministic ordering, and presenter isolation with a healthy status/narrative panel.

## 4. Browser Minimap Renderer

- [x] 4.1 Extend `elosern/protocol.js` with the `local_map` panel allowlist entry and exact available/unavailable validators sharing the D10a constants with the server (guarded by a dual-direction parity test) plus Node tests for valid, malformed, unavailable, remembered-node bounds, malformed movement descriptors, and unknown-node rejection cases.
- [x] 4.2 Implement a DOM-independent `local_map.js` render model reducing the validated panel into nodes/edges/legend/focus targets with `current`/`visible_*`/`remembered` states distinguishable by shape/border/label in addition to color and remembered-remote-node focus carrying no travel action; add Node tests for state distinction, focus, and bounded rendering.
- [x] 4.3 Replace the GoldenLayout `local-map` placeholder with the minimap renderer subscribing to the validated `local_map` panel, rendering legend text as text nodes, preserving the single-sync recovery and offline lock, and keeping narrative/text input usable when the panel is unavailable; update shell/layout tests that currently assert a placeholder.

## 5. Managed Browser Acceptance

- [x] 5.1 Extend deterministic browser fixtures with grid, wilderness, instance, and interior placements (including an activation relocation at 南門) without touching the developer database or invoking a remote, LLM, or image service.
- [x] 5.2 Add Playwright journeys proving the minimap renders for grid/wilderness/instance/interior rooms, current/visible/remembered states are distinguishable without color alone, focusing a remembered remote node shows its name/landmark with no travel action, and unknown nodes never appear in the DOM.
- [x] 5.3 Add a reconnect journey proving the minimap rebuilds from server-persisted knowledge in the new epoch's snapshot and no client map cache is authoritative.
- [x] 5.4 Verify at 1440x900 and 1280x720 that narrative, status, and the minimap remain visible, keyboard-operable, literal-text safe, and localhost-only.

## 6. Regression, Spec Sync, and Verification

- [x] 6.1 Run focused map-knowledge, movement, onboarding, instance, presenter, protocol, Node, and browser tests while iterating; fix regressions without weakening atomicity, target-validation, single-writer, envelope bounds, or accessibility assertions.
- [x] 6.2 Compare implementation and tests with all eight delta specs and the approved map/exploration design; confirm movement/look/interaction/dialogue/rest-wait adapters, service, creation, art, and combat remain outside this change and that no production action is registered.
- [x] 6.3 Sync the two new capability and six modified capability deltas into `openspec/specs/`, inspect the merged requirements, obtain canonical IDs with `uv run --locked python -m tools.spec_traceability list`, and annotate only substantively matching Python unit/integration/browser tests.
- [x] 6.4 Run `uv run --locked python -m tools.spec_traceability check`, `node --test web/static/webclient/js/tests/*.test.js`, and `uv run --locked python -m unittest discover -s web/tests/browser -t .` with the shared evidence path; close every genuine requirement gap without skipped or placeholder evidence.
- [x] 6.5 Run both required evidence-producing Python entry points and `uv run --locked python -m tools.spec_traceability verify --evidence <shared-path>`; preserve successful evidence for all current main requirements.
- [x] 6.6 Run the exact two-file aggregate branch-coverage sequence for `commands`, `server`, `typeclasses`, `web`, and `world`, verify coverage roots, and keep combined branch coverage at or above 90% with only `*/tests/*` omitted.
- [x] 6.7 Run `openspec validate map-knowledge-minimap --strict`, `openspec validate --all --strict`, `uv run --locked python -m compileall -q world typeclasses commands server web`, and `git diff --check`; verify no runtime/test path contacts a remote service or developer database.
