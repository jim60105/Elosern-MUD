## 1. Profile Registry

- [x] 1.1 Implement `world/ai/profiles.py` with a frozen `LLMProfile` dataclass carrying `base_url`, `path`, `headers`, `model`, `temperature`, `max_tokens`, `timeout_seconds`, `max_retries`, `supports_response_format`, and `enabled`, plus an `UnknownLayerError` for invalid layer keys.
- [x] 1.2 Implement strict construction-time validation that fails closed with named errors naming the layer and field: finite `temperature` in `0..2`, positive-integer `max_tokens` and `timeout_seconds`, non-negative-integer `max_retries`, non-empty `base_url`/`path`/`model`, boolean `enabled`/`supports_response_format`, and `headers` as an immutable `Mapping[str, tuple[str, ...]]` whose keys are strings and values are non-empty sequences of strings; add tests for every failing bound and for correct accepted values.
- [x] 1.3 Implement `LLM_PROFILES` registry construction from the Django setting keyed by exactly `narrator`, `npc_dialogue`, `scenario_director`, and `scene_builder`, with `get_profile(layer)` raising `UnknownLayerError` for unknown keys; test resolution for all four layers, unknown keys, duplicate keys, and a side-effect-free module import.
- [x] 1.4 Implement default profile values derived from `OLLAMA_BASE_URL` (fallback `http://127.0.0.1:11434`) and the fixed path `/v1/chat/completions`, with `supports_response_format` defaulting false; test both runtimes via `override_settings`/env patching: the bare-metal localhost fallback when unset and the exact `host.containers.internal:11434` value when the compose-injected variable is present.
- [x] 1.5 Add `LLM_PROFILES` defaults to `server/conf/settings.py` and a test asserting the four layer profiles are present, enabled, and locally targeted.

## 2. OpenAI-Compatible Client

- [x] 2.1 Implement `world/ai/client.py` `OpenAICompatClient(LLMClient)` that takes a frozen `LLMProfile` and an optional injected reactor in `__init__`, rebuilding the connection pool and `Agent` on the injected reactor when supplied; test that it constructs with a profile, never reads the global `LLM_HOST`/`LLM_PATH` settings, and that an injected reactor is honored without patching the global reactor.
- [x] 2.2 Override `_format_request_body(descriptor)` to build the OpenAI `/v1/chat/completions` body from a layer-neutral per-call descriptor carrying `messages` plus optional `output_schema`/`schema_id`: `model`, `messages`, `temperature`, `max_tokens`, and `response_format` exactly when the profile's `supports_response_format` flag is true and the descriptor declares a schema; test the exact payload for all four combinations of flag/schema presence.
- [x] 2.3 Override response parsing to return `choices[0].message.content` from the OpenAI envelope and implement `get_response(descriptor)` returning a Deferred; test successful parsing, and safe failure for non-200 status, connection error, timeout, and malformed/non-JSON bodies with no exception escaping and no raw payload logged.
- [x] 2.4 Implement the per-profile request timeout covering the complete exchange through response-body parsing, errbacking the Deferred within a bounded delay after `timeout_seconds` and cancelling/aborting the underlying request where Twisted supports it; test with `twisted.internet.task.Clock` that the Deferred errbacks deterministically for both a non-responding endpoint and an endpoint that sends headers but never completes the body, without blocking the caller.
- [x] 2.5 Add the chat-completion response envelope schema under `world/ai/schemas/` and use it in the client parse path; test schema-valid and schema-invalid envelopes and confirm the schema module is importable through the existing `schemas` package seam.

## 3. Guardrail Pipeline

- [x] 3.1 Implement `world/ai/guardrail.py` with per-layer semantic-validator and degrade-fallback registries supporting stable-order execution and later registration by layer name; test registration, ordering, duplicate registration rejection, and that no registration writes game state.
- [x] 3.2 Implement the async `guarded_call` pipeline that consumes transport through an injected client protocol (never importing `OpenAICompatClient`): a disabled profile short-circuits to the degrade fallback with no client invocation; a transport failure (timeout/connection/HTTP/unparseable non-JSON body) degrades directly without entering the validation loop; test both paths with `FakeLLMClient` failure fixtures and assert no error message is appended to a prompt on transport failure.
- [x] 3.3 Implement the validation–retry loop: parse the returned text, run the declared jsonschema then the layer's semantic validators, append every error message to the prompt on failure, and retry up to `1 + max_retries` total calls while leaving the original messages unchanged; test first-attempt acceptance, retry with the full round's errors appended, exhausted-budget degrade, and that only validation failures (not transport failures) append error text. Use a schema-invalid JSON fixture (not a non-JSON body) to exercise the retry path.
- [x] 3.4 Add mutation-boundary tests proving the guardrail, client, profiles, and fake client never write persistent state: compare trait storage, attributes, Script count, and world tick before/after repeated guarded calls, and verify logged summaries contain no prompt text, player content, or local path.

## 4. Fake Client and Test Contract

- [x] 4.1 Implement `world/ai/fake_client.py` `FakeLLMClient` with the same Deferred-returning `get_response` interface, resolving recorded fixtures keyed by a stable request matcher with no socket activity; test matched responses, unmatched-request named errbacks, and Deferred-interface parity with the real client signature.
- [x] 4.2 Implement scriptable failure modes (timeout, HTTP error status, connection error, unparseable non-JSON body) keyed by request matcher; test each drives the guardrail's transport-failure degrade path, and that a fixture which parses as JSON but fails the output schema drives the validation-retry path instead.
- [x] 4.3 Add a repository-level contract test with a precise boundary: only `world/ai/client.py` may import `OpenAICompatClient`, Evennia's `LLMClient`, or a Twisted `Agent`; `guardrail.py`, `profiles.py`, schemas, the fake client, and future layer modules must not import a live transport; every test outside the client's own unit-test module must never construct `OpenAICompatClient` (only `test_client.py` may, and only with an injected reactor/stubbed agent); and existing deterministic-path bans (`world/rules/`, `world/maps/`, `world/quests/`, commands must not import ollama/llm_client) remain intact.
- [x] 4.4 Write focused `world.ai` tests, run the full Evennia suite plus the top-level regression check, run the spec-traceability check, and run strict OpenSpec validation on the change. The four new capability requirements enter the traceability index only after their deltas sync into `openspec/specs/` (per `docs/development/spec-test-traceability.md`); the annotations for them are added at that sync step, not during this change's apply.
