## 1. Menu model

- [x] 1.1 In `web/static/webclient/js/elosern/exploration_menu.js` (the UMD source behind `web/webclient-app/lib/exploration_menu.js`), make the `navigate`-kind affordance branch emit `openDrawer: "shop"` (no `openServiceSubmenu`) when `affordance.surface === "shop"`, keeping `key: "service-shop"`, the label fallback, `enabled`, `description: null`, and `disabledReason`; the `surface === "guild"` row stays byte-identical (`openServiceSubmenu: "guild"`). Update the file header comment for the new shop shape. Verify with the Node tests in 1.2.
- [x] 1.2 Update `web/static/webclient/js/tests/exploration_menu.test.js` and `hud_dock_menus.test.js`: the shop navigate row carries `openDrawer: "shop"` and no `openServiceSubmenu`, a disabled shop navigate row keeps its server reason, and the guild navigate row is unchanged. Run `node --test web/static/webclient/js/tests/*.test.js` green.

## 2. Store and shell wiring

- [x] 2.1 In `web/webclient-app/stores/elosern/interaction.js`, replace both `item.openDrawer === "inventory"` interceptions (`handleExplorationItem`, `handleServiceItem`) with one shared allow-set of frameless drawer names (`inventory`, `shop`) that calls `ctx.openHudDrawer(item.openDrawer)` only — no `setActiveSubDock`, no push, no `setServiceSurface`. A disabled row never reaches the handler (router gate), so no extra check is added. Verify with the Vitest tests in 3.1.
- [x] 2.2 In `web/webclient-app/stores/elosern/hud.js`, add `"shop"` to `FRAMELESS_DRAWER_NAMES` and update the adjacent comment so closing the shop drawer returns before the hosted pop and the sub-dock re-home. In `web/webclient-app/composables/use-dock.js`, add `d === "shop"` to the frameless exclusion in `drawerHostsServiceFrame` and update its comment. Leave `SERVICE_SURFACE_DRAWERS`, the `services.shop|stock|sell` resolvers, and the quantity form untouched (design D3). Verify with 3.2 and 3.3.
- [x] 2.3 In `web/webclient-app/components/ShopPanel.vue`: add `@focusin="activateRow(row)"` to each stock and sellable row; bind each quantity `<input>`'s `min` to the row action's `quantity.min` (fallback `1`) and keep `max` on `quantity.max`, reconciling the `max` fallback with `quantityBounds()` (one shared bounds helper feeds the attributes, the clamp, and `buyNow` / `sellNow`, so a missing bound never means `1` in one place and unbounded in another); add a `@change` clamp that writes the entry back into `[min, max]` (non-integer or empty → the action's `min`); keep the bound check in `buyNow` / `sellNow`. Keep the `quantityForm` prop and its watch (deleted by `retire-service-keyboard-frames`). Verify with 3.4.

## 3. Tests with the behavior

- [x] 3.1 Vitest store tests (`web/webclient-app/tests/store/frameless_bag.test.js`, `declarative_surfaces.test.js`): activating the `service-shop` navigate row in an open target frame opens the shop drawer with frame stack, depth, current descriptor, `activeSubDock`, recorded service surface, and breadcrumb unchanged; a services-panel commit while the shop drawer is open never records a hosted surface or pushes a frame. Re-point every existing "shop activation still pushes and hosts" regression in these files at the guild surface (`service-guild` → `services.guild` pushes and the quest drawer hosts). Run the focused files with `pnpm exec vitest run <files>` (repository root) green.
- [x] 3.2 Vitest close-path tests (`web/webclient-app/tests/store/hud_drawer.test.js`, `tests/app_client_drawers.test.js`): closing the shop drawer by Escape, close control, and scrim pops nothing, dispatches nothing, restores focus to the `service-shop` row, and leaves `activeSubDock` / depth / current frame / breadcrumb exactly as before the open; closing the guild-hosted quest drawer still pops exactly one level (regression).
- [x] 3.3 Vitest composition test (`web/webclient-app/tests/app_client_frameless_bag.test.js`, or a new `app_client_frameless_shop.test.js` if it keeps the bag file focused): with the shop drawer open — including with a shop-family descriptor forced current through `store.router.pushFrame({ source: "services.stock" })` — no `[data-testid="dock-menu"]` or `[data-testid="dock-detail"]` renders inside the drawer and the drawer body's only child surface is `[data-testid="shop-panel"]`; the quest drawer still hosts its guild frame's rows (regression). Replace the existing `services.shop`-as-hosted-counterexample assertions with the guild equivalent.
- [x] 3.4 Component test `web/webclient-app/tests/world/shop_panel.test.js`: focusing a stock row's quantity entry makes that row carry `services-quantity` / `services-quantity-value`; the entry's `min`/`max` equal the action's bounds; a `change` above `max` clamps to `max` and below `min` clamps to `min`; `buyNow` / `sellNow` emit nothing for an out-of-bounds value set without a `change` event; Tab order reaches every enabled row's entry and button, and a disabled buy/sell button stays rendered with its reason. Run the file green.
- [x] 3.5 Fix the dead Vitest target in `web/webclient/tests/test_vue_hud_drawer_evidence.py::test_drawer_mutations_dispatch_and_confirmation`: it still runs `tests/world/quest_board.test.js`, which was deleted by the quest-drawer split, so `_assert_vitest_passes` fails. Point it at `tests/world/guild_counter.test.js` and `tests/world/quest_log.test.js` (abandon confirmation, dispatch) beside the existing `shop_panel.test.js` (now the clamp-on-change bounds from 3.4) and `store/store_dispatch_focus.test.js`, and update its comment. Run the Evennia shard containing that module green.
- [x] 3.6 Update `web/webclient-app/stories/World/ShopPanel.stories.js` only if a story relies on the hard-coded `min="1"`; run `pnpm run build-storybook` and `pnpm run showcase-coverage` green.

## 4. Managed browser journeys (CI-owned; not run locally)

- [x] 4.1 First sync the delta requirements into the main specs so their IDs exist (`openspec/specs/webclient-contextual-hud/spec.md`, `openspec/specs/webclient-service-menus/spec.md`), then read the canonical IDs from `uv run --locked python -m tools.spec_traceability list`.
- [x] 4.2 In `web/tests/browser/test_browser_services_base.py`, add a bounded keyboard helper that presses Tab until `document.activeElement` matches a selector inside the open drawer (fail after N presses), and a helper that replaces a focused number entry's value by keyboard (`Control+A`, digits). Keep `_open_surface` unchanged except that for `shop` it waits for `hudDrawer == "shop"` and the `shop-panel` element.
- [x] 4.3 Remap `web/tests/browser/test_browser_services_shop.py`: the buy journey uses the drawer's first stock row (type an out-of-bounds value, Tab away, assert the clamped display and zero `shop.buy`; replace with `2`, Tab to the buy button, Enter; assert exactly one `shop.buy` with `{item_key, quantity: 2}` and the wallet delta); the sell journey Tabs to the potion's sellable row, sets `1`, activates 賣出; the closed-shop journey asserts every buy/sell button in the drawer is `disabled` with its reason and nothing is sent; the "no host-like control" check reads the drawer's buttons/inputs instead of `.dock-menu-item`; every shop journey asserts zero `dock-menu` / `dock-detail` inside the drawer and an unchanged router depth/trail across open and close. Annotate the new frameless-shop coverage with the contextual-hud requirement ID, and keep the service-browser-acceptance annotation.
- [x] 4.4 Remap the reconnect journey in `web/tests/browser/test_browser_services_dispatch.py`: type an unsubmitted quantity in the shop drawer, drop the transport, assert the drawer closed, reconnect, reopen the shop drawer, and assert the entry is back at the action's lower bound, `services-quantity` is absent until a row takes focus, and no `shop.buy` was sent. The stale/duplicate journeys submit directly and need no change.

## 5. Validation

- [x] 5.1 Run the Node gate (`node --test web/static/webclient/js/tests/*.test.js`), `pnpm test`, `pnpm run build`, `pnpm run build-storybook`, `pnpm run showcase-coverage` (from the repository root), and `uv run --locked python -m tools.spec_traceability check`; all green.
- [x] 5.2 Run `openspec validate make-shop-drawer-frameless --strict` and `git diff --check`; both clean.
