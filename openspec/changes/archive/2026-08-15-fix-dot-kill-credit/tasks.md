## 1. Damaging-buff source identity

- [x] 1.1 In `world/rules/buffs.py`, add a frozen `TickRecord` dataclass (`definition_key`, `source_pk: int | None`, `delta`, `hp_before`)
- [x] 1.2 In `world/rules/action.py`, amend `_handle_buff_apply`: for a definition whose `rate` modifier damages HP (target `hp`, negative delta), validate the actor's `pk` as a positive int (else `RejectedAction(EFFECT_RESOLUTION_FAILED)`), pop any caller-supplied `source_pk` from `buff_kwargs`, and pass the authoritative `source_pk=int(actor.pk)` into `_add_buff`
- [x] 1.3 Test (`world/rules/tests/test_effect_handlers.py` or `test_buffs.py`): applying `buff:fire_scorch` through `_handle_buff_apply` stores the caster's dbref as `source_pk` in the buff cache; a caller-supplied `source_pk` cannot override it; an actor without an int pk rejects before commit; a fresh direct `_add_buff` (e.g. `poisoned`) leaves `source_pk` absent; re-applying the same buff key by a different caster replaces `source_pk`, while a direct `_add_buff` refresh that omits it retains the prior value

## 2. Tick-record collection

- [x] 2.1 In `world/rules/buffs.py`, change `tick_buffs` to return `tuple[TickRecord, ...]`: inside the accumulation loop, before each `at_tick` whose definition rate targets `hp` with negative delta, record the definition key, `getattr(buff, "source_pk", None)`, the delta, and the entity's current HP; non-damaging ticks keep today's behavior and produce no records
- [x] 2.2 Test in `world/rules/tests/test_buffs.py`: `tick_buffs(entity, 10)` firing `poisoned` and `fire_scorch` together returns two ordered records with correct delta/`hp_before`; marker and `conferred_growth_rate` ticks return an empty tuple; a buff applied with `source_pk` exposes it on the record; clock-path callers that ignore the return value observe unchanged HP behavior

## 3. Upkeep settlement boundary

- [x] 3.1 Create `world/rules/upkeep.py` with `settle_upkeep(battlefield, records_by_key, *, simulated=False, nonlethal_keys=frozenset()) -> list[EventLog]`: process records in application order; emit `damage` entries; detect lethal crossings from `hp_before`; emit exactly one defeat entry per target (dbref-deduplicated, `target_id`/`monster_tier`/`simulated` data keys identical to `_defeated_entry`); floor protected targets at 1 HP and stage battlefield `knocked_out` marks; stage one `grant_combat_kill_xp` effect per attributed lethal tick on a `Monster` with a registry tier (commit-time HP guard); resolve `source_pk` against the battlefield roster then the object database; unattributed ticks produce no entries/effects
- [x] 3.2 In `world/rules/upkeep.py`, build one `EventLog` per attributed source (`actor` = source key, `skill_key="combat_upkeep"`, entries for that source only) and run every registered `_EVENT_EFFECT_PLANNERS` value with a request-shaped object carrying the source as `actor` and `context.battlefield`; collect and stage all quest planner effects alongside XP and marks, committed through the existing `_commit`
- [x] 3.3 In `world/rules/combat.py`, change `_end_of_round_upkeep` to collect `tick_buffs` tuple returns per roster key and return them; treat non-tuple returns as no ticks; add keyword-only `simulated`/`nonlethal_keys` to `run_round`, call `settle_upkeep`, append its EventLogs to the round logs, and commit the staged effects after the upkeep
- [x] 3.4 In `world/rules/overwhelm.py`, add the same keyword-only `simulated`/`nonlethal_keys` to `resolve_overwhelm` and forward them to `combat.run_round` in `_resolve_overwhelm_raw` only when they differ from the defaults; widen the positional-only `run_round` side-effect stubs in `test_overwhelm_resolution.py` and `test_monster_behaviour_integration.py` to accept `**kwargs`
- [x] 3.5 In `world/rules/combat_session.py`, extract a small policy helper from `_context_for` (returns `simulated` and companion `nonlethal_keys` from the record/battlefield) and pass both into the `run_round` and `resolve_overwhelm` calls inside the outer transaction
- [x] 3.6 Test in a new `world/rules/tests/test_upkeep_settlement.py`: lethal tick emits exactly one `target_defeated` + `damage` entry; the `damage` entry reports the clamped applied amount (a -5 tick on a target at 2 HP reports 2); multiple DoTs in one tick emit one defeat entry credited to the lethal tick's source; a target killed by the applying action never double-settles; an unattributed or deleted-source tick crosses HP with no entries/XP/quest effects; a non-Monster target grants no XP; a `nonlethal_keys` companion crossing floors to 1, marks `knocked_out`, and emits a `target_knocked_out` entry; a simulated tick tags the entry `simulated` and grants no XP; a knocked-out companion's attributed tick grants the companion XP but no owner quest credit (mirroring the action path); planners run only for sources with defeat entries; planner failure propagates and `_commit` restores staged surfaces

## 4. Session and quest integration

- [x] 4.1 Test (`world/rules/tests/test_combat_session.py`): a hostile session where the player's DoT tick kills the final foe returns victory, includes the upkeep `damage`/`target_defeated` logs in the round result, grants the player exactly one kill XP, and commits atomically (rounds_elapsed + settlement + XP)
- [x] 4.2 Test (`world/rules/tests/test_guild_exams.py`): a guild-exam tick kill of the examiner settles the exam normally, tags the defeat `simulated`, and grants no kill XP or DEFEAT quest progress
- [x] 4.3 Test (`world/quests/tests/test_action_events.py` or `test_planner.py`): the quest planner advances a matching DEFEAT objective from an upkeep-built attributed EventLog with the same cap/one-transition rules, and ignores simulated/unattributed upkeep entries (including the protected-entity failure path)
- [x] 4.4 Test (`world/rules/tests/test_combat_session.py`): an upkeep settlement failure (injected quest planner exception) rolls back the whole round including tick HP and session metadata, and the in-process surfaces are restored

## 5. Traceability and verification

- [x] 5.1 Annotate the new/changed behavior tests with `covers_requirement` for the updated main-spec requirements (`combat-upkeep-settlement::*`, `buff-handler-integration::*`, `combat-resolution::*`, `magic-level-progression::*`, `quest-progress-tracking::*`, `player-combat-session::*`), obtaining canonical IDs via `uv run --locked python -m tools.spec_traceability list`
- [x] 5.2 Run `uv run --locked python -m tools.spec_traceability check`; run the touched package tests (`world.rules.tests.test_buffs`, `test_upkeep_settlement`, `test_combat_session`, `test_guild_exams`, `test_combat_party`, `test_progression`, `test_overwhelm_resolution`, `test_monster_behaviour_integration`, `world.quests.tests.test_action_events`, `test_planner`, `test_integration`) with `MUD_TEST_SETTINGS=1 uv run --locked evennia test --settings test_settings.py --keepdb`
- [x] 5.3 Run `openspec validate --change fix-dot-kill-credit --strict` and keep `git diff --check` clean
