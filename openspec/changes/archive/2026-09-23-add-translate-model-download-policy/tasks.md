# Tasks — translate model download policy

## 1. The knob

- [x] 1.1 Add `ART_TRANSLATE_DOWNLOAD_ENABLED = _env_bool("ART_TRANSLATE_DOWNLOAD_ENABLED", True)` to the translation block of `server/conf/settings.py`, with a comment mirroring the `ART_REMBG_DOWNLOAD_ENABLED` stanza, and rewrite the `ART_TRANSLATE_MODEL_DIR` comment's "The server never downloads into it — an operator seeds it" sentence to the dual-track wording (first-use fetch when enabled; script seeding with the flag false is the air-gapped configuration). Verify `ART_TRANSLATE_DOWNLOAD_ENABLED=off` imports to `False` and `=maybe` raises the named settings error.
- [x] 1.2 Add `ART_TRANSLATE_DOWNLOAD_ENABLED` to the pop list in `server/conf/test_settings.py` AND set `ART_TRANSLATE_DOWNLOAD_ENABLED = False` after the star-import, so every test run is air-gapped under test settings regardless of the code default (the browser harness pins `ART_TRANSLATE_BACKEND` to the fake already; add the same explicit `False` in `web/tests/browser/browser_settings.py` if its sanitization lacks one). Verify an inherited `ART_TRANSLATE_DOWNLOAD_ENABLED=true` shell value cannot make a test run download-capable (effective value `False` under test settings).
- [x] 1.3 Extend the inventory contract in `server/conf/tests/test_env_overrides/_support.py`: the name→env map, the default map (`True`), a coercion row (`off` → `False`), and a rejection row (`maybe` → boolean-word rule); verify `server/conf/tests/test_env_overrides/` passes, including the AST exact-set check against `.env.example` and settings reads (the test-settings `False` pin must not read as a second reader of the variable).

## 2. The download track in the backend

- [x] 2.1 In `world/art/translate_ct2.py`, restructure the construction path so the layout check, the engine-cache re-check, the fetch, and the post-fetch re-check ALL run under `_ENGINE_LOCK` (today `_check_layout` runs outside it); gate the `_check_layout` failure inside the lock on `settings.ART_TRANSLATE_DOWNLOAD_ENABLED`: false keeps today's exact immediate `art_translate_unavailable` before any library import; true routes the failing layout through a new `_populate(model_dir)` step before the re-check, so concurrent callers block and then observe the completed layout instead of degrading mid-fetch. Rewrite the module docstring and the `CTranslate2Backend` class-contract bullet ("no import and no outbound request") to the dual-track wording. Verify the existing unavailable-path tests still pass with the flag false and that a concurrent second caller blocks then translates rather than failing while a fetch is in flight.
- [x] 2.2 Implement `_populate`: stdlib-only fetch of `https://argos-net.com/v1/translate-zh_en-1_9.argosmodel` with connect/read timeouts and a streaming 128 MiB response-size cap (reject an oversize `Content-Length` before reading; abort once 128 MiB have been read), no retries; verify with `zipfile` integrity, member-path safety (everything under `translate-zh_en-1_9/`, no absolute/`..` paths), and the required-entry list (`model/config.json`, `model/model.bin`, `model/shared_vocabulary.json`, `sentencepiece.model`, `README.md`); unpack exactly those pieces (never `stanza/`), landing each via temp file written INSIDE `model_dir` (never `/tmp`; `os.replace` never crosses a filesystem) + `os.replace`, with every temp deleted on failure. Verify with a local fixture zip served over a stubbed opener: success populates the layout, and each failure mode (bad zip, unsafe member, missing entry, oversize `Content-Length`, oversize body past the cap, timeout, `OSError`/ENOSPC simulated at the write) raises `art_translate_unavailable` and leaves no temp behind.
- [x] 2.3 Add the per-process failure latch — a per-process GLOBAL module-level flag (not per-model-dir; `ART_TRANSLATE_MODEL_DIR` is code-only): the first `_populate` failure of any kind, including `OSError`/ENOSPC, records itself so later calls skip the fetch and fail immediately with `art_translate_unavailable`; a completed layout is never re-fetched. Verify a scripted first-failure followed by a second translation attempt performs exactly one network attempt.
- [x] 2.4 Emit the lifecycle events from the backend module via `world.observability`: exactly one `art_translate_model_download_done` info (url, bytes, duration_ms) on success and exactly one `art_translate_model_download_failed` warn (url, exception chain) on the first failure; verify the seam's own event counts (`art_translate_done` / `art_translate_failed`) are unchanged for the calling job and that the job still settles `done` on the untranslated description.

## 3. Backend tests

- [x] 3.1 Add `CTranslate2Backend` download-track tests to `world/art/tests/test_translate.py`: complete layout ⇒ zero network (spy count 0); missing layout + flag true (explicit `override_settings`) ⇒ one fetch then successful translation against the unpacked fixture; every fetch failure degrades through the normal `art_translate_failed` warn without crashing the caller. Verify the whole art suite imports no `ctranslate2`/`sentencepiece` and attempts no network at test settings (fake backend keeps the seam hermetic; the settings-level `False` pin keeps the air-gapped track default).
- [x] 3.2 Add the air-gapped regression: flag false against an absent directory reproduces today's byte-exact behavior — immediate `art_translate_unavailable`, no library import, zero network attempts, record settles `done` untranslated. Verify the existing "server never fetches" expectations now hold under `DOWNLOAD_ENABLED=false` (the effective value under test settings) and the suite-level network-spider sees zero model requests in both modes at test settings.
- [x] 3.3 Rewrite `CTranslate2BackendReachabilityTests` in `world/art/tests/test_translate.py` to the re-scoped ban: KEEP the `world.ai`/`requests`/`http` import bans, the `LLM_` substring ban, and the `test_the_module_never_reads_an_llm_setting` AST test; DROP the `urllib`, `socket`, `importlib`, and `world.observability` entries from `_BANNED_IMPORT_PREFIXES` and the `socket`, `urllib`, `requests` (keep `requests`), `http`, and `world.observability` raw-substring bans; ADD an assertion that the only `https://` URL literal in the module is the pinned `https://argos-net.com` model URL. Keep the seeded-path zero-network runtime test unchanged. Verify the rewritten test passes against the download-track module and that removing the pin (pointing the fetch at another host) fails it.
- [x] 3.4 Make `world/art/tests/test_art_observability.py::test_forward_default_failure_emits_one_bounded_event_and_still_settles` hermetic under the new default: add an explicit `ART_TRANSLATE_DOWNLOAD_ENABLED=False` to its `override_settings` (belt-and-braces alongside the settings-level pin of 1.2) and update its inline comment to say the run is air-gapped by settings; verify the test neither populates `server/.translate` nor trips the module latch (subsequent translate tests still see an unlatched module).

## 4. Seeder script volume-name fix

- [x] 4.1 In `scripts/fetch-translate-model.sh`, replace the hard-coded printed volume name with a resolver: exact `evennia-translate`, then `${COMPOSE_PROJECT_NAME:-mud}_evennia-translate`, then a `podman volume ls --format '{{.Name}}'` suffix match on `(^|_)evennia-translate$` (the same regex as design D7), run pipefail-safe — a failing `podman volume ls` yields no candidates and is never an error; fall back to the project-prefixed default annotated "unresolved" when `podman` is absent or nothing matches, with unchanged exit status. Verify by running the script's resolver function against a stubbed `podman` on PATH returning `mud_evennia-translate` (printed command names that volume), against a stubbed `podman` that exits nonzero (annotated default, exit status unchanged), and against no `podman` (annotated default).
- [x] 4.2 Update the script header's "The game server NEVER downloads the translation model (design D2 ...)" paragraph to the dual-track wording (host seeding is the air-gapped/pre-seed route; the server may first-use-fetch when `ART_TRANSLATE_DOWNLOAD_ENABLED=true`) and the busybox example to the resolved name; verify `scripts/fetch-translate-model.sh --help` output matches and `bash -n` passes.

## 5. Inventory docs and settings guide

- [x] 5.1 Add the commented `#ART_TRANSLATE_DOWNLOAD_ENABLED=false` entry with its boolean/type/default/air-gapped comment block to the translation stanza of `.env.example` (wording mirroring the rembg download stanza); verify the AST inventory contract finds a reader for it and no dead variable.
- [x] 5.2 Update `docs/development/settings-and-environment.md`: the `ART_TRANSLATE_DOWNLOAD_ENABLED` table row (boolean, default `True`) and the seed-policy paragraph rewritten to the dual track (first-use fetch into the `evennia-translate` volume when enabled; `false` + `scripts/fetch-translate-model.sh` pre-seed as the air-gapped configuration); verify every env-backed name in the guide table matches `_support.py`'s inventory.
- [x] 5.3 Update `docs/gm/prompts.md`: rewrite the 翻譯模型種子 seed-policy paragraph's "從不自己下載模型 / 沒有任何下載翻譯模型的執行期網路行為" assertion to the dual-track wording, and add the `ART_TRANSLATE_DOWNLOAD_ENABLED` row (同名, `True`) to the 生成設定 settings table next to the existing `ART_REMBG_DOWNLOAD_ENABLED` row; verify the table's env-backed names match `_support.py`'s inventory.

## 6. Container contract and verification

- [x] 6.1 Confirm no compose/Containerfile change is needed (the `/app/server/.translate` volume and layer-scan assertions are unchanged); run `tests/test_container_contract.py` and verify the no-baked-model scan still passes.
- [x] 6.2 Integration check: with a temp `ART_TRANSLATE_MODEL_DIR`, a stubbed URL fetcher, and `ART_TRANSLATE_DOWNLOAD_ENABLED=true`, drive `translate_description` end to end and observe populate → build → translate; then kill-and-restart the process against the same directory and observe zero further fetches — if the observation runs in one process instead of a true restart, explicitly reset the module-level latch and clear `_ENGINES` between the phases so the second phase proves "complete layout ⇒ no fetch", not a latched module. Verify `openspec validate add-translate-model-download-policy --strict` passes and `world/art/tests/test_translate.py` plus `server/conf/tests/test_env_overrides/` are green.
