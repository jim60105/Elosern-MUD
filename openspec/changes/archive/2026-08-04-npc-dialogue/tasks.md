## 1. Guarded reply layer

- [x] 1.1 Add `world/ai/npc_dialogue.py` with `generate_npc_reply(client, *, npc_context, player_context, memory) -> Deferred[NPCDialogueReply | None]` that rejects an explicit `None` client with a named `NPCDialogueClientRequiredError` as the first statement, builds a `ChatRequestDescriptor` (messages from `build_npc_dialogue_prompt`, `schema_id="npc_dialogue"`), yields `guarded_call("npc_dialogue", client, descriptor)`, `json.loads` the accepted text into a frozen `NPCDialogueReply(speech, intent)`, and resolves to `None` on any degrade trigger
- [x] 1.2 Add the registration gate at the top of `generate_npc_reply` that reads `guardrail._degrade_fallbacks` for the `npc_dialogue` layer's sentinel and raises `NPCDialogueNotRegisteredError` when the layer is genuinely absent, before any prompt build or transport work
- [x] 1.3 Add tests proving: a valid `{speech, intent}` fixture resolves to an `NPCDialogueReply` with no state change; an explicit `None` client errbacks with `NPCDialogueClientRequiredError` before any prompt/transport work; the result is a plain frozen value with no write-back path; calling before registration errbacks with `NPCDialogueNotRegisteredError`; and a degraded call resolves to `None` (never a sentinel object leaking out)

## 2. Prompt construction

- [x] 2.1 Add `build_npc_dialogue_prompt(npc_context, player_context, memory) -> tuple[dict, dict]` returning a deterministic system/user pair: system fixes the NPC role, 伊洛瑟恩大陸, 正體中文, and the `{speech, intent}` JSON output contract with the no-invention fidelity rule; user serializes the player's identity and `disguised_stats` (as what the NPC perceives), plus the bounded memory window, using stable JSON serialization with `ensure_ascii=False`
- [x] 2.2 Enforce hard bounds: a fixed maximum memory-line window with an explicit truncation marker, per-field string-length caps, and a bounded total size; identical input SHALL produce byte-identical prompts with no live entity references
- [x] 2.3 Add tests under `world/ai/tests/test_npc_dialogue.py` proving: identical input yields byte-identical prompts; `disguised_stats` are injected so a disguised elf reads as weak; oversized memory is truncated deterministically; and no live entity reference appears in the serialization

## 3. Schema, semantic validators, and registration

- [x] 3.1 Define `NPC_DIALOGUE_OUTPUT_SCHEMA` (object with string `speech` and object `intent`; `intent.kind` enum restricted to the seven kinds `give_item`/`take_item`/`offer_quest`/`request_guild_exam`/`adjust_relation`/`reveal_lore`/`none`) and register it under the `npc_dialogue` schema id
- [x] 3.2 Add the internal `_NPC_DIALOGUE_DEGRADED` sentinel, the `NPCDialogueNotRegisteredError`/`NPCDialogueClientRequiredError` exceptions, and the `npc_dialogue`-layer semantic validators: speech non-empty/bounded/CJK-present; `intent` is a dict with a whitelisted `kind`; `request_guild_exam` carries exactly one payload field `target_rank`; `give_item`/`take_item` carry `item_key` and positive integer `qty`; no leaked template placeholders
- [x] 3.3 Add `register_npc_dialogue()` that atomically installs the output schema, every semantic validator, and the sentinel degrade fallback with rollback on partial failure, and is idempotent: a second call is a no-op swallowing only this module's own duplicate-registration errors, never an incompatible one
- [x] 3.4 Add tests covering: an unknown intent kind is rejected and retried with the error appended; a malformed exam payload (extra/missing `target_rank`) is rejected; empty/non-Chinese/placeholder-leaking speech is rejected and retried; a valid bounded reply passes on the first attempt; disabled profile, transport failure, and exhausted retries resolve to `None` with zero or bounded client calls; narrating before registration errbacks with `NPCDialogueNotRegisteredError`; duplicate registration is a no-op; and a partial hook-registration failure leaves no `npc_dialogue` hooks installed

## 4. Deterministic intent applier

- [x] 4.1 Add `world/rules/npc_intents.py` with `IntentOutcome(applied: bool, reason: str | None)` and `apply_npc_intent(npc, player, intent) -> IntentOutcome` that verifies then applies through existing deterministic APIs only: `none` is a no-op; `request_guild_exam` validates `target_rank` and delegates to `start_guild_exam(actor=player, examiner=npc, target_rank, requested_by="npc_intent")`, mapping any `GuildExamError` to `applied=False`
- [x] 4.2 Implement the dedicated transfer primitive for `give_item`/`take_item`: verify the giver's `db.inventory` holds `item_key` × `qty`, precompute both `plan_inventory_delta` plans, snapshot both entities' affected surfaces (inventory, quest log, ACQUIRE pins), apply both plans inside one outer `transaction.atomic()`, and restore both in-process caches on any failure; missing holdings or quantity map to `applied=False` with no inventory change
- [x] 4.3 Reject `offer_quest`/`adjust_relation`/`reveal_lore` as forward-declared seams (`applied=False`, no state change, documented reason) and reject unknown kinds defensively
- [x] 4.4 Add tests under `world/rules/tests/test_npc_intents.py`: a valid `request_guild_exam` against an eligible co-located `GuildExaminer` host starts the exam with `requested_by="npc_intent"`; every failed gate (remote examiner, wrong branch, wrong next rank, below merit, active combat/exam) discards only the intent and preserves speech; `give_item`/`take_item` transfer verified holdings and update both inventories; an unverifiable item intent changes no inventory; a deliberate second-side transfer failure rolls back both entities' database inventory and in-process attributes; the three forward-declared kinds are rejected with no state change; and the applier never imports `world.ai`

## 5. LLMNPC entity typeclass

- [x] 5.1 Extend `typeclasses/npcs.py` with `LLMNPC(NPC)` porting the contrib chat-memory (per-character `chat_memory` with `max_chat_memory_size` window) and thinking-state (`thinking_timeout`/`thinking_messages`) behaviors as project code, without subclassing the contrib and with **no module-scope import of `world.ai` or `world.rules`** (design §4 amendment + D7 deferred-import rule)
- [x] 5.2 Add `at_talked_to(speech, character, client)` with the client as a required injected argument (explicit `None` errbacks with `NPCDialogueClientRequiredError`): function-locally imports `world.ai.npc_dialogue` and `world.rules.npc_intents`, appends the player line to memory (trimmed to the window), builds the npc/player contexts (identity, location, `disguised_stats`), calls `generate_npc_reply`, maps `None` to `greeting_for(self)` or silence, and on a valid reply presents the speech, records the NPC line, and routes the intent to `apply_npc_intent`
- [x] 5.3 Add tests under `typeclasses/tests/` using `FakeLLMClient`: a valid reply is presented and its verified intent applied; the degraded outcome yields the authored greeting when present and silence when absent; memory is appended and trimmed to the window; thinking feedback sends at most one message after the timeout and cancels the pending timer on a terminal result with no leaked cancellation error (deterministic clock); a fast reply never emits a thinking message; an explicit `None` client errbacks before prompt/transport work; and no test ever constructs a live client

## 6. Boundary and startup wiring

- [x] 6.1 Verify `world/ai/npc_dialogue.py` imports no state writer, no typeclass, no live transport symbol, and no socket, and consumes the client only through the injected protocol (repository contract test stays green with no edits)
- [x] 6.2 Add `_register_npc_dialogue_layer()` to `server/conf/at_server_startstop.py`'s `at_server_start()` hook (post-`evennia._init()`), calling `register_npc_dialogue()` inside a boot-tolerant try/except that logs and skips on a foreign leftover registration; add one startup integration test that invokes the seam and asserts the layer is registered with the sentinel fallback
- [x] 6.3 Add a regression test that imports `typeclasses.npcs` before `evennia._init()`, then starts and triggers a disabled-profile degrade, asserting clean greeting-or-silence with no logger failure
- [x] 6.4 Run focused `world.ai`, `world.rules`, and `typeclasses` tests, the repository-wide contract test, the full Evennia suite, `python -m tools.spec_traceability check`, and `openspec validate npc-dialogue --strict`
