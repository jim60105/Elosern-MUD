## Why

The ice skill tree (`docs/lore/skill-trees/ice.md`, 13 nodes: two roots — 遲緩路線 凍結之息 chain and 監禁路線 冰錐術 chain — two Lv.3 branch points, and the 神格 canopy 永凍神格 where the lore's 匯合 lands as the two-parent capstone) is the node-data authority, but the registry still ships the dev-era 2026-08-12 ten-row ice set with no prerequisites and no coefficients, and every one of its four control keys is INERT: `ice_slow` is the retired `bounds`-illusion shape (no settlement consumer — the ad084c9 finding), `ice_freeze`/`ice_prison` carry empty modifiers with NO `buff_active` action-lock rule anywhere, and `ice_wall`'s only payload is the inert `bounds: defense` illusion, so today's 「凍結」 never locks a single action (the existing `test_buffs.py` pin `assertFalse(blocks_action)` documents the inertness). Every mechanic this wave needs is ALREADY SHIPPED: the action-lock MECHANISM is the declarative `combat_modifiers.yaml` rule (`paralysis_locks_actions`, `suffocation_locks_actions`, `water_bind_locks_actions`, `fear_locks_actions` all live), the stillness-marker synergy is the `DamagePolicy(predicate=("buff:<key>", …), attack_multiplier=…)` shape with shipped OR-match-once semantics (the earth fissure triple prices 1.15 the same way), the wall's cell-gate reduces to the `marker: ground` footprint (earth fissure/fire lava live), and 處決級/毀滅級/two-parent canopy are all archived rungs. The dev-era set also has no `frost_mire`, no `crystal_shatter`, no 神格, and an off-tree `ice_wall` defensive buff — 固定防禦 is 土's exclusive §4.2 verb.

## What Changes

- Replace the ten dev-era ice registry rows with the 13-node tree verbatim from the node table (keys, labels, targets, MP costs AS-IS — every cost sits inside its §3.1 band, prerequisites via `SkillPrerequisite(node, N)`, coefficients per §3.2, caps via the existing reverse-edge derivation — no stored cap field; `eternal_frost_apotheosis` is the two-parent capstone at 長夜冰原 Lv.10 ＋ 絕對零度 Lv.10). Key census: TEN re-authored (all dev-era keys survive by name — `ice_shard`, `frost_breath`, `ice_wall`, `frost_arrow_rain`, `permafrost_domain`, `ice_prison`, `blizzard`, `absolute_tundra`, `absolute_zero`, `eternal_ice_field`), THREE new keys (`frost_mire`, `crystal_shatter`, `eternal_frost_apotheosis`). `ice_wall`'s off-tree `bounds: defense +5` buff is DELETED without alias — 固定防御 is 土's verb (§4.4 牆歸冰: the wall is a movement gate, not armor).
- Author every node's effect composition over SHIPPED vocabulary only: `damage:ice:magic` coefficients (1.0 → 4.0); `buff_apply:` freeze-rung rows on parallel-duration keys (the `earth_fissure`/`_quake`/`_apex` precedent — `apply_buff` has no per-effect duration override, so each §3.3-band rung is its own row) each locked by its own `buff_active → actions_per_turn: 0` rule row (design D3); `buff_apply:ice_wall_frost` + `buff_apply:ice_slow` on `ice_wall` and `buff_apply:ice_frost_mire` (ENEMIES audience) on `frost_mire` — the wall's target cell is binary in-combat position → the wall/mire cells author as `marker: ground` footprint rows, the ratified engine reduction of design D4 (the footprint fact + the first-rung slow both mount on the wall's target; the mire's −8 rung rides its own rule); the slow ladder as empty-modifier mounts + `combat_modifiers.yaml` `agility_flat` rules (dev-era `ice_slow` re-homed to the −3/15 s 學徒 rung per the node table, the ad084c9 insight); `crystal_shatter`'s stillness synergy as `DamagePolicy(predicate=(five buff: entries), attack_multiplier=1.5)` — ice is the game's sole stillness-marker supplier (constitution §4.1/§4.2); `absolute_zero` 處決級 = `DamagePolicy(bypass_defense=True, predicate=())`; `eternal_ice_field`/`eternal_frost_apotheosis` 毀滅級 = `max_hp_fraction` devastation at the shipped rung; `ice_mastery` stays exactly as shipped (PASSIVE, off-lineage, freeform ladder — census fact, zero rows change).
- Author the ice rulebook rows (data-only, same surfaces): the four freeze-rung rows (30/40/60/90 s — node-table-priced where explicit, §3.3-band-derived for the two lore-silent 主宰 freeze clauses, D6; the 主宰 60 s rung is ONE shared key consumed by both `absolute_zero` and `eternal_ice_field`) + the `ice_wall_frost`/`ice_frost_mire` footprint rows (both 60 s — the mire's −8/60 s is node-table-explicit, its −8 magnitude rides its own rule) + the 定身 row `ice_prison` kept with its own new lock rule + the `ice_slow` mount re-homed to the −3/15 s 學徒 rung; SEVEN new `combat_modifiers.yaml` rule rows (four freeze locks + the 定身 lock + the two agility ladder rungs — design D3's rung layout); `world/rules/rulebook/state_reactions.yaml` UNTOUCHED (zero ice reaction rows — every ice clause is a direct effect component or a buff_active rule).
- Retire the ice catalog echo tests in-file: `ICE_SPELL_CATALOG` and `IceSpellCatalogTests`'s echo methods (field-equality, effect round-trip, key-set equality) in `world/skills/tests/test_spell_catalogs.py` (the `_elemental_spells` builder-shape mechanic test stays — lightning still uses the builder; `ClosedVocabularyParseTests` untouched), the `test_ice_spell_tier_labels_match_the_catalog` tier-label echo in `world/skills/tests/test_cost_tiers.py`, the dev-era `ice_slow`/`ice_freeze` numeric pins in `world/rules/tests/test_buffs.py` (re-homed to load/apply/presence assertions per the ratified M4 discipline — the `assertFalse(blocks_action)` inertness pin is REPLACED by the freeze rung's real lock behavior in the new behavior module), plus the `tests/test_rules_skills_test_layout_contract.py` class-home pin update and whatever per-file adjustment `tools.test_data_lint check` output decides in this change (the wind-wave 7b66d14d precedent — the seed's data-contract entries for the two test files already exist and stay); the canonical ID `skill-registry::skill-registry-contains-the-full-冰-element-spell-set` retires with its requirement (freeze-list entries adjust at the separately authorized main-sync).
- Sync `world/rules/rulebook/status_display.yaml` with the key census (the shipped import-time coverage check covers exactly the live buff keys ∪ combat-modifier rule ids): swap the `ice_wall` row out, keep `ice_slow`/`ice_freeze`/`ice_prison`, add the three new freeze-rung rows, the mire row, the slow-rung mount row and every new modifier rule id per the table's label/severity conventions.
- Docs/traceability integration owned here: shard-manifest final state (`.github/evennia-shards.json` — this change owns the wave's only manifest edit); `ice.md` stays the authority and is untouched; `docs/lore/magic-system.md` §3's ice row checked for contradiction only (expected: none — it states the 物理性靜止＋靜止標記 identity this tree implements); `docs/development/adding-spells.md`'s `_elemental_spells` example stays grammatically valid (the builder survives for lightning); `tests/test_command_docs.py` untouched (no command-surface change).

## Capabilities

### New Capabilities
None.

### Modified Capabilities
- `skill-registry`: the duplicated ten-row 冰-element data-contract requirement is REMOVED and replaced by one behavioral requirement — ice spell progression composes executable physical-stillness behavior (a slow ladder settling through the real merged-modifier `agility_flat` consumers on to-hit/flee/overwhelm, freeze/定身 rungs that lock every action through the shipped rule mechanism at authored durations with rung-differentiated keys, a sole-supplier stillness marker whose live-instance facts price one OR-matched 1.5× synergy across BOTH action-lock families, a frost-wall/mire ground-marker footprint mounting the first-rung slow on its cell's occupants, a 處決級 execution rung that ignores defense, 毀滅級 devastation rungs, a two-root branching lineage whose two route chains converge only at the two-parent capstone gate) through the shared mechanics, with the dev-era rows — including the off-tree `ice_wall` defensive buff — deleted without alias.
- `skill-lineage`: ADDED 「The ice lineage ships as the authored two-root branching tree with a two-parent canopy」 (registered-content shipped contract mirroring the fire/wind lineage requirements' shape: two roots, two Lv.3 branch points, the two independent route chains converging ONLY at the 神格 canopy consuming both authored parents, mastery passives out-graph). No dev-era ice lineage requirement exists — the wave ADDS, the wind precedent.
- Consequences recorded without deltas (no behavior of a generic surface changes): (a) freeform-eligibility — dev-era `frost_breath` (`buff_apply`) was already ineligible and the authored census is stable: the four pure-damage nodes (`ice_shard`, `frost_arrow_rain`, `blizzard`, `crystal_shatter`) stay freeform-eligible through the shipped ladder, and every control/devastation node carries buff shapes that were never scalable — no freeform-casting scenario example references an ice key (verified at authoring); (b) the `blocks_action`/`BLOCKING_BUFF_KEYS` shipped inventory stays `{paralysis}` untouched — ice's lock is the modifier-rule path (the `action_preview` bundle path), the same path paralysis's own rule feeds, ratified at D3; (c) earth's three fissure spells keep consuming `ice_slow` as pure consumer data — the re-homed row (−3 bounds illusion → real `agility_flat: −3` via rule, 60→15 s per ice's node table) is exactly the "ice slow rung reused as consumer data" the earth requirement already names (earth ratified ice's authority over the rung's row), and the earth wave's behavior stays: the slow becomes REAL for earth's fissure targets, which is the fix, not a regression.

## Impact

`world/skills/registry.py` (ice block: `_elemental_spells` call replaced by individually-written `_spell` rows per the earth/fire/wind shape — per-node `effect_policies`, prerequisites, multi-prereq capstone); `world/rules/rulebook/buffs.yaml` (dev-era `ice_wall` row deleted; `ice_slow` re-homed; `ice_freeze` re-homed to the 30 s lock rung; `ice_prison` kept as the 定身 key with its new lock rule; freeze/mire/mount rows added), `world/rules/rulebook/combat_modifiers.yaml` (seven new rule rows; ZERO changes to the existing four lock rules — `paralysis_locks_actions` stays lightning's, mounted by `paralyzing_bolt`), and `world/rules/rulebook/status_display.yaml` (display-metadata census sync); `world/skills/tests/test_spell_catalogs.py` (echo retirement + new `IceLineageTreeCatalogTests` shipped-content contract), `world/skills/tests/test_cost_tiers.py` (tier-label echo removal), `world/rules/tests/test_buffs.py` (dev-era pin strip), `tests/test_rules_skills_test_layout_contract.py` (class-home pins per the wind precedent); new synthetic behavior module → `.github/evennia-shards.json`; traceability ledger hygiene at the separately authorized main-sync. `openspec list --json` returned an empty change set at authoring time — no serialization gate against active changes. One engineer-day: registry block + rulebook rows + echo retirement + one synthetic behavior module + one disposable offline scenario.

This turn creates planning artifacts only. Do not apply, archive, sync main specs, create feature branches or merge until requested.

## Batch

This is a single-change wave: zero dependencies — every primitive it consumes is merged at master 0c3f3a27 (research verified each against shipped source before authoring: the `buff_active → actions_per_turn: 0` rule rows live at `combat_modifiers.yaml:4-24`, the `buff:<key>` predicate OR-match lives at the earth fissure-spell 1.15 row, `marker: ground` loads/validates at `world/rules/buffs.py:216-232` with the standing-on-it + battlefield-exit semantics, `bypass_defense`/`max_hp_fraction` rungs live at the dark/fire/water capstones, the n-ary two-parent DAG validates with the wind canopy). No sibling change shares a file with it except as sole author. No honest behavior split surfaced: the freeze mechanism ships as the rule-table vocabulary, so the dev-era key reconciliation is data work at the rule-table layer, exactly as the earth/fire/wind waves recorded their ladder decisions.

- depends-on: (none)
