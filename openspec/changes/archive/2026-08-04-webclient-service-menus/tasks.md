## 1. Service Read Model and Messages

- [x] 1.1 Implement `world/rules/service_view.py` as the sole no-mutation builder of the version-1 services view: per-class host resolution (exactly one local `GuildStaff` for `guild`/`rank`, exactly one `GuildExaminer` for `rank`, exactly one `Merchant` for `shop`, with a display-only top-level `host` and no cross-class ambiguity), the `player` summary from canonical wallet/registration/rank/merit/catalog thresholds, and bounded `guild`, `shop`, and `inventory` sections reusing `parse_guild_registration`, `read_records`, `list_guild_offers`, `parse_merchant_stock`, `shop_is_open`, `list_items`, `get_catalog`, and `world.quests.describe`; add pure tests for per-class/ambiguous/no-host resolution, co-located different-class hosts, unregistered summaries, board rank filtering, quest detail/deadline/reward rendering, shop open/closed state, exact copper values, repeated-key inventory aggregation, and surface isolation on corrupt records (no mutation of any canonical surface, no clock creation, no `disguised_stats` read, display-only `host`).
- [x] 1.2 Implement `world/rules/service_messages.py` mapping every deterministic service rejection (`RegistrationReason`, `BoardAccessError`, `RewardClaimError`, `ExamReason`, `TradeReason`, `GuildDataError`, `QuestDataError`, `QuestNotFound`) to a stable code and safe Traditional Chinese message, with a bounded generic fallback that never exposes a traceback or raw payload; add tests for every mapped reason and the fallback.
- [x] 1.3 Prove the read model is side-effect free: add an Evennia test that builds the full services view before and after a snapshot and asserts every canonical value (wallet, inventory, quest log, registration, merit, rank, merchant stock) is unchanged, including an actor with an unmaterialized or absent registration and a disguise active.

## 2. Services Panel Presenter

- [x] 2.1 Implement `web/webclient/presentation/services.py` with the exact version-1 available schema (schema D4 bounds: max 12 board/12 quest/12 stock/12 sellable/32 inventory rows, string/ID/rank/quantity ceilings, closed `state`/`kind` values), the common unavailable form only outside exploration mode and on global-prerequisite `PanelUnavailableError`, and surface-level isolation when a single surface's canonical data is missing or malformed; register `services` in the production registry and update registry tests that assume a status/combat/local-map-only allowlist.
- [x] 2.2 Add server-side payload-validation tests for every bound in D4, deterministic ordering of board/quest/stock/sellable/inventory rows, pagination totals matching shipped row counts and null surfaces, host identity/display-name limits, action-descriptor enabled/disabled/quantity shapes, a worst-case serialization test proving the structurally maximal realistic payload (at all row ceilings) fits comfortably under the 65,536-byte envelope, and a second test proving a payload maximizing every string field at once is rejected by the byte gate.
- [x] 2.3 Add presenter isolation tests: a corrupt quest log or malformed merchant stock degrades only the affected section while the whole panel stays available, status and narrative stay healthy, and combat/creation modes render the schema-valid unavailable form with no fabricated service state.

## 3. Service Action Adapters

- [x] 3.1 Implement `web/webclient/actions/service_actions.py` with exact validators and adapters for `guild.register` (empty payload), `guild.quest_accept` (`definition_key`), `guild.quest_abandon` and `guild.quest_turnin` (`quest_id`), `guild.exam_start` (`target_rank` equal to the server-derived exact next rank), and `shop.buy`/`shop.sell` (`item_key` plus integer `quantity` in 1..1000); every adapter re-resolves the local host via `resolve_local_service_host` (`GuildStaff`/`GuildExaminer`/`Merchant`) and the referenced identity, calls only the listed deterministic APIs, and never accepts an actor/host/branch/session/price/stock/wallet field.
- [x] 3.2 Register the seven adapters in the production action registry and assert the production action set is exactly the three combat plus the seven service actions; update dispatcher tests that assert a combat-only production registry.
- [x] 3.3 Add Evennia integration tests for every adapter: success, domain rejection (unregistered, wrong branch, below threshold, already claimed, closed shop, insufficient funds, insufficient stock, stock overflow, insufficient items, bad quantity, unknown quest/item/rank), idempotent re-registration returning the original record, tampered identities rejected before the domain API, dispatcher-level stale revision and duplicate request handling, host disappearance between render and submit, commit-time price/stock revalidation settling against current canonical state, and a before/after assertion that no surface changes on rejection.
- [x] 3.4 Add `guild.exam_start` integration tests proving the exact next-rank-only enforcement, the transition to a `guild_exam` combat session, the publication of `status`+`services`+`context_actions` with mode `combat`, and that the `services` panel becomes unavailable in combat mode.

## 4. Browser Service Dock

- [x] 4.1 Extend `elosern/protocol.js` with the `services` panel allowlist entry and exact available/unavailable validators sharing the D4 constants with the server (guarded by a dual-direction parity test); add Node tests for valid, malformed, unavailable, unknown-surface, quantity-bound, and unknown-node-style rejection cases.
- [x] 4.2 Implement a DOM-independent `service_menu.js` render model reducing the validated panel into surface/submenu/row/focus targets with stable ordering, quantity-form state, and a destructive-abandon confirmation state; add Node tests for surface routing, focus geometry, quantity validation, confirmation gating, and disabled-row non-submission.
- [x] 4.3 Implement the `services_dock.js` GoldenLayout plugin rendering the Services root in exploration mode, the per-surface submenus, the quantity form, the quest-detail pane (rendering the server `detail` string as literal text), and the abandon confirmation screen; wire it into the action dock so it respects the in-flight lock, stale-revision unlock, offline overlay, and the exclusive mode transition — synchronously unloading and unregistering its keyboard handlers and discarding local quantity/selection/confirmation state when a `combat`-mode update is adopted, and remounting when exploration mode returns; update shell/layout tests and the shell smoke test.
- [x] 4.4 Ensure labels, disabled reasons, and the server-rendered detail are inserted as text (never trusted HTML), focus is distinguishable without color, and quantity input accepts bounded integers via keyboard only.

## 5. Managed Browser Acceptance

- [x] 5.1 Extend deterministic browser fixtures with a registered/unregistered guild-hall placement, an open and a closed merchant placement, seeded wallet/inventory/quest/rank/merit states, and a guild-exam eligibility case, without touching the developer database or invoking a remote, LLM, or image service.
- [x] 5.2 Add Playwright journeys proving registration success and idempotent re-registration, board list → detail → accept, active-quest abandon behind confirmation, completed-quest turn-in, and merit/exam eligibility with transition into the combat menu and the service dock torn down — all keyboard-only.
- [x] 5.3 Add Playwright journeys proving shop open/closed status at fixed world times, buy/sell quantity validation with exact copper/stock/wallet outcomes, stale and duplicate submission behavior, and repeated-inventory display with no use/equip control and no remote/ambiguous host control.
- [x] 5.4 Add a reconnect journey proving the services view rebuilds from server-persisted state in the new epoch's snapshot, an unsubmitted quantity is discarded, and no prior mutation is retried.
- [x] 5.5 Verify at 1440x900 and 1280x720 that narrative, status, and the service controls remain visible, keyboard-operable, literal-text safe, and localhost-only.

## 6. Regression, Spec Sync, and Verification

- [x] 6.1 Run focused service read-model, presenter, adapter, dispatcher, protocol, Node, and browser tests while iterating; fix regressions without weakening single-writer, atomicity, target-validation, envelope bounds, accessibility, or exactly-once assertions.
- [x] 6.2 Compare implementation and tests with all delta specs and the approved service/creation design; confirm character creation (23g), exploration movement/look/dialogue/rest-wait (23d), art, and combat-menu behavior remain outside this change and that no generic or unlisted action is registered.
- [x] 6.3 Sync the one new capability and one modified capability deltas into `openspec/specs/`, inspect the merged requirements, obtain canonical IDs with `uv run --locked python -m tools.spec_traceability list`, and annotate only substantively matching Python unit/integration/browser tests.
- [x] 6.4 Run `uv run --locked python -m tools.spec_traceability check`, `node --test web/static/webclient/js/tests/*.test.js`, and `uv run --locked python -m unittest discover -s web/tests/browser -t .` with the shared evidence path; close every genuine requirement gap without skipped or placeholder evidence.
- [x] 6.5 Run both required evidence-producing Python entry points and `uv run --locked python -m tools.spec_traceability verify --evidence <shared-path>`; preserve successful evidence for all current main requirements.
- [x] 6.6 Run the exact two-file aggregate branch-coverage sequence for `commands`, `server`, `typeclasses`, `web`, and `world`, verify coverage roots, and keep combined branch coverage at or above 90% with only `*/tests/*` omitted.
- [x] 6.7 Run `openspec validate webclient-service-menus --strict`, `openspec validate --all --strict`, `uv run --locked python -m compileall -q world typeclasses commands server web`, and `git diff --check`; verify no runtime/test path contacts a remote service or developer database.
