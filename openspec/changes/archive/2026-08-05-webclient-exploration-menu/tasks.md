## 1. Exploration Read Model and Presenter

- [x] 1.1 Implement `web/webclient/presentation/exploration.py` as the sole no-mutation builder of the version-1 `exploration` panel: the bounded `move` exit list (at most 12, `exit_ref` reusing the `local_map` `_exit_ref` convention, `label`, canonical `destination`, `enabled`/`disabled_reason` from presence + `traverse` lock), the `look` room/entities/objects lists (at most 32 entities and 32 objects with opaque identity, bounded display name, bounded kind, nullable `portrait_ref`), the `interact` target list (at most 32 targets, at most 8 affordances each) computed with the same checks commands use (`is_dialogue_host`/`dialogue_key_for` + dialogue-table keywords for scripted talk, `isinstance(npc, LLMNPC)` for free-form talk, present living hostile `Monster` without an active session for engage, `resolve_local_service_host` single-host for a `navigate`-kind service affordance with `surface` `"guild"`/`"shop"`), and the `character`/`quests`/`inventory` availability entries; add pure tests for each affordance rule, bounded ordering, locked-exit disclosure, the action-vs-navigate affordance shape (navigate never a registered adapter), and no mutation of any canonical surface.
- [x] 1.2 Implement `web/webclient/presentation/character.py` as the read-only version-1 `character` panel: bounded `traits` (gauges current/max, statics base), `passives`, `equipment`, `disguise` (active/description/displayed rows, never substituting disguised values), `guild` (rank/merit), and `wallet`, all through the existing `status_query`/`service_view` read models and sharing the same canonical trait/equipment/disguise source the `status` panel uses; add pure tests for true-vs-disguised values, empty displayed list when undisguised, byte-for-byte unchanged canonical state, and a status-vs-character parity test proving the two panels agree on shared values.
- [x] 1.3 Register `exploration` and `character` in the production presentation registry (schema version 1, exploration-mode-only unavailable form) and update registry tests that assume a status/combat/local_map/services/creation-only allowlist.
- [x] 1.4 Add presenter isolation tests: a corrupt dialogue table or missing host degrades only the affected affordance while the whole panel stays available, status and narrative stay healthy, and combat/creation modes render the schema-valid unavailable form with no fabricated exploration state.

## 2. Exploration Action Adapters

- [x] 2.1 Implement `web/webclient/actions/exploration_actions.py` with exact validators for `explore.move` (`exit_ref` 1..64 ASCII + `current_node` canonical node ID), `explore.look` (exactly one of `room: true` or a positive `target_id`), `explore.talk_scripted` (positive `npc_id` + `keyword_id` 1..64 non-empty), `explore.talk_freeform` (positive `npc_id` + `speech` 1..512 code points), `explore.engage` (positive `monster_id`), and `explore.wait` (exactly one of `daypart` in the four-name set, positive bounded `seconds`, or `sleep: true`); no payload accepts an actor/host/session/destination-room/price/stock/clock field.
- [x] 2.2 Implement the `explore.move` adapter: re-resolve the Exit from the actor's location by `exit_ref`, verify the actor's current node equals `current_node`, verify `exit.location is actor.location`, the destination exists, and the `traverse` lock, then invoke the Exit's own `at_traverse` so `MovementCostMixin.at_post_traverse` charges `command_defaults.move` and records the destination node; never relocate the actor or charge time directly; publish a full snapshot on success; add Evennia tests for exact 30-second charging, destination-node recording, stale guard, missing/locked exit, and combat `at_pre_move` veto with no time/knowledge change.
- [x] 2.3 Implement the `explore.look` adapter: route the room marker through `actor.at_look(actor.location)` and a present target through the ordinary display path so the `PlayerCharacter.at_look` onboarding hook still fires; present the appearance through the escaped text path; add Evennia tests for onboarding beat advancement at the gate, ordinary entity display, and tampered/absent target rejection with no onboarding write.
- [x] 2.4 Implement the `explore.talk_scripted` adapter: re-resolve the NPC, verify presence + resolved dialogue component + registered keyword, call `world.rules.onboarding.talk_response(npc, actor, keyword_id)`, and present the authored response; add Evennia tests for guard keyword recording, scripted-host no-state answer, and unregistered-keyword/non-host rejection.
- [x] 2.5 Implement `web/webclient/actions/dialogue_composition.py` with `build_dialogue_client()` mirroring `server/ai_director_service.py`: a function-local `OpenAICompatClient(get_profile("npc_dialogue"))` built only when the profile is enabled, a non-`None` offline stub otherwise, with no `world.ai` import or transport construction at module scope; add tests that a disabled profile yields the stub, an enabled profile yields a client, the stub's `get_response` fails loudly if ever invoked, and a cold import of the composition root binds no guardrail logger and loads no live transport when the profile is disabled.
- [x] 2.6 Implement the `explore.talk_freeform` adapter: re-resolve a present `LLMNPC`, obtain the client from the composition root, run `npc.at_talked_to(speech, actor, client)` as a Deferred, and publish a full snapshot on success; add Evennia tests with `FakeLLMClient` for reply presentation + memory + verified-intent application, offline degrade to greeting/silence, illegal-intent discard while speech is kept, and synchronous rejection of a tampered/non-eligible NPC before any client work.
- [x] 2.7 Implement the `explore.engage` adapter: re-resolve a present monster and call `world.rules.combat_session.engage(actor, monster)`, publishing `status` + `context_actions` with the mode change to `combat`; add Evennia tests for the engage-to-combat transition, `services`/`exploration` unavailable in combat, and rejection of remote/dead/non-hostile targets with no session created.
- [x] 2.8 Factor `world/rules/time_skip.py` as the single deterministic skip helper owning duration validation against the `clock.yaml` bounds, the bounded full-regen computation, and the `evaluate_skip_safety` + `WorldClock.advance(seconds, AdvanceSource.SKIP, [actor])` execution returning the `ScheduledEvent` list; refactor `commands/skip.py` to consume it (behavior-identical, existing `time-skip-commands` tests stay green). Implement the `explore.wait` adapter against the same helper: recheck safety, compute seconds exactly as `rest`/`sleep`/`wait` do, present the `ScheduledEvent` summary, and publish a full snapshot; add Evennia tests for daypart/duration/sleep arithmetic, safety rejections with no advance, and out-of-bounds or unknown-daypart values rejected before any advance.
- [x] 2.9 Register the six adapters in the production action registry and assert the production action set is exactly the three combat, seven service, four creation, and six exploration actions; update dispatcher tests that assert a combat+service+creation-only production registry.

## 3. Browser Exploration Dock and Protocol

- [x] 3.1 Extend `elosern/protocol.js` with the `exploration` and `character` panel allowlist entries and exact available/unavailable validators sharing the D1/D10 bounds with the server (12 exits, 32 entities/objects/targets, 8 affordances, 16 keywords, string/ID/portrait/identity limits, closed `kind`/`action_id` values), guarded by a dual-direction parity test and a worst-case serialization test; add Node tests for valid, malformed, unavailable, and over-bound rejection cases.
- [x] 3.2 Implement a DOM-independent `exploration_menu.js` render model reducing the validated `exploration` panel into the Move/Look/Interact/Character/Quests/Inventory root and submenus, with stable ordering, focus geometry, disabled-row non-submission, scripted keyword buttons, and the free-form speech flow; add Node tests for root routing, affordance rendering, disabled reasons, and the `navigate`-kind service affordance being dock-navigation only (never submitted as an action).
- [x] 3.3 Implement a DOM-independent `character_menu.js` render model for the `character` panel; add Node tests for trait/disguise/equipment/guild/wallet rendering with true values and no disguised substitution.
- [x] 3.4 Implement the `exploration_dock.js` and `character_dock.js` GoldenLayout plugins owning the action-dock surface in exploration mode: the stable root, per-category submenus, scripted keyword buttons, free-form dialogue that opens the command drawer with a server-held target reference, rest/wait daypart-and-duration forms parsed server-side, re-homed Quests/Inventory service submenus, and the client-local portrait-focus seam (only server-verified catalog values, `portrait_ref: null` never blocks look and never sends a focus packet); wire the dock to respect the in-flight lock, stale-revision unlock, offline overlay, and the exclusive mode transition — synchronously unloading and unregistering its keyboard handlers and discarding local selection/speech/quantity state when a `combat`-mode update is adopted, and remounting when exploration mode returns; update shell/layout tests and the shell smoke test.
- [x] 3.5 Wire the minimap's `node-action-ready` markers in `goldenlayout.js` to submit `explore.move` with the descriptor's `exit_ref` and the panel's `current_node`; remembered remote nodes stay inert with no travel action; update local-map render tests to assert movement submission and the no-invented-destination rule.
- [x] 3.6 Re-home the service dock under the exploration dock's Interact/Quests/Inventory roots in `services_dock.js` without changing the `services` panel payload or the seven service adapters; update the service-dock navigation entry points and tests, and move the mode-change teardown responsibility to the exploration dock so a `combat`-mode adoption unloads the exploration dock (unregistering keyboard handlers and discarding selection, speech, quantity, and confirmation state) with only the combat dock owning action-dock focus.
- [x] 3.7 Ensure labels, disabled reasons, server-rendered dialogue/detail, and portrait alternative text are inserted as text (never trusted HTML), focus is distinguishable without color, speech input accepts bounded text via keyboard only, and no take/drop or generic object-mutation control is ever rendered.

## 4. Managed Browser Acceptance

- [x] 4.1 Extend deterministic browser fixtures with grid/wilderness/instance/interior placements, a scripted-dialogue host, an `LLMNPC` with a disabled `npc_dialogue` profile, a living hostile monster, and an onboarding character at the South Gate, without touching the developer database or invoking a remote, LLM, or image service.
- [x] 4.2 Add Playwright journeys proving movement through `explore.move` for grid, wilderness, instance, and interior layers with matching time charge, location, narrative, and minimap updates together — all keyboard-only.
- [x] 4.3 Add Playwright journeys proving look at the room advances the onboarding look beat, look at present entities uses ordinary display, scripted keyword dialogue completes, and free-form dialogue degrades to greeting/silence offline with the dock unlocking afterward.
- [x] 4.4 Add Playwright journeys proving engage transitions into the combat dock, wait/rest daypart and duration acceptance plus safety rejections, service submenus reachable through Quests/Inventory, stale and duplicate submission behavior, tampered exit/NPC rejection, and reconnect retention — all keyboard-only.
- [x] 4.5 Add a reconnect journey proving the exploration dock rebuilds from server-persisted location/knowledge/NPC state in the new epoch's snapshot, an unsubmitted speech or selection is discarded, and no dialogue or mutation is automatically replayed.
- [x] 4.6 Verify at 1440x900 and 1280x720 that narrative, status, the exploration dock, and the minimap remain visible, keyboard-operable, literal-text safe, localhost-only, and that `portrait_ref: null` produces no portrait card and no focus packet.

## 5. Regression, Spec Sync, and Verification

- [x] 5.1 Run focused exploration read-model, presenter, adapter, dispatcher, protocol, Node, and browser tests while iterating, and re-run the existing `time-skip-commands` and `commands/tests/test_command_branch_behaviour.py` skip tests to prove the `world/rules/time_skip.py` refactor is behavior-identical; fix regressions without weakening single-writer, movement-charge, onboarding, dialogue-degrade, envelope-bound, accessibility, or exactly-once assertions.
- [x] 5.2 Compare implementation and tests with all delta specs and the approved map/exploration design; confirm art (23f), combat (23b), service payload/adapters (23e), creation (23g), and the deterministic game loop remain outside this change, that no take/drop or generic action is registered, and that the six `explore.*` adapters are the only additions to the production registry.
- [x] 5.3 Sync the one new capability and three modified capability deltas into `openspec/specs/`, inspect the merged requirements, obtain canonical IDs with `uv run --locked python -m tools.spec_traceability list`, and annotate only substantively matching Python unit/integration/browser tests.
- [x] 5.4 Run `uv run --locked python -m tools.spec_traceability check`, `node --test web/static/webclient/js/tests/*.test.js`, and `uv run --locked python -m unittest discover -s web/tests/browser -t .` with the shared evidence path; close every genuine requirement gap without skipped or placeholder evidence.
- [x] 5.5 Run both required evidence-producing Python entry points and `uv run --locked python -m tools.spec_traceability verify --evidence <shared-path>`; preserve successful evidence for all current main requirements.
- [x] 5.6 Run the exact two-file aggregate branch-coverage sequence for `commands`, `server`, `typeclasses`, `web`, and `world`, verify coverage roots, and keep combined branch coverage at or above 90% with only `*/tests/*` omitted.
- [x] 5.7 Run `openspec validate webclient-exploration-menu --strict`, `openspec validate --all --strict`, `uv run --locked python -m compileall -q world typeclasses commands server web`, and `git diff --check`; verify no runtime/test path contacts a remote service or developer database.
- [x] 5.8 Fix the gauge-storage defect the webclient movement path exposed: `_settle_gauge_regen` and buff rate ticks persisted float `current` values into character gauges (the Evennia base boundary clamp returns the float `max` when a gauge is full), which made the strict `status` read model raise and the status panel unavailable after any time-charging action; guarantee integral storage in `DeterministicGaugeTrait._enforce_boundaries`, carry a persisted sub-unit `regen_remainder` in `world/rules/clock.py` so segmented advances equal one equal-length advance, and add regressions for fractional regen, segmented-vs-single equality, full-gauge clamps, and full-gauge poison ticks.
