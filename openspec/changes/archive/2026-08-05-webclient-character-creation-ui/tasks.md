## 1. Creation Messages and Wizard Draft Service

- [x] 1.1 Implement `world/rules/creation_messages.py` mapping every deterministic `CharacterCreationError` (unknown preset, invalid display name, markup delimiter, underage actual/apparent age, unknown race, unknown/incompatible subrace, malformed/out-of-span/off-budget allocations, not pending, already complete) to a stable code and safe Traditional Chinese message, with a bounded generic fallback that never exposes a traceback or raw payload; add tests for every mapped reason and the fallback.
- [x] 1.2 Implement `world/rules/creation_wizard.py` as the sole writer of the `creation_draft` attribute: `read_creation_view` (no-mutation preset cards + custom descriptor from `PLAYER_PRESET_REGISTRY`, `RACE_REGISTRY`, `SUBRACE_REGISTRY`, and `resolve_starting_profile`), `save_preset_draft` (registry-validated `preset_selected`), `save_custom_draft` (validates the complete request through public `preflight_character_creation` before persisting `custom_filled`), `read_draft`, `activate_draft` (one deterministic `transaction.atomic()` block that re-validates the draft and re-checks ownership and `creation_pending` against committed state, calls `activate_player_character`, and clears the draft in the same transaction), and `clear_draft` (idempotent); add pure/Evennia tests for draft persistence across reload, invalid-save rejection leaving the prior draft and canonical attributes unchanged, activation clearing the draft atomically, a draft-clear write failure rolling back the whole activation, two concurrent activations applying exactly once, and reset idempotency.
- [x] 1.3 Prove the read model is side-effect free: add an Evennia test that builds the full creation view before and after a snapshot and asserts `creation_pending`, the draft, identity attributes, traits, and world time are unchanged, including a pending character with no materialized traits.

## 2. Creation Panel Presenter

- [x] 2.1 Implement `web/webclient/presentation/creation.py` with the exact version-1 available schema (D1/D2 bounds: at most 8 preset cards, 8 races, 16 subraces, 16 profiles, six axes per profile, string/age/allocation ceilings), the common unavailable form outside creation mode and on global-prerequisite `PanelUnavailableError`, and no persona/import-only field; register `creation` in the production registry and update registry tests that assume a status/combat/local-map/services-only allowlist.
- [x] 2.2 Add server-side payload-validation tests for every bound in D2, deterministic profile/preset ordering, draft shape for both stages, a worst-case serialization test proving the structurally maximal realistic payload fits comfortably under the 65,536-byte envelope, and a second test proving a payload maximizing every string field at once is rejected by the byte gate.
- [x] 2.3 Add presenter isolation tests: a malformed registry entry or corrupt draft degrades only the relevant card/profile/draft while the panel stays schema-valid, status and narrative stay healthy, and exploration/combat modes render the schema-valid unavailable form with no fabricated creation state.

## 3. Creation Action Adapters

- [x] 3.1 Implement `web/webclient/actions/creation_actions.py` with exact validators and adapters for `creation.preset` (`preset_key` 1..64), `creation.custom` (`display_name` 1..80; `age`/`apparent_age` integers 18..10000; `race` and nullable `subrace` registry keys 1..64; `allocations` exactly the six axes each an integer 0..10000), `creation.activate` (empty payload), and `creation.reset` (empty payload); every adapter obtains the owning account from the session puppet, rejects unknown/authority-like fields, validates through `preflight_character_creation`/`activate_player_character`/the wizard service, and never assigns `.db`, traits, identity attributes, or `creation_pending` directly.
- [x] 3.2 Register the four adapters in the production action registry and assert the production action set is exactly the three combat, seven service, and four creation actions; update dispatcher tests that assert a combat-plus-service production registry.
- [x] 3.3 Add Evennia integration tests for every adapter: success (preset selection, custom preflight-and-save, activation, reset), domain rejection (unknown preset, bad name, underage actual age, underage apparent age, unknown race, incompatible subrace, off-span/off-budget allocations, not pending, already complete, activate-without-draft, missing or unowned account on a non-character puppet), tampered identities and authority-like fields rejected before the domain API, dispatcher-level stale revision and duplicate request handling, and a before/after assertion that no canonical surface changes on rejection.
- [x] 3.4 Add `creation.activate` integration tests proving the all-or-nothing `activate_draft` outer transaction (re-validation and ownership/pending re-check against committed state, draft cleared in the same transaction), the unchanged South Gate relocation and onboarding arrival, the full exploration snapshot (no affected-panel update), `creation_pending` false, the draft-clear write failure that rolls the whole activation back, two concurrent activations applying exactly once, and the failed-relocation degradation that preserves activated state.

## 4. Browser Creation Dock

- [x] 4.1 Extend `elosern/protocol.js` with the `creation` panel allowlist entry and exact available/unavailable validators sharing the D2 constants with the server (guarded by a dual-direction parity test); add Node tests for valid, malformed, unavailable, unknown-card/race/profile, bound, and unknown-node-style rejection cases.
- [x] 4.2 Implement a DOM-independent `creation_menu.js` render model reducing the validated panel into preset cards, custom form state (selected race/subrace, six allocation inputs, name and adult age fields), saved-draft restoration, and confirmation state; add Node tests for card routing, field geometry, bounds validation, confirmation gating, draft restoration, disabled-row non-submission, and exact payload production.
- [x] 4.3 Implement the `creation_dock.js` GoldenLayout plugin rendering the creation dock in creation mode (preset cards, custom form with Tab/Shift+Tab field navigation, confirmation screens for activation and the destructive reset, field-associated live-region messages), respecting the in-flight lock, stale-revision review, offline overlay, and the exclusive mode transition — synchronously unloading and unregistering its keyboard handlers and discarding local state when a non-creation snapshot is adopted, and never resetting a router it does not own; update shell/layout tests and the shell smoke test.
- [x] 4.4 Ensure labels, disabled reasons, and validation messages are inserted as text (never trusted HTML), focus is distinguishable without color, adult fields advertise the 18 minimum, and no canonical or draft creation state is written to localStorage.

## 5. Managed Browser Acceptance

- [x] 5.1 Extend deterministic browser fixtures with a pending-creation account (creation-pending adult shell, empty trait set, no activation) and a saved-draft case, without touching the developer database or invoking a remote, LLM, or image service.
- [x] 5.2 Add Playwright journeys proving preset selection → confirmation → activation → exploration snapshot with the creation dock torn down, and custom finite controls + free-text field focus → confirmation → activation — all keyboard-only.
- [x] 5.3 Add Playwright journeys proving server rejection of `age=17` and `apparent_age=17` despite disabled client-side validation, the destructive reset confirmation, stale and duplicate submission behavior, and reconnect at a saved draft stage that never auto-resubmits activation.
- [x] 5.4 Verify at 1440x900 and 1280x720 that narrative, status-unavailable, and the creation controls remain visible, keyboard-operable, literal-text safe, localhost-only, and that the creation dock is the sole action-dock owner in creation mode.

## 6. Regression, Spec Sync, and Verification

- [x] 6.1 Run focused creation-message, wizard-service, presenter, adapter, dispatcher, protocol, Node, and browser tests while iterating; fix regressions without weakening single-writer, atomicity, adult-gate, envelope-bound, accessibility, or exactly-once assertions.
- [x] 6.2 Compare implementation and tests with all delta specs and the approved creation/service design; confirm service menus (23e), exploration (23d), art, and combat behavior remain outside this change, that no generic or unlisted action is registered, and that the `player-character-creation` MODIFIED delta is the only amendment to an existing deterministic creation contract.
- [x] 6.3 Sync the one new capability and the two modified capability deltas into `openspec/specs/`, inspect the merged requirements, obtain canonical IDs with `uv run --locked python -m tools.spec_traceability list`, and annotate only substantively matching Python unit/integration/browser tests.
- [x] 6.4 Run `uv run --locked python -m tools.spec_traceability check`, `node --test web/static/webclient/js/tests/*.test.js`, and `uv run --locked python -m unittest discover -s web/tests/browser -t .` with the shared evidence path; close every genuine requirement gap without skipped or placeholder evidence.
- [x] 6.5 Run both required evidence-producing Python entry points and `uv run --locked python -m tools.spec_traceability verify --evidence <shared-path>`; preserve successful evidence for all current main requirements.
- [x] 6.6 Run the exact two-file aggregate branch-coverage sequence for `commands`, `server`, `typeclasses`, `web`, and `world`, verify coverage roots, and keep combined branch coverage at or above 90% with only `*/tests/*` omitted.
- [x] 6.7 Run `openspec validate webclient-character-creation-ui --strict`, `openspec validate --all --strict`, `uv run --locked python -m compileall -q world typeclasses commands server web`, and `git diff --check`; verify no runtime/test path contacts a remote service or developer database.
