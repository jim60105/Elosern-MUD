## Why

The fire skill tree (`docs/lore/skill-trees/fire.md`, 12 nodes: one root chain, one three-way branch point, the two-parent 神格 convergence) is the node-data authority, but the registry still ships the dev-era 2026-08-12 ten-row fire set — no coefficients, a `self_heal` clause on `sacrificial_flame` that the tree never had (治療 is 光's verb), an ignite-less `lava_burst`, no `scorching_armor` or `crimson_apotheosis`, and a `fire_scorch` DoT frozen at the pre-§3.3 −5 rung. Every behavior primitive this wave needs is ALREADY shipped: the `physical_hit` event + `apply_buff_to_source` then-action (on-hit-counter-damage, live at HEAD as the earth carapace counter), the `marker: ground` hazard clause with standing-on-it/lifecycle semantics (terrain-marker, live at HEAD as the earth fissure rows), and `DamagePolicy(bypass_defense=True)` 處決級 (dark/water precedent). This change is a **pure data-only** catalog integration: it replaces the old rows wholesale and authors the 12-node tree as registry + rulebook data over that shipped vocabulary — zero new behavior code.

## What Changes

- Replace the ten dev-era fire registry rows with the 12-node tree verbatim from the node table (keys, labels, targets, MP costs, prerequisites via `SkillPrerequisite(node, N)`, coefficients per §3.2, 處決級 tier, caps via the existing reverse-edge derivation — no stored cap field; `crimson_apotheosis` is the two-parent capstone at 燔祭焰 Lv.10 ＋ 焚世之焰 Lv.10). Key census: TEN re-authored (all dev-era keys survive by name — `fire_arrow`, `fire_ball`, `scorching_wave`, `firestorm`, `flame_shroud`, `lava_burst`, `dragon_flame`, `hellfire`, `sacrificial_flame`, `final_blaze`), TWO new keys (`scorching_armor`, `crimson_apotheosis`). `sacrificial_flame`'s off-tree `self_heal` effect is DELETED without alias — the lore node's 燃身 clause is a self-burn cost (self-buff of a burn row), never healing (治癒 is 光's §4.2 exclusive verb; §4.4 ratified 燃身換爆發 as burn cost). Re-homed keys carry no aliases.
- Author every node's effect composition over SHIPPED vocabulary only: `damage:fire:magic` coefficients (1.0 → 4.4, the two 燃身 overflow coefficients 3.4/4.4 authored as static policy values — the cost IS the self-burn row); `DamagePolicy(bypass_defense=True, predicate=())` for `final_blaze` 處決級 (dark/water shipped shape); `buff_apply:fire_scorch` (re-homed to the −8/10 s rung, 300 s per the node table) on `scorching_wave`; `self_buff_apply:fire_scorching_armor` (empty-modifier mount, 60 s — the tree's own buff band, D2) on `scorching_armor`; `buff_apply:fire_lava` (NEW `marker: ground` row, the melt-the-ground hazard) on `lava_burst`; `self_buff_apply:` the self-burn cost rows (`fire_sacrifice_burn` −12/10 s 120 s on `sacrificial_flame`, `fire_crimson_self` −20/10 s 300 s on `crimson_apotheosis`) plus `buff_apply:fire_crimson_burn` (−40/10 s 300 s, ENEMIES audience) on the capstone.
- Author the fire rulebook rows (data-only, same surfaces): `fire_scorch` re-homed −5→−8 per interval (300 s duration kept — the node table prices it), NEW `fire_ignite` (灼燒 −8/10 s, 60 s — scorching_armor's 點燃), NEW `fire_scorching_armor` (empty-modifier self mount, 60 s — D2), NEW `fire_lava` (`marker: ground`, −12/10 s, 60 s — the magnitude/duration lore-gap decision of D3, derived from the §3.3 大師 rung + the burn ladder's −12 rung + earth_fissure precedent), NEW `fire_sacrifice_burn` (−12/10 s, 120 s), NEW `fire_crimson_self` (−20/10 s, 300 s), NEW `fire_crimson_burn` (−40/10 s, 300 s); and the ONE `state_reactions.yaml` rule `scorching_armor_ignite`: `when {event: physical_hit, buff_active: fire_scorching_armor}` `then {apply_buff_to_source: fire_ignite}` — the on-hit-counter-damage shipped event + source-targeted action, holder-gated through the shipped `buff_active` when-key, the earth carapace rule's shape inverted from counter to ignite. No fire line touches the reaction engine, marker loader, policy dataclass or any generic code.
- `fire_mastery` stays exactly as shipped: existing PASSIVE, off-lineage, `passive_trait:element_mastery`, freeform scale ladder already read from `progression.yaml` — zero rows change (recorded census fact, not an edit).
- Retire the fire catalog echo tests in-file: `FIRE_SPELL_CATALOG` and its `FireLineageTreeCatalogTests`-sibling `FireSpellCatalogTests` echo methods (field-equality, effect round-trip, fire_ball recost pin) in `world/skills/tests/test_spell_catalogs.py` (the `_elemental_spells` builder-shape mechanic test in that class MOVES beside the remaining builder users; the bare-`self_heal`-prefix parse test already lives in `ClosedVocabularyParseTests` and survives untouched), the `test_fire_spell_tier_labels_match_the_catalog` tier-label echo in `world/skills/tests/test_cost_tiers.py`; the canonical ID `skill-registry::skill-registry-contains-the-full-火-element-spell-set` retires with its requirement (per-file freeze-list entries adjusted only as `tools.test_data_lint check` output decides; the synthetic `replace(SKILL_REGISTRY["fire_ball"], cost=...)` band probes in `test_cost_tiers.py` survive untouched — they overwrite the cost with synthetic values and never observe the live row). The lineage-capability sibling `FireLineageTreeCatalogTests` in the same file asserts the dev-era edge table and the `sacrificial_flame` canopy — its expected-edge dict updates in place to the authored tree (+`scorching_armor`, +`crimson_apotheosis`) with the canopy assertion moving to `crimson_apotheosis` (design D5). The dev-era numeric pins in `world/rules/tests/test_buffs.py` for `fire_scorch` (duration 300, −5 tick, expiry) STRIP to load/apply/presence assertions per the ratified no-data-echo contract (explicit decision, design D7: no re-pinned values; the multi-buff tick-ordering test keeps order + record shape + `poisoned`'s own −5 pin and DROPS the fire-inclusive HP total, since the re-homed −8 row makes the cross-row −10 pin false).
- Sync `world/rules/rulebook/status_display.yaml` with the key census (the shipped import-time coverage check covers exactly the live buff keys ∪ combat-modifier rule ids; state-reaction rule ids are NOT in that census — verified at the earth wave): `fire_scorch` keeps its 灼燒 row; add rows for `fire_ignite` (點燃), `fire_scorching_armor` (灼熱裝甲), `fire_lava` (熔岩), `fire_sacrifice_burn` (燔祭自灼), `fire_crimson_self` (紅蓮自灼), `fire_crimson_burn` (紅蓮業火) — SIX new buff keys — per the table's label/severity conventions.
- Docs/traceability integration owned here: shard-manifest final state (`.github/evennia-shards.json` — this change owns the wave's only manifest edit); `fire.md` stays the authority and is untouched; `docs/lore/magic-system.md` §3's fire row checked for contradiction only (expected: none — it states the 爆發・處決・灼燒 identity this tree implements); `docs/development/adding-spells.md`'s `_elemental_spells` fire example stays grammatically valid (the builder survives for other elements) but no longer mirrors the fire block's authored shape (individually written `_spell` rows with `effect_policies`) — record the drift, edit only if the shipped example block itself breaks; `tests/test_command_docs.py` untouched (no command-surface change).

## Capabilities

### New Capabilities
None.

### Modified Capabilities
- `skill-registry`: the duplicated ten-row 火-element data-contract requirement is REMOVED and replaced by one behavioral requirement — fire spell progression composes executable HP・消滅 behavior (burn-DoT ladder rungs on the hp axis, an on-physical-hit ignite of the attacker mounted by a detectable self-buff through the shipped reaction vocabulary, a ground-lava marker hazard whose standing-on-it fact ticks the authored DoT, a 處決級 execution rung that ignores defense, self-burn cast costs priced as authored policy coefficients, a branch-point/three-terminal-lineage with a two-parent capstone gate) through the shared mechanics, with the dev-era rows — including the off-tree `self_heal` clause — deleted without alias.
- `skill-lineage`: the dev-era requirement 「The fire lineage ships as the first-round linear tree」 is MODIFIED to the authored 12-node fire tree (all nine dev-era spine/leaf edges survive verbatim; the `scorching_armor` and two-parent `crimson_apotheosis` edges are added; the strict-canopy premise moves from `sacrificial_flame` to `crimson_apotheosis` — the lore tree grew a child; mastery-passives clause preserved unchanged).
- Consequences recorded without deltas (no behavior of a generic surface changes): (a) freeform-eligibility — with the off-tree `self_heal` deleted, `sacrificial_flame`'s effect set (`damage` + `self_buff_apply`) stops satisfying `is_freeform_eligible`'s closed scalable-prefix set; freeform scaling of that one node is intentional ratified fallout of the verb correction (design D8) and the `freeform-casting` scenario's example swap is a main-sync item — no test pins the live row; (b) the `buff-handler-integration` ordered-tick scenario's illustrative `fire_scorch` delta example goes stale with the −8 re-home (requirement logic is generic; recorded for the main-sync).

## Impact

`world/skills/registry.py` (fire block); `world/rules/rulebook/buffs.yaml` (re-homed/new fire rows), `world/rules/rulebook/state_reactions.yaml` (the one ignite rule) and `world/rules/rulebook/status_display.yaml` (display-metadata census sync); `world/skills/tests/test_spell_catalogs.py` (echo retirement + lineage edge-table update), `world/skills/tests/test_cost_tiers.py` (tier-label echo removal), `world/rules/tests/test_buffs.py` (pin strip + ordering-total drop); new synthetic behavior module → `.github/evennia-shards.json`; traceability ledger hygiene + the freeform-scenario example swap at the separately authorized main-sync. `openspec list --json` returned an empty change set at authoring time — no serialization gate against active changes. One engineer-day.

## Batch

This is a single-change wave: zero dependencies (every primitive it consumes — `marker: ground`, `physical_hit` + `apply_buff_to_source`, `buff_active` when-key, `bypass_defense`, `self_buff_apply`, reverse-edge caps, n-ary lineage DAG — is merged at master 0cede4c; research verified each against shipped source before authoring). No sibling change shares a file with it except as sole author.

- depends-on: (none)

Planning artifacts only this turn; no apply/archive/sync/merge.
