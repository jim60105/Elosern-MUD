## 0. Prerequisites (exact predecessor contracts)

- `displaced-knockback` and `unconditional-multi-strike` are MERGED (batch rows 1–2) — their shipped deltas (`marker: positional` clause + mount refusals + ground sweep + gate, and predicate-free `extra_strikes`) are re-read from the merged specs before authoring; design.md D1/D2/D7 are the reconciliation point if a sibling's implementation revised a detail.
- Consumed archived surfaces verified at master 9ca95ba: `buff_apply` audience + `spell_tier_for` source_tier (`action.py:572+`), `buff_active` modifier rules + `agility_flat`/`accuracy` merge (`combat_modifiers.py`; earth defense-ladder + equipment precedent live), `bypass_defense`/`max_hp_fraction` rungs, n-ary DAG + two-parent capstone + reverse-edge caps (fire crimson live), freeform ladder (`wind_mastery` census fact), initiative raw-agility exception (ratified, D8 — ZERO initiative work).

## 1. Registry + rulebook data

 - [x] 1.1 Confirm both sibling changes are merged and inspect current source/exported references (codegraph/LSP) for the merged positional/gate spec, the merged DamagePolicy validation, the dev-era wind registry block, the dev-era buff keys (`wind_haste`, `wind_haste_domain`), the earth/fire individually-written block shape, and the shipped devastation rung form (re-read the water/dark capstone `max_hp_fraction` authored values — D11's one re-verification) before editing; if any D1-table mechanic is missing, STOP and fix it in its owning behavior surface — do not bolt code onto the catalog.
 - [x] 1.2 Author the 13-node tree exactly per design.md D1 (keys/labels/targets/MP costs/prerequisites/coefficient+policies/effect bindings; `gale_step`/`gale_chain_step`/`afterimage_step` SELF + SELF_ONLY; `haste_domain` ALLY-audience area; canopy two authored parents at threshold 10), deleting nothing but dev-era bindings (all nine surviving keys re-authored in place; four new keys); every new/re-authored wind ACTIVE skill declares `usable_out_of_combat=True` per the shipped deliberate-declaration policy (the frozen USABLE_OUT_OF_COMBAT_FALSE inventory stays exactly `{"flee"}` — an omitted declaration fails the inventory test); verify the two-root DAG + canopy validates at registry load through the shipped lineage validator and tip caps derive unchanged.
 - [x] 1.3 Author the wind buff rows per D1/D6: delete dev-era `wind_haste`/`wind_haste_domain`; add 4 empty-modifier ladder mounts (`gale_step_haste` 60 s; `gale_chain_step_haste` 60 s; `afterimage_step_haste` 60 s; `haste_domain_haste` 60 s — D4's duration derivation) and 3 positional rows (`displaced` 30 s / `displaced_tempest` 60 s / `displaced_apotheosis` 90 s — `marker: positional`, empty modifiers, debuff polarity).
 - [x] 1.4 Author the `combat_modifiers.yaml` wind rules per D3: `wind_gale_step_agility` `{agility_flat: 3}`, `wind_gale_chain_step_agility` `{agility_flat: 5}`, `wind_afterimage_agility_accuracy` `{agility_flat: 8, accuracy: -10}` (the bipolar rung), `wind_haste_domain_agility` `{agility_flat: 12}` — all `buff_active`-gated on the mount keys; NO rule keyed to the `displaced*` rows (D5 Non-Goal).
 - [x] 1.5 Author the effect compositions per D7: `storm_domain` coefficient 1.4 + `buff_apply:displaced` (ENEMIES); `gale_dance_strike` coefficient 2.0 + `DamagePolicy(extra_strikes=1)`; `sky_tempest` coefficient 2.8 + devastation rung + `buff_apply:displaced_tempest`; `vacuum_severance` coefficient 4.0 + `DamagePolicy(bypass_defense=True, predicate=())`; `sky_apotheosis` coefficient 3.8 + devastation rung + `buff_apply:displaced_apotheosis`; `wind_blade` 0.7 and the plain rungs' coefficients per the table AS-IS. `world/rules/rulebook/state_reactions.yaml` MUST NOT be edited — zero wind reaction rows (D2; the hp_loss dispatch carries no source_skill).
 - [x] 1.6 Sync `world/rules/rulebook/status_display.yaml`: delete `wind_haste`/`wind_haste_domain`; add the 4 mounts (beneficial) + 3 positional rows (harmful) at the table's label/severity conventions (the shipped import-time coverage check fails closed on drift); modifier rule IDs are not in that census (verified at authoring).
 - [x] 1.7 Buff-key + registry-key census: no dangling `buff_apply:`/`self_buff_apply:` target; `wind_haste*` gone from buffs.yaml/registry/status_display with no alias; `player_presets.py` re-checked (references only surviving keys); `test_buffs.py`'s `wind_haste*` pair stripped per D6 (load/apply/presence for the new mounts, positional behavior lives in the sibling's module — NO re-pinned ceilings/durations, no row mirroring).

## 2. Echo-test retirement + docs hygiene

 - [x] 2.1 Retire `WIND_SPELL_CATALOG` + `WindSpellCatalogTests` echo methods in `world/skills/tests/test_spell_catalogs.py`; relocate builder-shape MECHANIC assertions beside remaining builder users the fire way; retire the wind tier-label echo row in `world/skills/tests/test_cost_tiers.py`; ledger/freeze hygiene deferred to the separately authorized main-sync.
 - [x] 2.2 Update the shipped-content lineage contract: the wind lineage catalog test (fire `FireLineageTreeCatalogTests` shape) covering the authored 13-node two-root tree incl. the canopy; lineage edges are the registered-content shipped contract (the lineage capability's convention), NOT a data echo.

## 3. Behavioral evidence (synthetic only)

 - [x] 3.1 Disposable offline engine scenario before permanent tests, exercising each distinct D1 composition through real casts: ladder rungs on real to-hit/flee consumers + expiry; bipolar rule both poles on holder only; ally-domain audience skip; storm knockback (victim displaced, melee gate flips both ways, ground-marker sweep on mount, ally + defeated refusals); gale_dance_strike two-roll settlement incl. ordered projection + single payment; execution vs high defense; devastation fraction matches the shipped rung read in 1.1; canopy three-component settlement + both-parent gate; retired-key rejection paths.
 - [x] 3.2 Synthetic behavior tests in a new module `world/skills/tests/test_wind_spell_behavior.py` mirroring the ADDED requirement's scenarios (no catalog-row equality, key-set, or data-echo assertions anywhere — ratified NON-GOAL); register in exactly one shard (`rules-a`) in `.github/evennia-shards.json` (the wave's final manifest edit; the lineage catalog test file already carries a home).

## 4. Focused invocation

```sh
uv run --locked evennia test --settings test_settings.py --keepdb world.skills.tests.test_spell_catalogs world.skills.tests.test_cost_tiers world.skills.tests.test_skill_registry world.rules.tests.test_skill_lineage world.skills.tests.test_wind_spell_behavior world.rules.tests.test_buffs world.rules.tests.test_status_display world.rules.tests.test_combat_modifiers world.rules.tests.test_damage_state_feedback world.rules.tests.test_positional_marker world.rules.tests.test_unconditional_multi_strike
uv run --locked python -m tools.observability_lint check
uv run --locked python -m tools.test_data_lint check
uv run --locked python -m tools.spec_traceability check
uv run --locked openspec validate wind-spell-catalog --strict
```

Pass `MUD_TEST_SETTINGS=1` through the tool environment, never a shell prefix. No full local suite/browser/aggregate-coverage run; no command above ten minutes. No apply, archive, main-spec sync or merge in the proposal turn. Canonical IDs via `uv run --locked python -m tools.spec_traceability list` at the separately authorized main-sync; the retired 風 ID `skill-registry::skill-registry-contains-the-full-風-element-spell-set` leaves the ledger with its requirement at the sync; the new IDs `skill-registry::wind-spell-progression-composes-executable-speed-and-knockback-behavior` and `skill-lineage::the-wind-lineage-ships-as-the-authored-two-root-branching-tree-with-a-two-parent-canopy` annotate `test_wind_spell_behavior`/the lineage catalog test (defer-annotation-to-main-sync applies while the IDs read red pre-sync).
