## Why

The earth skill tree (`docs/lore/skill-trees/earth.md`, 14 nodes: two roots, two branch points, the two-parent 神格 convergence) is the node-data authority, but the registry still ships the dev-era 2026-08-12 ten-row earth set — no prerequisites, no coefficients, `earth_bind`-era bindings, defense buffs with no lineage, and no terrain markers at all. Every behavior primitive this wave needed is now specified by the sibling changes: the ground-hazard marker fact + lifecycle + `buff:<key>` predicate seam (`terrain-marker`) and the `physical_hit` + `counter_damage` reaction vocabulary (`on-hit-counter-damage`). This change is the final **data-only** integration: it replaces the old rows wholesale, authors the 14-node tree as registry + rulebook data over that vocabulary, exactly mirroring the archived `dark-spell-catalog` precedent.

## What Changes

- **Batch declaration.** This change lands LAST; both sibling earth changes are listed in `## Batch` below with machine-readable `depends-on:` lines and the code-conflict notes the supervisor queues from.
- Replace the ten dev-era earth registry rows with the 14-node tree verbatim from the node table (keys, labels, targets, MP costs, prerequisites via `SkillPrerequisite(node, N)`, coefficients per §3.2, 處決級/毀滅級 tiers, caps via the existing reverse-edge derivation — no stored cap field; `earthen_apotheosis` is the two-parent capstone). Key census: nine re-authored (`stone_shard`, `hardened_skin`, `stone_armor`, `dust_veil`, `rockslide`, `earthquake`, `earthen_ward`, `mountain_collapse`, `earths_judgment`), FIVE new keys (`bedrock_bastion`, `thorned_carapace`, `ground_fissure`, `fault_rupture`, `earthen_apotheosis`), and `earth_bind` DELETED — the tree has no bind node and 束縛 stays ice's verb per the lore's own note (土讓腳下變壞，冰讓身體停下). Re-homed keys keep no aliases.
- Author every node's effect composition over the wave's vocabulary: `damage:earth:magic` coefficients; `DamagePolicy(unconditional_defense_bypass=True)` execution rung, `max_hp_fraction=0.10` devastation rung, and the 大地審判 synergy as `DamagePolicy(predicate=("buff:earth_fissure", "buff:earth_fissure_quake", "buff:earth_fissure_apex"), attack_multiplier=1.15, unconditional_defense_bypass=True)` — D6's owned rung (4.0 × 1.15 lands the flat +0.6 exactly) (all terrain-marker shipped); `buff_apply:` the defense rows / dust accuracy row / `ice_slow` reuse / the three marker-hazard rows (terrain-marker shipped); one `state_reactions.yaml` rule `physical_hit` + `counter_damage: 1.0` mounted by the `thorned_carapace` buff (on-hit-counter-damage shipped).
- Author the earth rulebook rows (data-only, same surfaces): `earth_hardened_skin` re-homed (+3/60 s, unchanged numbers, now lineage-gated by the registry), `earth_stone_armor` re-homed (+5/60 s), NEW `earth_bedrock` (+8/60 s), `earth_ward` re-homed (+12/60 s per the node table), `earth_dust_veil` re-homed (命中 −5, duration 20 s per the node table — the dev-era 60 s row's duration moves; its shape is already the lore's), `earth_root` DELETED (the tree has no bind node; `earth_bind` gone), NEW `earth_fissure` (marker clause + rate −12/10 s + duration 60, the ground-hazard row), NEW `earth_fissure_quake` (same clause/DoT, duration 40 — the node table prices 地震術's mark at 40 s and durations are row-level; design D5), NEW `earth_fissure_apex` (marker clause + rate −40/10 s + duration 90, the 神格 top rung — 敏捷 −3 rides via the `ice_slow` reuse composed at the node, per lore 「掛冰階梯第一 rung」), NEW `earth_carapace` (marker-less self buff with no modifiers — the reaction rule keys off `buff_active: earth_carapace`… see design D2 for the rule's holder condition), and the one `state_reactions.yaml` counter rule. `ice_slow` is reused as pure data reuse (lore explicitly names 冰階梯第一 rung); no ice-side row changes.
- Retire the earth catalog echo tests in-file: `EARTH_SPELL_CATALOG` and its test class in `world/skills/tests/test_spell_catalogs.py`, the earth rows in `world/skills/tests/test_cost_tiers.py`'s tier table; the canonical ID `skill-registry::skill-registry-contains-the-full-土-element-spell-set` retires with its requirement (per-file freeze-list entries adjusted only as `tools.test_data_lint check` output decides). The dev-era numeric pins in `world/rules/tests/test_buffs.py` for the five earth rows STRIP to load/apply/presence assertions per the ratified no-data-echo contract (explicit decision, design D7: no re-pinned durations/ceilings; the `earth_root` test retires with its row).
- Sync `world/rules/rulebook/status_display.yaml` with the key census (the shipped import-time coverage check covers exactly the live buff keys ∪ combat-modifier rule ids — state-reaction rule ids are NOT in the census, verified at authoring against `world/rules/status_display.py`) and fails closed on drift: delete `earth_root`; add rows for `earth_fissure` (裂縫), `earth_fissure_quake` (震盪裂縫), `earth_fissure_apex` (神格裂縫), `earth_bedrock` (磐石壁壘), `earth_carapace` (荊棘反甲) — FIVE new buff keys — per the table's label/severity conventions; re-homed codes keep their entries.
- Docs/traceability integration owned here: shard-manifest final state (`.github/evennia-shards.json` — this change owns the wave's last manifest edit); `earth.md` stays the authority and is untouched; `docs/development/adding-spells.md`'s SELF_ONLY example names `hardened_skin`/`self_buff_apply:earth_hardened_skin` — both SURVIVE the swap (root node, re-homed row with unchanged numbers), so the example is expected to stay valid; touched only if the shipped block drifts from it (record the check); `docs/lore/magic-system.md` §3's earth row checked for contradiction only (expected: none — it states the 地形與護 identity this tree implements); `tests/test_command_docs.py` untouched (no command-surface change).

This design.md additionally carries the wave-wide **interface-ownership matrix, batch order and shared verification contract** (light/water/dark pattern) for the whole earth wave.

## Capabilities

### New Capabilities
None.

### Modified Capabilities
- `skill-registry`: the duplicated ten-row 土-element data-contract requirement is REMOVED and replaced by one behavioral requirement — earth spell progression composes executable 地形與護 behavior (fixed-defense ladder, accuracy debuff, ground-fissure marker hazards whose standing-on-it fact prices a synergy strike, an unconditional execution rung, devastation rungs, an on-physical-hit counter via the reaction vocabulary, two-root lineage with branch points and a two-parent capstone) through the shared mechanics, with the old set — including the bind node — deleted without alias.

## Impact

`world/skills/registry.py` (earth block); `world/rules/rulebook/buffs.yaml` (re-homed/new earth rows), `world/rules/rulebook/state_reactions.yaml` (the one `physical_hit`/`counter_damage` rule) and `world/rules/rulebook/status_display.yaml` (display-metadata census sync); `world/skills/tests/test_spell_catalogs.py`, `world/skills/tests/test_cost_tiers.py`, `world/rules/tests/test_buffs.py` (echo retirement + re-home updates); `docs/development/adding-spells.md` (example-key re-point); new synthetic behavior module → `.github/evennia-shards.json`; traceability ledger hygiene at the separately authorized main-sync. `openspec list --json` returned an empty change set at authoring time — no serialization gate against active changes; the earth wave's internal ordering is declared below. One engineer-day.

## Batch

- depends-on: terrain-marker
  (code conflicts: `world/rules/rulebook/buffs.yaml` — this change authors the `earth_fissure`/`earth_fissure_apex` marker rows over that change's shipped clause, and the `buff:earth_fissure` predicate data in `registry.py` over its shipped seam; `.github/evennia-shards.json`)
- depends-on: on-hit-counter-damage
  (code conflicts: `world/rules/rulebook/state_reactions.yaml` — this change authors the one counter rule over that change's shipped event/then vocabulary; the reaction engine surface is frozen by it, this change only authors data; `.github/evennia-shards.json`)

Planning artifacts only this turn; no apply/archive/sync/merge.
