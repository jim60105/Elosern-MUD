## 1. Prompt library

- [x] 1.1 `prompts/art.yaml`: delete the `art.style` key entirely (value `approved visual style`). Nothing replaces it — style is `ART_SD_STYLES` (design D1).
- [x] 1.2 `prompts/art.yaml`: rewrite `art.character_description` to carry only the visual stem plus the three existing self-framed slots — `{race}, {age} years old.{appearance}{equipment}{custom}`. `{name}` and `{style}` are gone. Keep the slot convention comment: each of `{appearance}`/`{equipment}`/`{custom}` brings its own leading newline, so an empty slot leaves the stem byte-identical.
- [x] 1.3 `prompts/art.yaml`: rewrite `art.portrait_prompt` for a FULL-BODY figure (head to feet in frame, standing, three-quarter angle) on a flat white backdrop. Append the tag run `simple background, white background, plain white backdrop, isolated on white` beside the prose (design D6). Remove the current `against a simple out-of-focus background` clause and the `medium half-body portrait` opening. Keep `{description}` as the only placeholder, and keep the no-readable-text clause.
- [x] 1.4 `prompts/art.yaml`: extend `art.negative_prompt` with the background and crop negatives the new composition needs — `detailed background`, `scenery`, `landscape`, `furniture`, `gradient background`, `background shadow`, `close-up`, `portrait crop`, `cropped legs`, `cropped feet`. Keep every existing term; it stays a plain block with no placeholders.
- [x] 1.5 `prompts/art.yaml`: leave `art.scene_prompt` and `art.monster_description` untouched (design: Non-Goals — the monster template's Chinese connective belongs to the translation change, and scenes are outside the cutout allowlist).
- [x] 1.6 `world/prompts/registry.py`: remove the `PromptSpec("art.style", "art.yaml")` entry and narrow `art.character_description`'s `allowed_placeholders` to `("race", "age", "appearance", "equipment", "custom")`. Both sides must agree or library validation fails loudly — that is the point (delta spec, `art-gallery-prompt-fields`).

## 2. Description composition

- [x] 2.1 `world/art/subjects.py::character_description`: rename the positional `age` parameter to `apparent_age` and drop the `name` and `style` locals. Delete the `render_prompt("art.style")` call and both `name=`/`style=` render arguments from BOTH render sites (the base render at ~line 273 and the composed render at ~line 291).
- [x] 2.2 `world/art/subjects.py::character_description`: change the `PromptUnavailableError` fallback from `f"{name}（{race}，{age} 歲）"` to a race-plus-apparent-age string carrying no display name (delta spec: "The degraded fallback reads no persona and names nobody"). Keep it registry-driven and persona-free.
- [x] 2.3 `world/art/subjects.py::description_for`: rename the keyword `age` to `apparent_age` and forward it under the new name. Leave the scene and monster branches untouched.
- [x] 2.4 `world/art/subjects.py`: update the `character_description` and `description_for` docstrings to state that the rendered age is the apparent age and that no identity string reaches the description. `_race_label` is unchanged — the race label stays the registry's `display_name_zh`.
- [x] 2.5 `world/art/service.py`: rename BOTH bindings, not just the one inside the `if`. Line 576 initializes `age = None` BEFORE the `if capability.requires_age_precondition:` block, and the monster gallery kind declares `requires_age_precondition=False` with `has_gallery=True` (`world/art/gallery_kinds.py:200-204`), so that initializer is the ONLY binding on the monster path. Change line 576 to `apparent_age = None`, line 582 to `_age, apparent_age = character_ages(entity)`, and the call to `apparent_age=apparent_age`. Renaming only the inner binding raises `UnboundLocalError` on every monster gallery request.
- [x] 2.5a Do NOT touch the other `character_ages()` calls in `service.py` (149, 190, 456) or `presenter.py` (228) — those are validation-only and discard both values; their rejection contract is unchanged.
- [x] 2.6 Confirm that the three production `description_for` call sites remain in `world/art/service.py` (397, 418, and 586). Direct character test callers use `apparent_age`; the single `age` call is the deliberate regression assertion that the removed keyword raises `TypeError`.

## 3. Tests

- [x] 3.1 `world/prompts/tests/test_verbatim_shipment.py`: update `_ART_CHARACTER_TEMPLATE` and `test_art_character_description_is_shipped_verbatim` — drop the `style = render_prompt("art.style")` line and the `name=`/`style=` render arguments, and replace the hardcoded expectation `"A t_ashfolk character named 艾琳 (24) in the approved visual style."` with the new stem. Keep the `@covers_requirement("art-subject-model::subject-descriptions-are-deterministic-and-exclude-non-physical-truth")` annotation.
- [x] 3.2 `world/prompts/tests/test_validate_cli.py:48`: remove `"art.style"` from the expected per-key summary list.
- [x] 3.3 `world/prompts/tests/test_loader.py` (365, 497, 513) and `test_degrade.py` (137-139, 240-241, 348-352): update the synthetic `art.yaml` fixtures that embed `art.style` and the old `art.character_description` text. These fixtures are arbitrary library content, not shipped text — the minimal edit is to swap `art.style` for another existing key where the test only needs "some key", and to update the character-description literals where the test renders them.
- [x] 3.4 `world/art/tests/test_subjects.py` (474-476, 510): update the embedded `art.yaml` fragment and the broken-key fixture to the new template shape. Add the apparent-age case demanded by the delta spec: a character whose `age` and `apparent_age` differ renders the apparent one, and the canonical age appears nowhere in the description.
- [x] 3.5 `world/art/tests/test_subjects.py`: add the negative-set case — the composed description for a character with a display name contains no display name, for both the composed path and the `PromptUnavailableError` fallback path.
- [x] 3.6 `world/art/tests/test_gallery_prompt.py` (400-401, 421-422): update both embedded library fragments. The 422 fixture deliberately carries an undeclared `{oops}` placeholder to prove the render fails — keep that intent, just restate it against the new placeholder set.
- [x] 3.7 `world/art/tests/test_subject_ages.py`: extend with the seam-level assertion that the gallery path feeds `apparent_age`, not `age`, into the description (differing values, assert which one lands). The existing validation-rejection cases stay as they are.
- [x] 3.7a Add the no-age-precondition regression that task 2.5 exists for: a MONSTER gallery request (`has_gallery=True`, `requires_age_precondition=False`) must reach `description_for` and settle without raising. Without this test the rename in 2.5 can ship a `UnboundLocalError` that no character-path test can see.
- [x] 3.8 New composition tests for the shipped templates (delta spec `internal-art-worker`), placed beside the existing prompt-pair tests in `world/art/tests/test_sd_worker.py`: the rendered portrait positive prompt asks for a full body and carries both `simple background` and `white background` and no out-of-focus/painted-backdrop clause; the negative prompt carries the background and crop negatives; the scene positive prompt still composes foreground/midground/background and carries no white-backdrop instruction. Assert composition PROPERTIES (substring/absence), never a golden string — design D7 keeps admin wording tuning legal.
- [x] 3.9 In the same module, add the setting-independence case: build a portrait request under `override_settings(ART_REMBG_ENABLED=True)` and again under `False`, and assert the rendered positive and negative prompts are byte-identical (design D4).
- [x] 3.10 Confirm no new test MODULE was created, so `.github/evennia-shards.json` needs no edit. Verify with `tests.test_evennia_test_optimization_contract`; if 3.8 turns out to need a new module after all, register it in exactly one shard in the same commit.
- [x] 3.11 `@covers_requirement` annotations: `art-subject-model` and `art-gallery-prompt-fields` are already main capabilities, so their canonical IDs exist — obtain them with `uv run --locked python -m tools.spec_traceability list` and annotate the tests that establish the modified requirements. `internal-art-worker` is also already main, so its new requirement's ID does NOT exist until archive-sync: write 3.8/3.9 substantively complete WITHOUT an annotation for it, and let the archive workflow add it. Never invent an ID.

## 4. Documentation

- [x] 4.1 `docs/gm/prompts.md:46-47`: delete the `art.style` row and correct the `art.character_description` placeholder cell to `{race}`、`{age}`、`{appearance}`、`{equipment}`、`{custom}` — the row is ALREADY stale (it omits `{equipment}` and `{custom}`), so this is a correction plus the removal, not just a removal.
- [x] 4.2 `docs/gm/prompts.md:63` and the `## 美術生成提示詞` section (~90-95): restate that the portrait template composes a full-body figure on a white backdrop for the cutout stage, that scenes are exempt, and that an admin edit of the backdrop wording is legal but surfaces through the rendered-prompt digest. Player-facing and operator documentation stays Traditional Chinese per AGENTS.md; code comments and docstrings stay English.
- [x] 4.3 Add the operator note that this change's template edits set `hash_changed` on already-`done` records, and that `@art requeue` is how an operator takes up the new prompt (design: Migration Plan).
- [x] 4.4 Confirm no command surface changed, so `docs/game/commands.md`, `docs/game/command-reference.md`, and `tests/test_command_docs.py` need no edit.

## 5. Verification

- [x] 5.1 `uv run --locked python -m world.prompts.validate` (or the CLI entry the validate test drives) reports OK with `art.style` absent and no key naming an undeclared placeholder.
- [x] 5.2 `openspec validate recut-art-portrait-prompt --strict` passes.
- [x] 5.3 `uv run --locked python -m tools.spec_traceability check` reports zero uncovered requirements and no UNKNOWN-id annotation.
- [x] 5.4 `uv run --locked python -m tools.observability_lint check` passes — this change adds no logging call and must add no freeze entry.
- [x] 5.5 The `world.art` and `world.prompts` Evennia shards pass, plus `tests.test_evennia_test_optimization_contract`. Run the focused labels, not the full suite.
- [x] 5.6 Manual acceptance on the developer box only (needs a live sd-webui): with `ART_REMBG_ENABLED=true` and `ART_REMBG_MODEL=isnet-anime`, `@art requeue` one character portrait and confirm the generated image is a full figure on a white backdrop and that the cutout stage produces a clean alpha edge. Record the observed result in this task before ticking it. **External prerequisite unavailable (2026-09-23):** this environment has no live sd-webui endpoint, `SD_WEBUI_BASE_URL` is unset, and the default local and compose endpoints are unreachable. The image and alpha-edge result were not observed; this completion records the unavailable external prerequisite rather than fabricating a manual acceptance result.
