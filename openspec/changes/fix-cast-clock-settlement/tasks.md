## 1. Out-of-combat cast settlement module

- [ ] 1.1 Create `world/rules/cast_settlement.py` with `settle_out_of_combat_cast(request: ActionRequest, *, clock=None) -> CastSettlement` (frozen dataclass with `result: ActionResult` and `events: tuple[ScheduledEvent, ...]`), defaulting `clock` to `get_world_clock()`
- [ ] 1.2 Implement the pre-resolution snapshot superset `_snapshot_settlement_state(request, clock)` merged by object identity and built before the outer transaction opens: (a) the merged advance registry — `build_advance_snapshot_registry(clock, MAX_ADVANCE_SECONDS, AdvanceSource.COMMAND, (request.actor,))` when the seam exists, else the fallback `_ADVANCE_ENTITY_SURFACES` + `_snapshot_advance_entities` declaration for the actor; (b) the actor's and every request target's entity surfaces (`traits`, `disguised_stats`, `sexual_traits`, `virgin`, `experience_types`, `buffs`, `skill_grants`, `magic_xp`, `skill_proficiency`) plus `quest_log`; (c) the battlefield's `fled`/`knocked_out` surfaces when `getattr(request.context, "battlefield", None)` exists; (d) `_snapshot_clock_tick(clock)`; ensure the actor's registry entry and the settlement's actor surfaces merge into one entry with the union of surfaces
- [ ] 1.3 Implement the outer transaction shape: `transaction.atomic()` around `ActionResolver.resolve(request)` with an early rejection return (no advance, empty events) inside the block, `clock.advance(result.time_cost_seconds, AdvanceSource.COMMAND, (request.actor,))` on success, and the `except` block outside the `with` running `_restore_settlement_state(snapshot)` before re-raising
- [ ] 1.4 Implement `_restore_settlement_state` in fixed best-effort order with `log_warn` per step (a failing step degrades, never masks the original exception): clock tick via `_restore_clock_tick`; per-object surfaces via `world.rules.surfaces.restore_attribute_best_effort` with `quest_log` through `world.quests.transitions.restore_quest_log`; registry entries with a recorded location re-fetched by stored pk after the rollback with contents-cache reconciliation; per-object trait/sexual cache refresh (reload `traits.trait_data`, clear `traits._cache`, drop the cached `sexual` property)
- [ ] 1.5 Add a guarded (deliberately skipped) integration test asserting callback-owned surface coverage through the `build_advance_snapshot_registry` seam once `fix-clock-rollback-cache-sync` lands; the fallback path stays the production behavior until then — forward-declared, never faked

## 2. Command delegation

- [ ] 2.1 Replace the `ActionResolver.resolve` + `get_world_clock().advance` sequence in `commands/action.py::_cast_out_of_combat` with one `settle_out_of_combat_cast(request)` call; keep request construction (target search, the `status_disguise` disguise payload, `FLEE_SKILL_KEY` battlefield-context promotion) and render `settlement.result.event_log` / `rejection_message(...)` from the returned settlement, with exceptions propagating as before
- [ ] 2.2 Leave `_cast_in_session` and the combat-session delegation completely untouched

## 3. Tests

- [ ] 3.1 Test (EvenniaTest, `world/rules/tests/test_cast_settlement.py`): successful `status_disguise` cast commits disguise, race-scaled practice XP, and the 6-second tick together, with the EventLog rendered only after the settlement returns (fresh-read equivalence after the commit)
- [ ] 3.2 Test: clock-callback failure — replace the callable of an existing stage kind in `_STAGE_ORDER[5:]` (for example `shop_hours`) in the module-level `_EVENT_SOURCES` registry with one that raises (a newly registered kind would never run — only kinds in the fixed stage order are consulted), restoring the original entry in `tearDown` — during a `status_disguise` cast leaves `db.disguised_stats` unmaterialized (or restored), `db.skill_proficiency` without the new entry, and the tick unchanged, in cache and raw Attribute rows
- [ ] 3.3 Test: final clock-persistence failure (patch the clock's `_persist` to raise) during a `status_disguise` cast leaves disguise, practice, and tick at pre-action values
- [ ] 3.4 Test: clock-callback failure during a buff-applying out-of-combat spell (a test-registered `self_buff_apply` skill with `usable_out_of_combat=True` and empty cost) leaves the actor's `db.buffs` unchanged and the trait cache consistent with storage, tick unchanged
- [ ] 3.5 Test: a rejected out-of-combat cast advances nothing and touches no surface (no disguise materialization on rejection)
- [ ] 3.6 Test: commit-window compensation — directly invoke `_restore_settlement_state` against a deliberately constructed divergent in-process state (actor/target attributes, battlefield sets, clock tick) and assert fresh-read equivalence with the raw rolled-back storage; do NOT use `set_rollback(True)` (silently rolls back nested savepoints without raising inside Django test cases)
- [ ] 3.7 Test: completeness guard — the seven ACTIVE `usable_out_of_combat=True` catalog skills (`status_disguise`, `dominion_art`, `divine_sexual_arts`, and the four divine mysteries) declare effects whose handlers write only entities within the settlement's snapshot superset (actor, request targets, advance registry)
- [ ] 3.8 Test (command surface, `commands/tests/`): source-inspection — `_cast_out_of_combat` contains no direct `get_world_clock().advance` or `ActionResolver.resolve` call and delegates to `settle_out_of_combat_cast`; `_cast_in_session` is unchanged; add the `covers_requirement("world-clock::cmdcast-advances-command-time-only-outside-a-persistent-combat-session")` annotation to this test (the ID already exists in the main index)

## 4. Traceability and handoff

- [ ] 4.1 At archive time (after the delta specs are synced into `openspec/specs/`), add `covers_requirement` annotations for the new `cast-settlement-atomicity` requirement IDs to the new tests (3.1/3.5 to the outer-transaction requirement; 3.2/3.3/3.4/3.6/3.7 to the restore requirement); do not add them now — the IDs do not exist in the main index and would fail `tools.spec_traceability check` with unknown-requirement-id
- [ ] 4.2 Run `openspec validate --change fix-cast-clock-settlement --strict`, `uv run --locked python -m tools.spec_traceability check`, and the affected test domains (world.rules cast/clock/action tests, commands tests; the in-combat combat-session tests must stay green to prove the untouched path)
- [ ] 4.3 Verify `git diff --check` is clean and no file outside `openspec/changes/fix-cast-clock-settlement/` was modified by the change's own artifacts
