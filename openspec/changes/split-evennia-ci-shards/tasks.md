# Tasks: Split the Evennia Suite into Machine Shards

## 1. Manifest and ownership contract

- [x] 1.1 Create `.github/evennia-shards.json` with six shards: three
      `world.rules` file groups generated by sorting the 112 `test_*.py`
      modules under `world/rules/tests` and splitting into thirds, plus
      `world.quests world.skills world.art world.ai world.onboarding
      world.lore`,
      `world.maps web.webclient world.imports world.prompts world.tests`,
      and
      `commands server typeclasses` (the three non-rules groups reflect the
      measured rebalance in task 5.2); each shard has unique
      sorted `index`, `name`, and non-empty `labels`
- [x] 1.2 Add `test_evennia_shard_manifest_owns_every_non_browser_test_module_exactly_once`
      to `tests/test_evennia_test_optimization_contract.py`: enumerate
      `test_*.py` modules under the five roots recursively, resolve each
      manifest label to a module file (`label.replace(".", "/") + ".py")` or
      walk a package directory for `test_*.py` files, assert the labeled set
      equals the discovered set with no cross-shard overlap, indices unique
      and sorted, and every label resolving to at least one module; annotate
      with `covers_requirement("evennia-test-optimization::machine-shards-preserve-exact-per-module-test-ownership")`
      (verify the literal ID with `uv run --locked python -m
      tools.spec_traceability list` after the delta spec is written)

## 2. Workflow: preflight output and evennia matrix job

- [x] 2.1 Add a "Validate execution shard manifests" preflight step (reject a
      manifest with no shards, non-sorted or duplicate indices, or a shard
      without non-empty string labels, so a valid-but-empty manifest cannot
      skip the whole gate) and a "Compute evennia shard matrix" preflight step
      reading `.github/evennia-shards.json` into output `evennia-shards`
      (`fromJSON`-compatible, mirroring the browser step)
- [x] 2.2 Convert the `evennia` job into a matrix job
      (`fail-fast: false`, `include: fromJSON(needs.preflight.outputs.evennia-shards)`)
      where each shard runs
      `coverage run --concurrency=multiprocessing --parallel-mode -m evennia
      test --settings test_settings.py --noinput --parallel 4
      ${{ join(matrix.labels, ' ') }}` with
      `COVERAGE_FILE=coverage-evennia-shard-${{ matrix.index }}`,
      `MUD_TEST_SETTINGS=1`, and
      `OPENSPEC_TEST_EVIDENCE=evidence.evennia-shard-${{ matrix.index }}.jsonl`;
      upload `coverage-evennia-shard-${{ matrix.index }}*` and the evidence
      file as `evennia-shard-${{ matrix.index }}-artifacts` with
      `if-no-files-found: error`

## 3. Gate aggregation updates

- [x] 3.1 Replace the single-evennia completeness checks in the gate's
      "Validate artifact completeness" step with a loop over the evennia
      manifest indices, checking each
      `coverage-evennia-shard-<i>*` sidecar and
      `evidence.evennia-shard-<i>.jsonl` are present and non-empty (fail with
      a diagnostic otherwise)
- [x] 3.2 Change evidence concatenation to
      `cat evidence.evennia-shard-*.jsonl evidence.browser-shard-*.jsonl
      evidence.top-level.jsonl > evidence.combined.jsonl`
- [x] 3.3 Confirm `coverage combine coverage-evennia* coverage-browser-shard-*
      coverage-top-level*` still matches every sidecar (no change expected)

## 4. Contract tests for the workflow structure

- [x] 4.1 `tests/test_quality_gate_contract.py`: update the evennia job
      assertions to the matrix shape (preflight `evennia-shards` output,
      `fail-fast: false`, shard step name, `--parallel 4`,
      `--concurrency=multiprocessing --parallel-mode`, `join(matrix.labels,
      ' ')`, `MUD_TEST_SETTINGS=1`, no broad-`.` discovery) and the coverage
      env assertion to `coverage-evennia-shard-${{ matrix.index }}`; keep all
      existing `covers_requirement` annotations on the same methods
- [x] 4.2 `tests/test_evennia_test_optimization_contract.py`: update the
      evennia step assertions (COVERAGE_FILE shard expression, labels
      expression, no `web.tests.browser` labels, gate `needs` list unchanged)
- [x] 4.3 `tests/test_browser_verification_contract.py`: no semantic change
      expected; run it to confirm nothing regressed

## 5. Balance measurement and rebalance

- [x] 5.1 Measure each shard's serial wall time locally
      (`MUD_TEST_SETTINGS=1 uv run --locked evennia test --settings
      test_settings.py --noinput --keepdb <shard labels>`), warm the retained
      DB once first
- [x] 5.2 If `max/mean > 1.35`, move whole files/packages between shards in
      the manifest and re-measure the touched shards once; keep each
      `world.rules` file in exactly one `rules-*` shard

## 6. Documentation and CI validation

- [x] 6.1 Update `AGENTS.md` (CI structure, `.github/evennia-shards.json`,
      the CI-only shard warning: never run shard invocations concurrently on
      one machine because they share `server/db/evennia-test.sqlite3`),
      `docs/development/evennia-testing-guide.md` (CI profile), and
      `docs/development/evennia-test-performance.md` (new dated section with
      the before/after per-job durations)
- [x] 6.2 Run `uv run --locked -m unittest discover -s tests -t .`,
      `MUD_TEST_SETTINGS=1 uv run --locked evennia test --settings
      test_settings.py --noinput --parallel 16 commands server typeclasses
      world web.webclient`, `uv run --locked python -m tools.spec_traceability
      check`, and `openspec validate split-evennia-ci-shards --strict`
- [ ] 6.3 Push the branch, watch the quality gate, record every job's
      duration, rebalance the manifest once if a shard dominates (≥ 2× the
      median), and record final numbers in the performance report

## 7. Final handoff

- [ ] 7.1 Sync the delta spec into
      `openspec/specs/evennia-test-optimization/spec.md`, archive the change,
      run `openspec validate --all --strict`, and confirm `git diff --check`
      is clean
