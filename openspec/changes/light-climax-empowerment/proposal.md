## Why

The emergency heal needs actor-bound phase progression, while godhood grants a cycle-scoped state benefit. Reuse canonical transition edges and declarative transition reactions without bypassing the existing action lock.

## What Changes

- Add typed peak effect and actor-bound staging through canonical pleasure gains without changing divine_pleasure_max target semantics.
- Add phase-triggered marker application/removal using the existing condition evaluator, a narrow qualification primitive and the canonical phase-change chokepoint.
- Connect configured marker maxima to the reusable state magnitude evaluator and restore reaction-touched buffs in every action/clock/outer settlement rollback.
- Verify the existing turn lock and afterglow resumption through actual ActionResolver/clock behavior; document no-retroactive entitlement.
- **BREAKING** where authored policy or removed light data replaces old behavior; no compatibility aliases, migrations, or interim fake effects.
- Verification uses synthetic behavior tests and targeted runtime scenarios, never light catalog key/count/value contracts.

## Capabilities

### New Capabilities
- `phase-scoped-spell-empowerment`: The emergency heal needs actor-bound phase progression, while godhood grants a cycle-scoped state benefit. Reuse canonical transition edges and declarative transition reactions without bypassing the existing action lock.

### Modified Capabilities
- `skill-effect-model`: The executable contract changes as specified in this proposal's delta.

## Impact

Implementation surfaces: `world/skills/effects.py`, `world/rules/action.py`, `world/rules/sexual_state.py`, `world/rules/sexual_transitions.py`, `world/rules/state_reactions.py`, `world/rules/rulebook/state_reactions.yaml`, `world/rules/rulebook/schema.py`, `world/rules/surfaces.py`.

Dependencies: `light-effect-routing`, `light-sacrament-casting`. Estimated bounded implementation: 7 engineer-hours including focused tests and integration, no project-wide suite. This is one independently reviewable workday slice; it does not ship incomplete catalog entries.

This turn creates planning artifacts and authorized lore corrections only. Do not apply, archive, sync main specs, create feature branches or merge until the user requests it. The complete dependency/conflict schedule and shared interface ownership are in `../light-spell-catalog/design.md`.
