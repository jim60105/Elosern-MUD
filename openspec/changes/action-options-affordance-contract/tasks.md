## 1. Shared node-ID encoder

- [ ] 1.1 Create `web/webclient/actions/node_ids.py` with the pure function `node_id_for_location(location)` reproducing exactly the current `_current_node` logic in `web/webclient/actions/exploration_actions.py` (GridRoom `encode_grid(str(z), x, y)`, TerrainRoom `encode_wild(WILDERNESS_NAME, ...)`, ordinary room `encode_room(int(location.pk))`, `None` when location/coordinates are absent).
- [ ] 1.2 Replace `_current_node(actor)` with a thin actor-bound wrapper in `exploration_actions.py` calling `node_id_for_location`; update `_move_adapter`'s `stale_location` compare to use it; remove the old encoder body.
- [ ] 1.3 Add unit tests in `web/webclient/actions/tests/`: byte-identical node IDs for GridRoom, TerrainRoom, and ordinary-room fixtures, and `None` for a locationless actor; assert the move adapter's `stale_location` rejection fires when the encoder would differ.

## 2. Canonical affordance vocabulary

- [ ] 2.1 Create `web/webclient/presentation/affordances.py` with the frozen discriminated `AffordanceView`: action entries (`action_id`, `label`, `params`, `freeform`, `navigation: false`, `enabled`, `disabled_reason`) and navigation entries (`surface` `"guild"`/`"shop"`, `label`, `navigation: true`, `enabled`, `disabled_reason`, no `action_id`/`params`). Constants: `ACTION_CODE_ALLOWLIST` (exactly `explore.move`, `explore.look`, `explore.talk_scripted`, `explore.talk_freeform`, `explore.party_invite`, `explore.party_leave`, `explore.engage`, `explore.wait` — no `explore.interact`), `SUGGESTIBLE_ACTION_IDS` (the set minus `party_invite`/`party_leave`), `MAX_CARDS = 5`.
- [ ] 2.2 Implement the candidate builders by extracting the rule logic from `exploration.py` (`_scripted_affordance`, `_freeform_affordance`, `_party_invite_affordance`, `_party_leave_affordance`, `_engage_affordance`, `_service_affordance`, `_interact_targets`, `_scripted_keywords`, `_move_rows`, `_exit_ref`, `_traversable`, `_destination_node`, `_entity_kind`, `_bounded_display_name`, `_resolve_single_host`): each produces `AffordanceView` entries with the **same eligibility gates and disabled semantics as v1 today** — no new schedule gate in the vocabulary, freeform always enabled for `LLMNPC`s, dead monsters as disabled engage entries with `target_dead`, party-bound/full-party rules, companion rule, exact-local-host navigation entries.
- [ ] 2.3 Wire-shape normalization: for every non-freeform action entry, build the candid payload and pass it through the action's registered validator from `exploration_actions.py` (`validate_move_payload`, `validate_look_payload`, `validate_talk_scripted_payload`, `validate_party_invite_payload` (empty message), `validate_party_leave_payload`, `validate_engage_payload`, `validate_wait_payload`), storing the normalized output in `params`; move uses `node_id_for_location(actor.location)` for `current_node`. The freeform entry uses `params={"npc_id": int}` binding-only, never normalized (single documented exception).
- [ ] 2.4 Idle baseline: emit the `explore.look` room entry (`{"room": true}`) whenever the actor is a puppeted player inside a location (exploration mode); emit the `explore.wait` entry (`{"daypart": "noon"}`, fixed legal value from `DAYPARTS`) only when `unsafe_rejection(actor)` is absent. Export the vocabulary collector `exploration_affordances(actor)` returning the ordered `AffordanceView` tuple (move rows, then per-target entries, then baseline — vocabulary order).
- [ ] 2.5 Implement `suggestible_candidates(affordances)`: action entries with `enabled`, code in `SUGGESTIBLE_ACTION_IDS`, not blocked by `interaction_reason(npc, "talk")`, and not an unsafe-room wait.
- [ ] 2.6 Add pure `unittest` fixtures in `web/webclient/presentation/tests/`: empty room, exits room, schedule-blocked host, monster (living/dead/in-session), quest-objective NPC, multi-LLM-NPC, guild/shop hosts; assert eligibility, v1-identical disabled semantics, `ACTION_CODE_ALLOWLIST`/`surface` membership, validator-normalized params (including freeform binding-only), and that suggestible filtering excludes blocked/disabled/party/navigation entries.

## 3. Exploration panel delegation (byte-stable)

- [ ] 3.1 Refactor `web/webclient/presentation/exploration.py` so `_interact_targets`, `_move_rows`, look-rows, and the affordance builders delegate to the shared vocabulary in `affordances.py`, serializing the shared candidates into the exact version-1 descriptor shapes (`kind: "action"`/`"navigate"`, `action_id`/`surface`, `label`, `enabled`, `disabled_reason`, plus per-target `keywords` and move-row fields). Keep `_in_exploration_mode`, bounds, identifiers, and the panel payload assembly unchanged.
- [ ] 3.2 Add a byte-stability test: for each rule-table fixture, the rendered v1 `exploration` payload equals the pre-refactor expected payload from `test_exploration_panel.py` fixtures (same dicts, same order); assert the idle-baseline entries never appear in the v1 payload.
- [ ] 3.3 Run `web/webclient/presentation/tests/test_exploration_panel.py` and verify all existing tests pass unchanged (behavior preservation proof).

## 4. context_actions v4 server validator and presenter

- [ ] 4.1 In `web/webclient/presentation/combat_panel.py`, bump `CONTEXT_ACTIONS_SCHEMA_VERSION` to `4` and dispatch `validate_context_actions` on `kind`: the `combat` branch keeps the exact v3 field set, validation, and normalized output; add a new exploration branch accepting exactly `schema_version (4)`, `available: true`, `kind: "exploration"`, and `affordances` (list of at most `MAX_CONTEXT_AFFORDANCES` (320) discriminated `AffordanceView` entries validated against `ACTION_CODE_ALLOWLIST`/`surface` set, shape bounds, exact booleans, params bounds, and disabled-reason shape).
- [ ] 4.2 Add the exploration-presenter path: `context_actions_presenter` switches on canonical mode — active combat session → combat form (unchanged serialization), exploration mode → exploration available form from `affordances.py` in vocabulary order, creation-pending/absent location → `PanelUnavailableError`; the v1 `exploration` panel presenter keeps raising only outside exploration mode.
- [ ] 4.3 Add tests in `web/webclient/presentation/tests/test_combat_panel.py` (or a sibling): combat fixtures pass byte-identically at schema 4 (keep a v3-vs-v4 comparison fixture); exploration fixtures validate; a maximal-legal-room fixture serializes untruncated and passes both validators; cross-form contamination (combat fields in exploration form and vice versa) rejects; unavailable form differs only in `schema_version` (4 vs 3); read-only via the existing deterministic-path assertion.
- [ ] 4.4 Sweep and update every hard-coded `context_actions` schema-version-3 fixture and expectation across `web/webclient/presentation/tests/`, `web/webclient/actions/tests/`, and any browser fixture that asserts `schema_version === 3` or `PANEL_ALLOWLIST.context_actions === 3`; update `combat_menu.js` consumers if they branch on the version.

## 5. default_cards() degradation derivation

- [ ] 5.1 Implement `default_cards(affordances, *, objective_npc_ids=frozenset())` in `affordances.py` over `suggestible_candidates(affordances)`: rank objective-relevant first (params referencing a present NPC id in `objective_npc_ids`), then talk/engage over baseline, preserve vocabulary order within a rank, cap at 5, never empty in v1 (room-look baseline always suggestible).
- [ ] 5.2 Add tests: at-least-1/at-most-5 bounds, objective-first ordering, navigation/party/disabled/schedule-blocked exclusion, strict subset of the current union (same `action_id`, `params`, `label`) per fixture, purity (no state reads/mutations), and the empty-room baseline case.

## 6. Client mirror v4

- [ ] 6.1 In `web/static/webclient/js/elosern/protocol.js`: set `PANEL_ALLOWLIST.context_actions` to `4`; extend `validateContextActionsPanel` with the exploration available form (exact fields `schema_version`, `available`, `kind`, `affordances`; `kind === "exploration"`; per-entry bounds mirroring the server validator including the discriminated action/navigation shape) while keeping the combat form validation byte-identical.
- [ ] 6.2 Update every v3 fixture in `web/static/webclient/js/tests/protocol.test.js` (and `combat_menu.test.js` if it asserts the version) to v4; extend the dual-direction parity test with v4 fixtures for: combat available (byte-identical to v3 expectations via a comparison fixture), exploration available, and both unavailable forms (differing only in `schema_version`).
- [ ] 6.3 Run the Node suite (`node --test web/static/webclient/js/tests/protocol.test.js`) and the evidence bridge `web/webclient/tests/test_node_suite_evidence.py`; keep them green.

## 7. Cross-cutting verification

- [ ] 7.1 Annotate the new main-spec requirements after spec sync with `covers_requirement` from `tools.spec_traceability` on the owning tests; add the new main-spec requirement IDs via `openspec sync` per the slice workflow, then keep `uv run --locked python -m tools.spec_traceability check` green.
- [ ] 7.2 Run the affected package tests (`web.webclient.presentation`, `web.webclient.actions`, `web.webclient.tests`) plus the repository-wide `unittest discover -s tests -t .`; confirm no command-doc or typeclass build breaks.
- [ ] 7.3 Run `openspec validate action-options-affordance-contract --strict`; final `git diff --check` clean.