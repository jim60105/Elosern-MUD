## 1. Menu model

- [ ] 1.1 Confirm `make-shop-drawer-frameless` is applied and archived (its shop navigate row, frameless allow-set, and synced service-menus text exist); stop and report if not.
- [ ] 1.2 In `web/static/webclient/js/elosern/exploration_menu.js`: change the root `quests` item from `openServiceSubmenu: "quests"` to `openDrawer: "quest"` (availability gate unchanged), and make the `navigate` affordance branch emit `openDrawer` for both surfaces via one `{ guild: "quest", shop: "shop" }` map (no `openServiceSubmenu`), keeping each row's key, label fallback, `enabled`, `description: null`, and `disabledReason`. Update the header comment. Verify with 1.3.
- [ ] 1.3 Update `web/static/webclient/js/tests/exploration_menu.test.js` and `hud_dock_menus.test.js`: the root `quests` item and the `service-guild` row carry `openDrawer: "quest"`, no exploration menu item carries `openServiceSubmenu`, and disabled navigate rows keep their reason. Run `node --test web/static/webclient/js/tests/*.test.js` green.

## 2. Store wiring

- [ ] 2.1 In `web/webclient-app/stores/elosern/interaction.js`, add `quest` to the frameless drawer allow-set so both openers call `openHudDrawer("quest")` only. In `stores/elosern/hud.js`, add `"quest"` to `FRAMELESS_DRAWER_NAMES` and update the comment. Leave the `openServiceSubmenu` branch, `serviceSurface`, `SERVICE_SURFACE_*`, and the settle-time hosting block untouched (design D3). Verify with 4.1–4.2.

## 3. Remove the drawer hosting render path

- [ ] 3.1 In `web/webclient-app/AppClient.vue`: delete the drawer-body `<DockMenu v-if="drawerHostsServiceFrame" …>` block and its comment, the `:body-class` binding on `<HudDrawer>`, the `&& !drawerHostsServiceFrame` clause on the dock's `DockMenu` `v-if`, the `drawerHostsServiceFrame` destructure, the `.hud-drawer__body--dock` CSS rules and comment, and the `--dock` sentence in the `.quest-drawer` CSS comment. Verify with 4.3 and `pnpm run build`.
- [ ] 3.2 In `web/webclient-app/composables/use-dock.js`, delete `drawerHostsServiceFrame` and its return entry; in `web/webclient-app/components/HudDrawer.vue`, delete the `bodyClass` prop, its comment, and its class binding. `grep -rn "drawerHostsServiceFrame\|hud-drawer__body--dock\|bodyClass\|body-class" web/webclient-app --include='*.vue' --include='*.js'` (excluding `dist/`) returns nothing.

## 4. Vitest

- [ ] 4.1 Store tests (`web/webclient-app/tests/store/frameless_bag.test.js`, `declarative_surfaces.test.js`, `store/hud_drawer.test.js`): activating the `service-guild` row opens the quest drawer with frame stack, depth, current descriptor, `activeSubDock` (`null`), recorded surface, and breadcrumb unchanged; `tabToRootAndConfirm("quests")` from depth > 1 pops to the root and then opens the quest drawer with depth 1 and nothing pushed; closing the quest drawer by the store close entry pops nothing. Replace every remaining "guild activation pushes and hosts" regression with the frameless assertion. Run the focused files (`pnpm exec vitest run <files>`, repository root) green.
- [ ] 4.2 Close-path and focus tests (`web/webclient-app/tests/app_client_drawers.test.js`, `tests/hud_drawer.test.js`): Escape, close control, and scrim each close the quest drawer without a pop or dispatch and restore focus to the opener; `HudDrawer` renders without a `bodyClass` prop and never applies a `--dock` modifier.
- [ ] 4.3 Composition test (`web/webclient-app/tests/app_client_frameless_bag.test.js` or the shop change's frameless composition file): for each of the skill, inventory, shop, quest, lore, status, and party drawers — including with a `services.guild` / `services.stock` descriptor forced current via `store.router.pushFrame` — no `[data-testid="dock-menu"]` or `[data-testid="dock-detail"]` exists inside `[data-testid="hud-drawer"]`, and the dock still renders exactly the router's current frame. Run `pnpm test` green.

## 5. Traceability and managed browser journeys (browser suite CI-owned; not run locally)

- [ ] 5.1 Sync this change's deltas into the main specs (`webclient-contextual-hud`, `webclient-desktop-shell`, `webclient-exploration-menu`, `webclient-frame-resolution`, `webclient-service-menus`), then in the same step: re-anchor `web/webclient/tests/test_vue_hud_drawer_evidence.py`'s hosting test to the new "reference drawers present no router frame…" ID (Vitest marker text updated to the frameless assertion), remove every `webclient-frame-resolution::a-drawer-follows-the-stack-when-its-hosted-frame-pops` reference, re-anchor every `webclient-contextual-hud::the-bag-drawer-opens-without-a-router-frame-and-hosts-no-row-region` and `…::the-shop-drawer-opens-without-a-router-frame-and-hosts-no-row-region` annotation (`web/tests/browser/test_browser_inventory_grid.py`, `test_browser_services_shop.py`, and any evidence module the shop change added) to the generic ID, re-anchor the two `webclient-desktop-shell::the-dock-s-row-region-and-detail-panes-are-direct-children-of-their-host` annotations (`web/tests/browser/test_browser_combat_scales.py`, `test_browser_exploration_frame.py`) to the renamed action-dock requirement's ID, and read the new IDs from `uv run --locked python -m tools.spec_traceability list`.
- [ ] 5.2 Remap `web/tests/browser/test_browser_services_guild.py` per design D4: register, board accept, abandon (focus/cancel/Escape send nothing; confirm sends once), turn-in, exam, and the 1280x720 visibility journey use `guild-counter__*` / `quest-log__*` controls reached with the bounded Tab helper and Enter; the board-refresh journey asserts the counter's board rows and drops its resolver-table annotation; delete `test_quest_drawer_closes_with_the_hosted_frame` and add the "quest vanishes → book drops the row, drawer stays open" assertion; every journey asserts no `dock-menu` / `dock-detail` in the drawer. Annotate the frameless-drawer coverage with the new contextual-HUD ID and keep the service-browser-acceptance annotations.
- [ ] 5.3 In `web/tests/browser/test_browser_services_quest_drawer.py`, replace the `.dock-menu` direct-child layout assertion in `test_keyboard_service_journey_frames_render_inside_drawer` (rename it to describe the frameless contract) with "no `dock-menu` / `dock-detail` in the drawer and an unchanged router depth/trail across open and close". Update the `_wait_services_available` comment in `test_browser_services_base.py` (the quest drawer no longer hosts a frame).
- [ ] 5.4 In `web/tests/browser/test_browser_exploration_nav.py`, rewrite `test_escape_from_quests_service_submenu_leaves_root_clean` (rename to describe the drawer): the top-navigation 任務 click opens the quest drawer with `activeSubDock` still `null` and router depth 1; Escape closes the drawer, dispatches nothing, and leaves the dock root keys `["move", "look", "interact", "wait", "suggestions"]` with the router's focused item unchanged from before the open. Keep its exploration-menu annotation.

## 6. Validation

- [ ] 6.1 Run the Node gate, `pnpm test`, `pnpm run build`, `pnpm run build-storybook`, `pnpm run showcase-coverage` (repository root), `uv run --locked python -m tools.spec_traceability check`, and the Evennia shard containing `web.webclient.tests.test_vue_hud_drawer_evidence`; all green.
- [ ] 6.2 Run `openspec validate make-quest-drawer-frameless --strict` and `git diff --check`; both clean.
