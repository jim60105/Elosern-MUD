## Batch order (earth wave — integration contract in ./design.md D8)

0. Predecessors (ALL must be merged before this change starts; this change authors data only over their shipped vocabulary):
   1. `terrain-marker` (the `marker: ground` clause, the standing-on-it fact, battlefield-exit extinguishment, the `buff:<key>` predicate entry, `unconditional_defense_bypass`)
   2. `on-hit-counter-damage` (the `physical_hit` event + `counter_damage` then-action)
   Both are queued siblings; neither constrains the other (file-disjoint), this change is strictly after both.

## 1. Registry swap + rulebook data (data only)

- [ ] 1.1 Confirm both predecessor contracts (terrain-marker design D1–D3, on-hit-counter-damage design D2–D4) are merged and inspect current source/exported references (codegraph/LSP) for the earth registry block, the dev-era buff keys (`earth_hardened_skin`, `earth_stone_armor`, `earth_dust_veil`, `earth_root`, `earth_ward`), every dev-era registry key (incl. `earth_bind`), the `marker` clause loader, the `buff:` predicate validator, and the `physical_hit`/`counter_damage` reaction surface before editing; re-run the repo-wide census of `earth_bind`/`earth_root` references; if any mechanic from the D1 coverage table is missing, STOP and fix it in its owning behavior change — do not bolt code onto the catalog.
- [ ] 1.2 Author the 14-node tree exactly per design.md D1 (keys/labels/targets/MP costs/prerequisites/coefficient+policies/effect bindings), deleting the `earth_bind` row wholesale without alias and adding `bedrock_bastion`/`thorned_carapace`/`ground_fissure`/`fault_rupture`/`earthen_apotheosis`; verify the two-parent capstone (山嶽崩落 Lv.10 + 大地審判 Lv.10) validates at registry load through the existing lineage validator and tip caps derive unchanged; author the `earths_judgment` synergy exactly at D6's 1.15 rung with all three fissure keys in the predicate and `unconditional_defense_bypass=True`.
- [ ] 1.3 Author the earth rulebook rows per D1/D3: re-homes (`earth_stone_armor` unchanged values, `earth_ward` ceiling 5→12, `earth_dust_veil` duration 60→20), NEW `earth_bedrock` (defense +8/60), `earth_carapace` (empty-modifier self buff 60), `earth_fissure` (`marker: ground`, rate −12/10, 60), `earth_fissure_quake` (same clause/DoT, duration 40, D5), `earth_fissure_apex` (`marker: ground`, rate −40/10, 90); delete `earth_root`; the ONE reaction rule `thorned_carapace_counter` (`when: {event: physical_hit, buff_active: earth_carapace}`, `then: {counter_damage: 1.0}`) — every row ordinary vocabulary, no new grammar beyond the predecessors' shipped clauses; `ice_slow` untouched.
- [ ] 1.4 Sync `world/rules/rulebook/status_display.yaml` per D3 (delete `earth_root`; add `earth_fissure`/`earth_fissure_quake`/`earth_fissure_apex`/`earth_bedrock`/`earth_carapace` rows at the table's label/severity conventions) — the shipped import-time coverage check (`world/rules/status_display.py`) fails closed on any drift between the file and the live buff keys ∪ combat-modifier rule IDs; state-reaction rule ids are NOT in that census (verified at authoring) — the counter rule id needs no row.
- [ ] 1.5 Buff-key census: no dangling `buff_apply:`/`self_buff_apply:` target; `earth_root` gone from buffs.yaml, registry and status_display.yaml with no alias; `world/rules/tests/test_buffs.py`'s five dev-era earth numeric pins stripped to load/apply/presence assertions per D7 (behavior kept, NO re-pinned durations/ceilings, `earth_root`'s test deleted with its row, no row mirroring added); `world/rules/tests/test_status_display.py` green (set equality against the post-change displayable set).

## 2. Echo-test retirement and docs hygiene

- [ ] 2.1 Remove the earth `EARTH_SPELL_CATALOG` table and its test class from `world/skills/tests/test_spell_catalogs.py` and the earth rows from the tier-correspondence table in `world/skills/tests/test_cost_tiers.py`, in-file; adjust a `tools/test_data_lint.json` per-file entry only where its file stops naming shipped content as that linter's output decides; verify remaining suites' substantive coverage is untouched (no waivers).
- [ ] 2.2 Docs check under the node-data authority rule: `docs/lore/skill-trees/earth.md` untouched; `docs/lore/magic-system.md` §3 earth row edited ONLY if a real contradiction surfaces (none found at authoring — record the check); `docs/development/adding-spells.md`'s SELF_ONLY example names `hardened_skin`/`self_buff_apply:earth_hardened_skin` — both survive the swap (root node; re-homed row, unchanged numbers) — verified valid at authoring, no edit expected; `tests/test_command_docs.py` untouched (no command surface changes); the 2026-08-12 design doc is frozen history — never edited.

## 3. Behavioral evidence and integration

- [ ] 3.1 Disposable offline engine scenario exercising each distinct D1 composition through real casts (defense ladder read-back at authored values + expiry + ally-area audience; dust −5 at 20 s; fissure DoT + `ice_slow` co-application, expiry, and the flee sweep; earthquake's 40 s variant; synergy priced once on each fissure rung + off-marker silence + unconditional bypass vs high defense; devastation riders; carapace counter at 1.0 vs landed physical / magic / miss / tick; capstone's four-component settlement; two-root branch/convergence gates + two-parent capstone through the lineage engine; deleted `earth_bind`/`earth_root` rejecting as unknown): observe HP/stats/buffs/marker facts/counters/practice and delete only after full proof.
- [ ] 3.2 Synthetic behavior tests for settlement paths not pinned by the sibling suites in a new module `world/rules/tests/test_earth_terrain_guard.py` (marker+synergy+unconditional-bypass composed settlement incl. parallel-rung any-match-once; carapace counter + nonlethal floor + magic/miss silence; family prerequisite/capstone gates on earth-shaped synthetic data). No catalog-row equality, key-set, cost/tier table or skill-tree-table echo assertions anywhere (ratified NON-GOAL).
- [ ] 3.3 Finalize `.github/evennia-shards.json` (this change owns the wave's last manifest edit): register new non-browser modules in exactly one shard; verify the ownership optimization contract test.
- [ ] 3.4 Run the focused labels below after editing stops; `tools.observability_lint check` in the same batch; `tools.test_data_lint check`; `tools.spec_traceability check`; `openspec validate earth-spell-catalog --strict`. Canonical IDs via `uv run --locked python -m tools.spec_traceability list` at the separately authorized main-sync; annotate exactly the tests establishing them (the removed 土-element ID leaves the ledger with its requirement).

### Focused invocation

```sh
uv run --locked evennia test --settings test_settings.py --keepdb world.skills.tests.test_spell_catalogs world.skills.tests.test_cost_tiers world.rules.tests.test_skill_lineage world.rules.tests.test_earth_terrain_guard world.rules.tests.test_buffs world.rules.tests.test_phase_spell_reactions world.rules.tests.test_damage_state_feedback world.rules.tests.test_status_display
uv run --locked python -m tools.observability_lint check
uv run --locked python -m tools.test_data_lint check
uv run --locked python -m tools.spec_traceability check
uv run --locked openspec validate earth-spell-catalog --strict
```

Pass `MUD_TEST_SETTINGS=1` through the tool environment, never a shell prefix. `world.rules.tests.test_earth_terrain_guard` is the intended synthetic settlement module owned by this change, not an existing-test claim (tasks 1.1 confirms the shipped reaction-test module name; adjust the label to the actual module holding reaction-rule validation, never claiming a non-existent file). No full local suite/browser/aggregate-coverage run; no command above ten minutes. No apply, archive, main-spec sync or merge in the proposal turn.
