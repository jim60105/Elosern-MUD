# Tasks: webclient-align-11-dialogue-ux

## 1. Server: exit seam + choice cap

- [ ] 1.1 In `web/webclient/actions/exploration_actions.py`, add `validate_dialogue_leave_payload` (exactly `npc_id`, positive int) and `_dialogue_leave_adapter`: re-read `live_dialogue_session(actor)`; no live session or `npc_id` mismatch → reject with stable code `dialogue_inactive` and a stable player line before any write; success → `clear_dialogue_session(actor)`, push the presentation update through the same seam the other clear seams use, deterministic success message (`你結束了對話。`). Register `explore.dialogue_leave` in `web/webclient/actions/registry.py` (validator + adapter, exploration family).
- [ ] 1.2 Update the `world/rules/dialogue.py` sole-writer docstring/comment to name the `explore.dialogue_leave` adapter success path as a clear seam (no logic change; `clear_dialogue_session` stays the sole writer).
- [ ] 1.3 In `web/webclient/presentation/dialogue.py`, set `DIALOGUE_MAX_CHOICES = 4` as a panel-owned literal (decouple from `MAX_SCRIPTED_KEYWORDS`, which stays 16); keep table-order truncation of `choices` to the first four; update the validator message text.
- [ ] 1.4 Mirror the cap in the UMD client protocol `web/static/webclient/js/elosern/protocol.js` (`DIALOGUE_MAX_CHOICES = 4`) and add the mirror's comment note that it tracks the panel-owned bound; keep the Node gate (`web/static/webclient/js/tests/protocol.test.js`) pinning the new bound/rejection message.
- [ ] 1.5 Add tests with `covers_requirement` annotations: adapter success clears the session and only the session (mode returns to `exploration` at the next commit); stale/mismatched `npc_id` rejects `dialogue_inactive` with zero writes; payload validator exactness; registry action-ID list includes `explore.dialogue_leave` (update `test_dispatcher.py` expected set); presenter truncates a five-keyword table to the first four; server validator rejects a fifth choice (`web/webclient/presentation/tests/test_dialogue_panel.py`, `web/webclient/actions/tests/test_exploration_actions.py`).
- [ ] 1.6 Run focused server tests + `uv run --locked python -m tools.spec_traceability check`; confirm the freeze list and shard manifest need no changes (no new test modules).

## 2. Client store: retire the dock dialogue form

- [ ] 2.1 In `web/webclient-app/stores/elosern.js`: delete `MODE_ROOT_DESCRIPTORS.dialogue` and the dialogue-first branch in `rootDescriptorFor` (dialogue mode re-homes to `exploration.root`); delete `dialogueFormPresented()` and the ArrowRight dialogue-borrow branch; rework the digit path: while mode is `dialogue` and `dialogueViewModel(panel("dialogue"))` yields ≥1 pick, digits `1`–4 activate caption picks through the same dispatch entry (`handleDialogueItem` kept as the pick dispatcher, keyed off the committed panel instead of the router descriptor); free/exit rows take no slot; otherwise digits keep dock semantics. Keep `borrowDialogueCommand` keyed off the committed panel.
- [ ] 2.2 In `web/webclient-app/stores/frame-resolvers.js`, delete the `dialogue.root` resolver (unregistered source → shared marker). Export/grep sweep: remove `DIALOGUE_TAB_KEY`/`DIALOGUE_TAB_LABEL` if unreferenced after the cut; keep `dialogueViewModel` + `DIALOGUE_FREE_ROW_KEY`.
- [ ] 2.3 In `AppClient.vue` and `AppShell.vue`: add the `dialogue-leave` emit path (`@dialogue-leave` → `store.dispatchAction("explore.dialogue_leave", { npc_id: vm.host.identity })`), drop the `dialogueForm` dock prop wiring and `dialogueRootForm`/`dialogueFramePresented` computeds; in `DockTabBar.vue` delete the `dialogueForm` prop and the dialogue legend branch (regular legend only).
- [ ] 2.4 Update the Vitest store/dock suites: replace the `dialogue.root` teardown/descriptor/digit/ArrowRight assertions in `web/webclient-app/tests/dialogue_store.test.js` with dialogue-mode→`exploration.root` teardown, caption-digit retarget (digits hit picks, not dock rows; free/exit rows unclaimed; no picks → dock fallback), and `dialogue-leave` dispatch; rewrite `dialogue_dock.test.js` into the regular-form assertions (ordinary tabs in dialogue mode, single regular legend, no `對話選項` tab).

## 3. Caption: head-out-of-viewport + one-surface dialogue variant

- [ ] 3.1 Restructure `NarrativeFeed.vue`: card root non-scrolling; `.narrative-head` static sibling; new `.narrative-scroll` region owns `overflow-y:auto`, `role="log"`, `data-testid="narrative-feed"`, tabindex, and the scroll-keep/unread/pin owners (behaviour moved verbatim); move the 32vh bound onto the scroll region.
- [ ] 3.2 In the dialogue variant's `.choices` unit: two-column pick grid (single column at the narrow fallback width), plus the trailing exit row (`✕` badge, `結束對話`, `data-testid="dialogue-exit"`, `class="pick pick-exit"`) after the freeform row, emitting `dialogue-leave`; exit/freeform rows take no digit badge.
- [ ] 3.3 Update `web/webclient-app/tests/dialogue_feed.test.js` and `narrative_feed.test.js`: exit row renders/emits once with picks+freeform, head-outside-viewport structural assertion (head is a sibling of the scroll region), scroll-keep/unread/jump regressions on the new element; keep the announce-once and suppression suites green.
- [ ] 3.4 Add `covers_requirement` annotations to the Vitest-visible Python-traceable requirements only where the gate applies (Python side already covered in 1.5); run `pnpm test` and the Node gate.

## 4. Integration, docs, and verification

- [ ] 4.1 Verify the full user path with the browser surface (managed-class budget, one class/file): open a scripted dialogue (no dock button press needed — the caption variant appears on the committed reply), pick a keyword from the caption, digits 1–4 pick caption rows while the dock keeps its ordinary tabs, `結束對話` returns the stage to exploration mode, moving away still clears the session.
- [ ] 4.2 Confirm `docs/game/commands.md` / `command-reference.md` need no change (no player text command surface moved) and `tests/test_command_docs.py` stays green.
- [ ] 4.3 Run focused Python suites (`web.webclient.actions`, `web.webclient.presentation`, `tests.test_evennia_test_optimization_contract` sanity), `pnpm test`, Node gate, `tools.spec_traceability check`, `git diff --check`.
- [ ] 4.4 `openspec validate webclient-align-11-dialogue-ux --strict`; reconcile any artifact drift discovered during implementation.
