# Tasks: align-openspec-cli-1-13-0

## 1. Authoritative version pin

- [ ] 1.1 Create `openspec/CLI_VERSION` containing exactly `1.13.0` plus a trailing newline, mirroring the `.python-version` pattern; verify with `printf '%s' "$(cat openspec/CLI_VERSION)" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$' && [ "$(openspec -V)" = "$(cat openspec/CLI_VERSION)" ]` on a machine with the pinned CLI.
- [ ] 1.2 Edit `.github/workflows/quality-gate.yml` "Install OpenSpec" step to `npm install --global "@fission-ai/openspec@$(<openspec/CLI_VERSION)"`; verify the step YAML parses (`uv run --locked python -c "import yaml; yaml.safe_load(open('.github/workflows/quality-gate.yml'))"`) and no other CLI version literal remains in the workflow.

## 2. Archive gate version assertion

- [ ] 2.1 In `scripts/openspec-gates.sh`, before the first gate, read `openspec/CLI_VERSION`, compare it with `openspec -V`, and exit non-zero printing both versions on mismatch; replace the `uv run --locked openspec validate …` calls with bare `openspec validate …` (keep `uv run --locked python -m tools.spec_traceability check` unchanged); verify with a PATH shim reporting a wrong version (script must fail naming both versions, running no validation) and with the real 1.13.0 CLI (script proceeds).

## 3. Drift-prevention contract test

- [ ] 3.1 Add `tests/test_openspec_cli_version_contract.py` (top-level `unittest discover -s tests` entry point; no `.github/evennia-shards.json` or `.github/browser-shards.json` change) asserting the three checks in the `openspec-cli-version-pinning` delta spec against committed files only, with `@covers_requirement` annotations using exactly: `openspec-cli-version-pinning::the-repository-declares-one-authoritative-openspec-cli-version`, `openspec-cli-version-pinning::the-archive-gate-fails-when-the-cli-version-does-not-match-the-pin`, `openspec-cli-version-pinning::a-committed-contract-test-detects-cli-pin-drift`. Scope the "no literal CLI version pin" assertion to the `Install OpenSpec` step's `run` text only — other steps legitimately contain `@vMAJOR.MINOR.PATCH` literals (e.g. `astral-sh/setup-uv@v10.0.0`); a whole-workflow regex false-positives. Expect the traceability annotations to report `unknown-requirement-id` until section 5.2(b) syncs the capability (expected and temporary; see 5.2); verify the test file itself with `uv run --locked python -m unittest tests.test_openspec_cli_version_contract -v` (all pass) and one temporary mutation of the workflow step proving the drift assertion actually fails.
- [ ] 3.2 Confirm the existing workflow contract tests still pass after the workflow edit: `uv run --locked python -m unittest tests.test_quality_gate_contract tests.test_browser_verification_contract -v`.

## 4. Purpose backfill (30 specs, Purpose-prose-only edits)

- [ ] 4.1 For each of the 10 art/import specs — `art-asset-lifecycle`, `art-gallery-kind-capabilities`, `art-queue-worker`, `art-stable-key-contract`, `art-staff-commands`, `art-subject-model`, `internal-art-worker`, `import-loader`, `import-reference-example`, `import-schema` — read the spec's requirement headings and text, then replace the `TBD - created by archiving change …` line under `## Purpose` in `openspec/specs/<capability>/spec.md` with one-to-three factual English sentences derived from those requirements; verify each file's diff touches only the Purpose section (`git diff` shows no `### Requirement:` line changed) and `openspec validate <capability> --type spec --strict` reports no WARNING for it.
- [ ] 4.2 For each of the 11 combat/action specs — `action-resolution-pipeline`, `battlefield-action-context`, `buff-handler-integration`, `combat-modifier-table`, `combat-resolution`, `damage-effect-handlers`, `dice-roller`, `effect-context-validation`, `overwhelm-threshold`, `single-shot-resolution`, `targeting-validation` — perform the same read-then-rewrite-and-verify as 4.1.
- [ ] 4.3 For each of the 9 remaining specs — `entity-sex-vocabulary`, `event-log`, `event-log-compression`, `rulebook-schema`, `sexual-transition-rulebook`, `sexual-vocabulary`, `skill-category-registry`, `webclient-frame-resolution`, `import-validation` — perform the same read-then-rewrite-and-verify as 4.1, except `entity-sex-vocabulary`: its `## Purpose` already carries real requirement-derived prose beneath a stale `TBD - created by syncing change entity-sex-field. Update Purpose after archive.` line — delete only the stale TBD line, keep the existing prose, and verify with `openspec validate entity-sex-vocabulary --type spec --strict` (no WARNING before/after diff).

## 5. Integration verification

- [ ] 5.1 Run `openspec validate --all --strict` under the pinned 1.13.0 CLI; verify `Totals: 225 passed, 0 failed` (or the then-current item count with zero failures) with no placeholder-Purpose warnings.
- [ ] 5.2 Run `uv run --locked python -m tools.spec_traceability check` twice: (a) on the feature tree before the new capability is synced into `openspec/specs/` — the ONLY reported errors may be the expected `unknown-requirement-id` entries for the three `openspec-cli-version-pinning::…` IDs, proving the 30 Purpose edits touched no requirement text or IDs; note that these errors make CI preflight red for this window — expected and temporary, do not chase; (b) as part of archiving, sync the capability into `openspec/specs/` carrying the delta's real `## Purpose` prose into the new main spec (a fresh `TBD` is forbidden — that habit created the 30 placeholders this change removes; "archive without syncing" is forbidden here because the archive gate's `spec_traceability check` exits 1 on the unsynced IDs), then re-run the check — verify a fully green report with the three new IDs covered by `tests/test_openspec_cli_version_contract.py`, and re-run `openspec validate --all --strict` (expect the then-current item count with zero failures, now including the new capability spec).
- [ ] 5.3 Run `scripts/openspec-gates.sh archive align-openspec-cli-1-13-0` end-to-end with the 1.13.0 CLI on PATH; verify exit 0, and repeat once with a wrong-version PATH shim to confirm the assertion path fails loudly.
