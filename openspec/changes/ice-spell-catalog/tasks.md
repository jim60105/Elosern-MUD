## Batch order (ice wave — integration contract in ./design.md D9)

0. Single-change wave: zero dependencies. Every consumed primitive is SHIPPED and already exercised live at master 0c3f3a27 (the four `buff_active → actions_per_turn: 0` lock rules consumed by `action.py` step 4 + `action_preview`; the `marker: ground` clause + standing-on-it fact + battlefield-exit extinguishment from terrain-marker, live via the earth fissure and fire lava rows; the `agility_flat` timed-ladder shape live via the wind rows; `bypass_defense`/`max_hp_fraction` rungs live at the water/dark/earth/fire capstones; the n-ary DAG + two-parent canopy validator live via the fire/wind canopies) — this change authors data only over their frozen grammar.

## 1. Registry swap + rulebook data (data only)

- [ ] 1.1 Confirm every predecessor contract is shipped and inspect current source/exported references (codegraph/LSP) for the dev-era ice registry block (`_elemental_spells("ice", …)`), the dev-era buff keys (`ice_slow`, `ice_wall`, `ice_freeze`, `ice_prison`), the shipped `BLOCKING_BUFF_KEYS = {"paralysis"}` ownership, the four live lock rules, the `marker: ground` loader clause (`buffs.py`), the earth fissure rows' `buff_apply:ice_slow` consumer edges, the individually-written `_spell` block shape (earth/fire/wind), and the shipped devastation rung form before editing; re-run the repo-wide census of ice keys (`player_presets.py`, quests, spawns, docs) if the audit at authoring missed a surface; if any mechanic from the D1 coverage table is missing, STOP and fix it in its owning behavior surface — do not bolt code onto the catalog.
- [ ] 1.2 Author the 13-node tree exactly per design.md D1 (keys/labels/descriptions/targets/MP costs AS-IS/prerequisites via `SkillPrerequisite(node, N)`/coefficients + per-node `effect_policies` per D8/effect bindings), deleting the dev-era `_elemental_spells("ice", …)` block wholesale without alias and adding the three new keys `frost_mire`/`crystal_shatter`/`eternal_frost_apotheosis`; verify at registry load through the shipped lineage validator: two roots, `ice_wall`/`ice_prison` each consumed by exactly two edges at 3/5, `eternal_ice_field` consumed ONLY by the canopy edge at 10 (no cross-route edge anywhere outside the canopy's two parent edges — D2), tip caps derive unchanged; author `absolute_zero` at exactly `DamagePolicy(bypass_defense=True, predicate=())`, the devastation rungs at `max_hp_fraction=0.10`, and the canopy's two prereqs on the shipped n-ary DAG; every new/re-authored ice ACTIVE skill declares `usable_out_of_combat=True` per the shipped deliberate-declaration policy (the frozen USABLE_OUT_OF_COMBAT_FALSE inventory stays exactly `{"flee"}`).
- [ ] 1.3 Author the ice rulebook rows per D1/D3/D6/D7 in `world/rules/rulebook/buffs.yaml`: re-home `ice_slow` (bounds illusion → empty-modifier mount, duration 60→15, `stacking: refresh`, `polarity: debuff` kept); re-home `ice_freeze` (30 s empty-modifier lock rung, shape unchanged); keep `ice_prison` (30 s, unchanged shape); DELETE `ice_wall`'s buff row wholesale (the off-tree `bounds: defense` buff — 固定防禦 is 土's verb); NEW rows `ice_wall_frost` (`marker: ground`, 60 s, debuff, empty modifiers), `ice_frost_mire` (`marker: ground`, 60 s, debuff, empty modifiers), `ice_freeze_tundra` (40 s), `ice_freeze_nightfall` (60 s, shared by `absolute_zero` + `eternal_ice_field`), `ice_freeze_apotheosis` (90 s). `world/rules/rulebook/state_reactions.yaml` MUST NOT be edited — zero ice reaction rows.
- [ ] 1.4 Author the SEVEN `combat_modifiers.yaml` ice rules per D3/D7, each its own row, appended beside the wind ladder with the wave's comment convention: `ice_freeze_locks_actions`, `ice_freeze_tundra_locks_actions`, `ice_freeze_nightfall_locks_actions`, `ice_freeze_apotheosis_locks_actions`, `ice_prison_locks_actions` (all `when {buff_active: <key>}` → `then {actions_per_turn: 0}`) + `ice_slow_agility_penalty` (`{agility_flat: -3}`) + `ice_frost_mire_agility_penalty` (`{agility_flat: -8}`). ZERO changes to the existing four lock rules (`paralysis_locks_actions` stays lightning's) and zero changes to `BLOCKING_BUFF_KEYS`/`blocks_action` (D3 — ice never enters lightning's key inventory).
- [ ] 1.5 Author the effect compositions per D8: `crystal_shatter`'s synergy at exactly `DamagePolicy(predicate=("buff:ice_freeze", "buff:ice_freeze_tundra", "buff:ice_freeze_nightfall", "buff:ice_freeze_apotheosis", "buff:ice_prison"), attack_multiplier=1.5)` (any-match-once, the `earths_judgment` precedent); the freeze/mire/wall riders as ordinary `buff_apply:` components with per-effect `EffectPolicy` tuples in effect order; `ice_wall` keeps its AS-IS 自/友 faction treatment (the shipped ally-target precedent), the harmful area rows at their shipped audiences.
- [ ] 1.6 Sync `world/rules/rulebook/status_display.yaml`: delete the `ice_wall` code row; keep `ice_slow`/`ice_freeze`/`ice_prison`; add five new buff rows (`ice_wall_frost`, `ice_frost_mire`, `ice_freeze_tundra`, `ice_freeze_nightfall`, `ice_freeze_apotheosis` — all harmful, labels per the table's 凍結/泥沼 naming convention) and seven new rule-id rows (five lock + two agility) at the table's label/severity conventions — the shipped import-time coverage check fails closed on any drift between the file and the live buff keys ∪ combat-modifier rule IDs.
- [ ] 1.7 Buff-key + registry-key census: no dangling `buff_apply:` target; `ice_wall` buff key gone from buffs.yaml/registry/status_display with no alias while the `ice_wall` registry NODE survives; `ice_slow` consumers (earth's three fissure rows) still resolve; `player_presets.py`/quests/spawns re-checked for retired-key references; `world/rules/tests/test_buffs.py`'s dev-era `ice_slow`/`ice_freeze` numeric pins stripped to load/apply/presence assertions per the ratified M4 discipline — the `assertFalse(blocks_action)` inertness pin on the real `ice_freeze` row is DELETED together with the synthetic-shape inertness assertions beside it (`t_prison_shape`/`t_wall_shape` build synthetic BuffDefinitions; the real lock behavior is asserted in the new synthetic module; NEVER re-pinned as inertness), no row mirroring added.

## 2. Echo-test retirement and docs hygiene

- [ ] 2.1 Remove the `ICE_SPELL_CATALOG` table and the echo methods of `IceSpellCatalogTests` (field-equality, effect round-trip, key-set equality) from `world/skills/tests/test_spell_catalogs.py`, and the `test_ice_spell_tier_labels_match_the_catalog` tier-label echo method from `world/skills/tests/test_cost_tiers.py`, in-file; `ElementalSpellsBuilderTests` (builder-shape mechanic, lightning) and `ClosedVocabularyParseTests` stay untouched; UPDATE (not retire) the shipped-content lineage contract: add `IceLineageTreeCatalogTests` in `test_spell_catalogs.py` in the fire `FireLineageTreeCatalogTests`/wind shape — expected-edge dict to the twelve-edge authored tree (two roots; `ice_wall`→{frost_mire 3, permafrost_domain 3}; `ice_prison`→{blizzard 5, crystal_shatter 5}; tundra→eternal_ice_field 8; blizzard→absolute_zero 8; canopy ←{eternal_ice_field 10, absolute_zero 10}; `eternal_ice_field` consumed only by the canopy), the lineage capability's registered-content convention, NOT a data echo.
- [ ] 2.2 Mechanical pins: update the `tests/test_rules_skills_test_layout_contract.py` class-home rows — the `IceSpellCatalogTests` retirement plus the new `IceLineageTreeCatalogTests` home and the new behavior-module home — per the wind-wave 7b66d14d precedent; the `tools/test_data_lint_seed.json` data-contract entries for `test_spell_catalogs.py`/`test_cost_tiers.py` already exist and stay — any further per-file adjustment is decided by `tools.test_data_lint check` output, never a guessed edit.
- [ ] 2.3 Docs check under the node-data authority rule: `docs/lore/skill-trees/ice.md` untouched (authority); `docs/lore/magic-system.md` §3's ice row edited ONLY if a real contradiction surfaces (none expected — it states the 物理性靜止＋靜止標記 identity this tree implements; record the check); `docs/development/adding-spells.md`'s `_elemental_spells` example checked for drift only (the builder survives for lightning — expected no edit, record the check); `tests/test_command_docs.py` untouched (no command-surface changes); the 2026-08-12 dev-era design archive is frozen history — never edited.

## 3. Behavioral evidence and integration

- [ ] 3.1 Disposable offline engine scenario before permanent tests, exercising each distinct D1 composition through real casts: slow −3 at 15 s on the real to-hit/flee/overwhelm consumers + full recovery on expiry; EACH freeze rung + 定身 resolving `actions_per_turn: 0` for exactly its authored duration (just-before/past-expiry boundary, adjacent entities unaffected, independently held/refreshed/expired, control-immunity honored exactly as the shipped lock consumers honor it); synergy priced once on a freeze holder, once on a 定身 holder, once (not twice) on a holder of two referenced facts, silent on a non-stilled target and on slow-mount/unrelated-buff holders; wall/mire footprint: occupying fact + first-rung slow mounted, sustained −8 ticking at its authored rung while held, battlefield-exit extinguishment with non-marker buffs persisting, expiry ending both facts; `absolute_zero` bypass vs high defense; devastation fraction on landed vs zero on missed; `ice_freeze_nightfall` shared refresh from either 主宰 caster; two-root gates + branch-point thresholds + two-parent canopy unlock through the lineage engine; retired `ice_wall`-buff / never-existing-key rejection paths.
- [ ] 3.2 Synthetic behavior tests for settlement paths not pinned by the sibling suites in a new module `world/rules/tests/test_ice_stillness_behavior.py` mirroring the ADDED requirement's scenarios (lock-rung settlement incl. expiry/refresh independence; synergy any-match-once across both lock families incl. slow-mount silence; footprint mount + exit sweep + expiry; execution/devastation rungs; two-root/two-parent gates on ice-shaped synthetic data). No catalog-row equality, key-set, cost/tier table or skill-tree-table echo assertions anywhere (ratified NON-GOAL).
- [ ] 3.3 Finalize `.github/evennia-shards.json` (this change owns the wave's only manifest edit): register `world.rules.tests.test_ice_stillness_behavior` in exactly one shard (rules-a, beside `test_fire_burn_immolation`/`test_earth_terrain_guard`); verify the ownership-contract test.
- [ ] 3.4 Run the focused labels below after editing stops; `tools.observability_lint check` in the same batch; `tools.test_data_lint check`; `tools.spec_traceability check`; `openspec validate ice-spell-catalog --strict`. Canonical IDs via `uv run --locked python -m tools.spec_traceability list` at the separately authorized main-sync; annotate exactly the tests establishing them (the removed 冰-element ID `skill-registry::skill-registry-contains-the-full-冰-element-spell-set` leaves the ledger with its requirement; the new ID `skill-registry::ice-spell-progression-composes-executable-physical-stillness-behavior` is annotated on `test_ice_stillness_behavior`; `skill-lineage::the-ice-lineage-ships-as-the-authored-two-root-branching-tree-with-a-two-parent-canopy` on `IceLineageTreeCatalogTests`; defer-annotation-to-main-sync if either reads red before the sync).

### Focused invocation

```sh
uv run --locked evennia test --settings test_settings.py --keepdb world.skills.tests.test_spell_catalogs world.skills.tests.test_cost_tiers world.skills.tests.test_skill_registry world.rules.tests.test_skill_lineage world.rules.tests.test_buffs world.rules.tests.test_status_display world.rules.tests.test_combat_modifiers world.rules.tests.test_ice_stillness_behavior world.rules.tests.test_earth_terrain_guard world.rules.tests.test_fire_burn_immolation world.rules.tests.test_freeform_casting
uv run --locked python -m tools.observability_lint check
uv run --locked python -m tools.test_data_lint check
uv run --locked python -m tools.spec_traceability check
uv run --locked openspec validate ice-spell-catalog --strict
```

Pass `MUD_TEST_SETTINGS=1` through the tool environment, never a shell prefix. `world.rules.tests.test_ice_stillness_behavior` is the intended synthetic settlement module owned by this change, not an existing-test claim. `test_earth_terrain_guard` is in the batch as the cross-element regression check for the `ice_slow` re-home (D-Risk 1) — expected green, earth asserts co-application presence on synthetic keys only. No full local suite/browser/aggregate-coverage run; no command above ten minutes. No apply, archive, main-spec sync or merge in the proposal turn.
