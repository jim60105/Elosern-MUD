## Context

Data-only integration of the ice wave: the 13-node tree of `docs/lore/skill-trees/ice.md` authored into `world/skills/registry.py` + the ice rulebook rows over vocabulary that is ALL SHIPPED at master 0c3f3a27 — no sibling behavior changes, exactly the fire-wave shape. Research at authoring verified each needed primitive against current source: the declarative action-lock is the `combat_modifiers.yaml` `when {buff_active: <key>} then {actions_per_turn: 0}` rule (four live rows — `paralysis_locks_actions`, `suffocation_locks_actions`, `water_bind_locks_actions`, `fear_locks_actions` — consumed by `action.py:409-413` and the `action_preview` bundle path), the stillness-marker synergy is `DamagePolicy(predicate=("buff:<key>", …), attack_multiplier=…)` with shipped OR-match-once semantics (the earth `earths_judgment` triple prices 1.15 the same way), the wall/mire footprint is the `marker: ground` clause (loads/validates at `buffs.py:216-232`; live rows `fire_lava`, `earth_fissure*` — standing-on-it fact + battlefield-exit extinguishment shipped), 處決級 is `DamagePolicy(bypass_defense=True, predicate=())` and 毀滅級 `DamagePolicy(max_hp_fraction=0.10)` (live at the water/dark/earth/fire capstones), the timed-stat ladder is empty-modifier mount + `agility_flat` rule (live wind rows at `combat_modifiers.yaml:144-155`), and the n-ary two-parent DAG + reverse-edge cap derivation validates (fire/wind canopies live).

Ratified givens:

- **NON-GOAL — game-data contracts.** No ice catalog/data-contract tests: no key-set equality, no row mirroring, no MP-cost/cap/tier assertions, no echo of the ice.md table. All verification is BEHAVIOR on synthetic skills/state transitions (light/water/dark/earth/fire/wind convention). The dev-era `test_buffs.py` pin `assertFalse(blocks_action(entity))` on `ice_freeze`/`ice_prison` documents the inertness this wave inverts — it is REPLACED (not re-pinned) by the freeze rung's real lock behavior in the new synthetic module; the numeric duration/stacking pins re-home to load/apply/presence assertions per the ratified M4 discipline.
- **No new behavior code.** If any mechanic in the D1 coverage table turns out missing at implementation, that is a defect in the owning shipped surface, fixed there — nothing is bolted onto the catalog.
- **Zero users: no migrations, no aliases, no back-compat.** The dev-era ice rows are wholesale replaced; `ice_wall`'s off-tree defensive buff dies with the key (`ice_wall` survives as the registry node, the `bounds: defense +5` buff key is deleted) — 固定防禦 is 土's exclusive §4.2 verb, and §4.4 牆歸冰 ratified the wall as a movement gate, not armor.
- **BLOCKING_BUFF_KEYS ownership:** `{"paralysis"}` is LIGHTNING's key (buffs.py:304) and the `blocks_action` marker path stays lightning's. Ice's lock is the modifier-rule path — the same path `paralysis_locks_actions`, `water_bind_locks_actions` and `fear_locks_actions` already feed (D3). Ice ships ZERO keys into `BLOCKING_BUFF_KEYS`; the shipped inventory is untouched.
- **Serialization:** `openspec list --json` returned an empty change set at authoring time — no active sibling change; this change is sole author of every file it touches.
- **Node-data authority:** MP costs, coefficients, targets and durations are authored from ice.md's columns AS-IS — do NOT rebalance. All thirteen costs sit inside their §3.1 bands at authoring (11/13 學徒, 25/28 術師, 48/48/44 大師, 82/88/80 賢者, 158/140 主宰, 225 神格) — the cost-derived tier grouping (`spell_tier_for`) derives every 位階 label cleanly; implementation verifies the derivation, never «corrects» the cost. Cap semantics: reverse-edge derived (leaf 10, `PROFICIENCY_TIP_CAP`); this change authors no cap field.

## Goals / Non-Goals

**Goals:** Registry data for all 13 nodes (two roots, two Lv.3 branch points each feeding two children, two route chains converging only at the two-parent 神格 canopy); every lore clause delivered by some shipped primitive (coverage table D1); the dev-era set — including the off-tree defensive buff and the inert-control shape — gone without trace; the freeze ladder actually locking actions for the first time; the stillness-marker synergy (ice's constitution-mandated team contribution, §4.2) live as policy data; echo tests retired the fire/wind way; status-display census synced; the wave's shard manifest finalized.

**Non-Goals:** No new behavior code anywhere (see Context); no `state_reactions.yaml` ice rows (every ice clause is a direct effect component or a `buff_active` rule — zero reaction vocabulary needed); no change to `BLOCKING_BUFF_KEYS`/`blocks_action` or the existing four lock rules; no main-spec sync, no freeze-list pre-editing, no command-surface work; no data-contract tests; `ice.md` and `tests/test_command_docs.py` untouched; `magic-system.md` §3's ice row checked for contradiction only; the 2026-08-12 design doc (`openspec/changes/archive/2026-08-13-spell-catalog-ice/`) stays frozen history; `ice_mastery` untouched; earth's fissure rows untouched (they keep consuming `ice_slow` as pure consumer data).

## Decisions

### D1 — Node coverage table (all 13 nodes × delivering primitive)

Stat-axis mapping (lore → engine): freeze/定身 clauses are empty-modifier marker buff rows + one `actions_per_turn: 0` rule each (D3); the slow rungs are mount rows + `agility_flat` rules (the live wind ladder's shape — `bounds` is the retired presentation vocabulary with no settlement consumer, the ad084c9 finding); wall/mire cells are `marker: ground` footprint rows (D4); the synergy is `DamagePolicy(predicate=(five buff: entries), attack_multiplier=1.5)` (D5); 處決級/毀滅級 are the shipped policy rungs. Buff keys below are the authored homes. Registry keys: all TEN dev-era names survive by name under tree data; THREE new (`frost_mire`, `crystal_shatter`, `eternal_frost_apotheosis`).

| Node (key) | 節點數據 (ice.md) | Delivered by |
|---|---|---|
| 凍結之息 `frost_breath` | 學徒 11 MP 單體；敏捷 −3、15 s；cap 3 | `buff_apply:ice_slow` (RE-HOMED row: the bounds illusion → empty-modifier mount, duration 60→**15** per the node table / §3.3 學徒 band) + rule `ice_slow_agility_penalty` `{agility_flat: -3}`; root (遲緩路線) |
| 冰牆術 `ice_wall` | 術師 25 MP 單體(自/友)；目標周邊結霜成牆，進出該格者掛第一 rung（敏捷 −3），60 s；cap 3 | `buff_apply:ice_wall_frost` (NEW `marker: ground` footprint row, 60 s — D4's ratified cell reduction) + `buff_apply:ice_slow` (第一 rung, the earth-fissure reuse shape); prereq (frost_breath, 3) — the branch point feeding BOTH children; TargetSpec AS-IS 自/友 (faction-constraint precedent: the wind ally mount) |
| 凝霜泥沼 `frost_mire` | 大師 48 MP 範圍；地面霜化，留在其上者敏捷 −8、60 s；cap 10（樹冠） | `buff_apply:ice_frost_mire` (NEW `marker: ground` row, −8 via rule `ice_frost_mire_agility_penalty` `{agility_flat: -8}`, 60 s, ENEMIES audience); prereq (ice_wall, 3); branch-terminal leaf → tip cap 10 by derivation |
| 永凍領域 `permafrost_domain` | 大師 48 MP 範圍；凍結（無法行動）30 s；cap 8 | `buff_apply:ice_freeze` (RE-HOMED: empty-modifier 30 s kept — already the node's number) + rule `ice_freeze_locks_actions` `{actions_per_turn: 0}`; prereq (ice_wall, 3) |
| 絕對凍土 `absolute_tundra` | 賢者 82 MP 範圍；2.0＋凍結 40 s；cap 8 | coefficient 2.0 + `buff_apply:ice_freeze_tundra` (NEW 40 s key — `apply_buff` has no per-effect duration override, the `earth_fissure_quake` precedent); prereq (permafrost_domain, 8) |
| 長夜冰原 `eternal_ice_field` | 主宰 158 MP 範圍；2.8，毀滅級，附加凍結（duration 未標）；cap 10 | coefficient 2.8 + `max_hp_fraction=0.10` + `buff_apply:ice_freeze_nightfall` (NEW 60 s key — §3.3 主宰 band, D6); prereq (absolute_tundra, 8); consumed by the canopy |
| 冰錐術 `ice_shard` | 學徒 13 MP 單體；1.0；cap 3 | `damage:ice:magic` coefficient 1.0; root (監禁路線) |
| 冷凍箭雨 `frost_arrow_rain` | 術師 28 MP 範圍；1.0；cap 3 | coefficient 1.0; prereq (ice_shard, 3) |
| 冰封監牢 `ice_prison` | 大師 44 MP 單體；定身 30 s；cap 5 | `buff_apply:ice_prison` (key kept; 30 s kept) + its OWN rule `ice_prison_locks_actions` `{actions_per_turn: 0}` — 定身 and 凍結 are both action locks by §4.2 「冰讓你動不了」, differentiated by key + duration + own rule row (D3), never a second mechanism; prereq (frost_arrow_rain, 3) — the second branch point |
| 暴風雪 `blizzard` | 賢者 88 MP 範圍；2.0；cap 8 | coefficient 2.0; prereq (ice_prison, 5) |
| 冰晶爆裂 `crystal_shatter` | 賢者 80 MP 單體；2.8，對凍結／定身目標 1.5×；cap 10（樹冠） | coefficient 2.8 + `DamagePolicy(predicate=("buff:ice_freeze", "buff:ice_freeze_tundra", "buff:ice_freeze_nightfall", "buff:ice_freeze_apotheosis", "buff:ice_prison"), attack_multiplier=1.5)` — OR-match-once (the `earths_judgment` precedent); prereq (ice_prison, 5); branch-terminal leaf → tip cap 10 |
| 絕對零度 `absolute_zero` | 主宰 140 MP 單體；4.0，處決級（無視防禦），附加凍結（duration 未標）；cap 10 | coefficient 4.0 + `DamagePolicy(bypass_defense=True, predicate=())` + `buff_apply:ice_freeze_nightfall` (the 主宰 60 s rung is ONE shared key consumed by both 主宰 freeze nodes — D3); prereq (blizzard, 8) |
| 永凍神格 `eternal_frost_apotheosis` | 神格 225 MP 範圍；3.8，毀滅級，附加凍結 90 s；cap 10 | coefficient 3.8 + `max_hp_fraction=0.10` + `buff_apply:ice_freeze_apotheosis` (NEW 90 s key — node-table-priced, §3.3 神格 band agrees); TWO prereqs (eternal_ice_field 10 ＋ absolute_zero 10) ride the shipped n-ary DAG — the lore's 匯合 lands HERE and nowhere else (D2 lineage note) |

Every `*_locks_actions` rule (`ice_freeze`, `ice_freeze_tundra`, `ice_freeze_nightfall`, `ice_freeze_apotheosis`, `ice_prison`) is its own row with its own `buff_active` gate — SEVEN new rule rows total (4 freeze locks + 定身 lock + 2 agility rungs). `ice_mastery` is NOT a lineage node (PASSIVE 精通, off-tree, index.md §1): shipped row untouched, freeform ladder reads `progression.yaml` — census fact, zero edits.

### D2 — Lineage: the 匯合 is the canopy, not an edge (the corrected audit)

The lore ASCII shows a 匯合 bracket spanning 長夜冰原 and 絕對零度 into 永凍神格. Read against the node-data table (the authority), `eternal_ice_field`'s ONLY prerequisite column entry is 絕對凍土 Lv.8 — the bracket is purely the narrative of the two-parent canopy: `eternal_frost_apotheosis` requires `eternal_ice_field` >= 10 AND `absolute_zero` >= 10, and NO cross-route prerequisite edge exists anywhere else. Every edge in the specs delta was re-audited line-by-line against the table at this authoring pass and matches exactly (12 edges, two roots, both Lv.3 branch points with two children each, the two Lv.5 children, the 8-chain, the two-parent top). The delta additionally asserts `no cross-route edge exists … outside the canopy's two authored parent edges` — the honest reading, and the regression guard against exactly the phantom-edge defect an earlier draft surfaced.

### D3 — Freeze/定身 rung key layout: ice owns its keys, one lock rule per rung (ratified)

`BLOCKING_BUFF_KEYS = {"paralysis"}` is lightning's property (the `paralysis_locks_actions` rule + the `blocks_action` marker path + `spell_conditions`' freeze-adjacency check). Ice MUST NOT co-opt it: ice's stillness rungs are all author-owned keys — `ice_freeze` (30 s, re-homed) / `ice_freeze_tundra` (40 s) / `ice_freeze_nightfall` (60 s, shared by both 主宰 nodes) / `ice_freeze_apotheosis` (90 s) / `ice_prison` (定身 30 s) — each a parallel-duration row (the `earth_fissure`/`_quake`/`_apex` precedent: `apply_buff` has no per-effect duration override) and each locked by its OWN `buff_active → actions_per_turn: 0` rule row. 定身 and 凍結 are both action locks by §4.2 「冰讓你動不了」; they differentiate by key, duration, and their own rule row — never by a second mechanism, never by entering `BLOCKING_BUFF_KEYS`. Rungs hold/refresh/expire independently (refresh stacking per row, `stacking: refresh` kept). Consequences: the shipped `blocks_action`/`BLOCKING_BUFF_KEYS` inventory stays `{paralysis}` untouched; `spell_conditions`'s `blocks_action(target)` gate stays lightning-only-scoped (ice's lock lands through the same `action_preview`/`action.py` step-4 modifier path that paralysis's own rule feeds — locked ice holders resolve 0 actions and reject at step 4 identically); `crystal_shatter`'s predicate references ALL five keys (both families) at 1.5, OR-matched once.

### D4 — The wall/mire cells reduce to `marker: ground` footprints (ratified engine reduction)

The lore prices a cell-level gate (牆 surrounds the target's cell; 泥沼霜化一片地面). In-combat position is binary — `marker: ground`'s shipped semantics already define the whole clause set: the occupying fact is 「the entity holds a live instance of a definition carrying the clause」, the battlefield-exit extinguishment releases those who leave, and the earth fissure rows + fire lava row are live precedent for a harmful footprint mounted on the cell's occupant. So `ice_wall` authors `buff_apply:ice_wall_frost` (`marker: ground`, 60 s — the node's own duration) + the first-rung `buff_apply:ice_slow` on its target, and `frost_mire` authors `buff_apply:ice_frost_mire` (`marker: ground`, 60 s, its −8 riding rule `ice_frost_mire_agility_penalty`) to its ENEMIES area audience. Zero behavior code: the wall node keeps its AS-IS 自/友 target (the wall stands where its owner chooses), and the footprint + slow land on the walled target exactly as the lore prices 「進出該格的目標一律掛第一 rung」 under the binary-position reading. The mire's −8/60 s is node-table-explicit (not lore-silent). The wall's own lore-silent facts are none: duration 60 s is its column's number. Alternative rejected: a positional-shaped row — `marker: positional` is wind's 擊退 vocabulary with reachability-gate behavior; ice's clause is a standing-on-it damage/slow field, exactly 土/火's terrain shape.

### D5 — The stillness synergy is one OR-matched policy across both lock families

`crystal_shatter`: `DamagePolicy(predicate=("buff:ice_freeze", "buff:ice_freeze_tundra", "buff:ice_freeze_nightfall", "buff:ice_freeze_apotheosis", "buff:ice_prison"), attack_multiplier=1.5)` — the shipped any-match-once evaluation (the `earths_judgment` triple prices 1.15 the same way; two live facts never stack). This is ice's constitution-mandated team contribution (§4.2: 冰是全遊戲唯一的靜止標記供應商 — fear stays 暗's psychological stillness and is NOT referenced; a slow mount is not stillness and never prices the multiplier). 2.8 × 1.5 = the node's 「額外造成 1.5 倍傷害」 authored as the multiplier exactly, no rebalancing.

### D6 — Catalog-owned authored numbers (the lore is silent)

Two freeze durations and one lineage-adjacent fact, decided here with derivation; single-row revision if the lore owner later prices differently: (a) `ice_freeze_nightfall` 60 s for both `absolute_zero` and `eternal_ice_field` — §3.3's 主宰 control-duration band (60 秒), the only anchor (the nodes say 附加凍結 with no duration; the tree prices every other freeze explicitly). Rejected: reusing the 大師 30 s key (the主宰 rungs must differentiate — the whole point of parallel keys) or inventing a fifth duration. (b) The 主宰 rung is ONE key shared by both 主宰 nodes (the 賢者 rung is per-node because tundra 40 s ≠ blizzard-none; the two 主宰 nodes price the same silent rung — one row, two consumers, still its own lock rule). (c) Everything else is node-table-explicit: 30/40/90 s freezes, 定身 30 s, −3/15 s and −8/60 s slows, wall/mire 60 s.

### D7 — Key census and wholesale replacement

Old→new registry: TEN dev-era keys re-authored under tree data (`frost_breath`, `ice_wall`, `permafrost_domain`, `ice_prison`, `ice_shard`, `frost_arrow_rain`, `blizzard`, `absolute_tundra`, `absolute_zero`, `eternal_ice_field` — all survive by name; coefficients/prereqs/bindings rewritten); THREE keys added (`frost_mire`, `crystal_shatter`, `eternal_frost_apotheosis`). The dev-era ice block is written through `_elemental_spells`; the new block follows the earth/fire/wind individually-written `_spell` shape (needed for per-node `effect_policies`, the ally-faction wall, multi-prereq capstone); the builder itself STAYS (lightning still uses it). Buff keys: `ice_wall` DELETED (the off-tree defensive buff — 固定防禦 is 土's verb; the node survives, the buff key dies); `ice_slow` RE-HOMED (60→15 s, bounds illusion → mount + rule; earth's three fissure rows keep consuming it — the rung becomes real for their targets, which is the fix); `ice_freeze` RE-HOMED (30 s lock rung, first live lock rule); `ice_prison` KEPT (30 s, gains its lock rule); NEW: `ice_wall_frost`, `ice_frost_mire`, `ice_freeze_tundra`, `ice_freeze_nightfall`, `ice_freeze_apotheosis`. Display census: swap `ice_wall` (beneficial) out; `ice_slow`/`ice_freeze`/`ice_prison` rows kept; add five new buff rows (all harmful) + seven new rule-id rows per the table's label/severity conventions (the import-time coverage check covers exactly live buff keys ∪ combat-modifier rule ids — state-reaction ids are not in it; the wind/fire waves verified this fact).

### D8 — Effect-policy sequencing on multi-effect nodes

Per-effect `EffectPolicy` tuples, earth/fire/wind shape, one entry per effect id in order. Single-effect buff nodes: no policies needed beyond defaults (`frost_breath`, `ice_wall`, `frost_mire`, `permafrost_domain`, `ice_prison`). `absolute_tundra`: `(EffectPolicy(coefficient=2.0), EffectPolicy())`. `eternal_ice_field`: `(EffectPolicy(coefficient=2.8, damage=DamagePolicy(max_hp_fraction=0.10)), EffectPolicy())`. `crystal_shatter`: `(EffectPolicy(coefficient=2.8, damage=DamagePolicy(predicate=(five buff: entries), attack_multiplier=1.5)),)`. `absolute_zero`: `(EffectPolicy(coefficient=4.0, damage=DamagePolicy(bypass_defense=True, predicate=())), EffectPolicy())`. `eternal_frost_apotheosis`: `(EffectPolicy(coefficient=3.8, damage=DamagePolicy(max_hp_fraction=0.10)), EffectPolicy())`. Effect components are independent settlements (fire precedent): the freeze/定身 rider lands whether or not the damage leg lands. Audience follows each node's TargetSpec (area = shipped area audience; the mire's footprint is ENEMIES-audience like every harmful area row).

### D9 — Wave interface-ownership matrix (integration contract)

| Interface | First owner | Consumers |
|---|---|---|
| `buff_active → actions_per_turn: 0` rule + step-4/preview consumption | shipped lock rules (paralysis/suffocation/water_bind/fear, archived waves) | this change's five lock rows (data only) |
| `marker: ground` clause + standing-on-it fact + battlefield-exit extinguishment + `buff:<key>` predicate entry | terrain-marker (SHIPPED, archived 2026-09-16) | this change's `ice_wall_frost`/`ice_frost_mire` rows + `crystal_shatter` predicate (data only) |
| `agility_flat` timed rules + empty-modifier mounts | earth defense ladder + wind agility ladder (SHIPPED) | this change's two agility rows (data only) |
| `bypass_defense` 處決級 / `max_hp_fraction` 毀滅級 / n-ary DAG / reverse-edge caps / freeform ladder | archived light/water/dark/earth/fire waves | this change's data only |
| `ice_slow` key | dev-era catalog (this change RE-HOMES it) | earth's three fissure rows keep consuming — zero earth-side edits |
| 13-node registry block + ice buff/modifier rows + lineage catalog contract + status-display census + echo-test retirement + shard manifest final state | this change | — |

Shared-file schedule: the conflict matrix is EMPTY — sole author everywhere; `.github/evennia-shards.json` gets this wave's only edit here; `state_reactions.yaml` untouched.

### D10 — Echo-test retirement (fire/wind-spell-catalog's exact pattern)

`skill-registry::skill-registry-contains-the-full-冰-element-spell-set` REMOVED with reason (mandates the duplicated dev-era ten-row data contract — inert controls, bounds illusion, off-tree defensive buff, no lineage; the ratified wave NON-GOAL rules out data-contract tests) and migration (delete `ICE_SPELL_CATALOG` + `IceSpellCatalogTests`'s echo methods in `world/skills/tests/test_spell_catalogs.py`, the `test_ice_spell_tier_labels_match_the_catalog` tier-label echo in `world/skills/tests/test_cost_tiers.py`, the dev-era `ice_slow`/`ice_freeze` numeric pins in `world/rules/tests/test_buffs.py` — with the `assertFalse(blocks_action)` inertness pin REPLACED by real lock behavior in the new synthetic module, the obsolete traceability/freeze entries at the separately authorized main-sync, the `tests/test_rules_skills_test_layout_contract.py` class-home pin update (`IceSpellCatalogTests` retirement + the new `IceLineageTreeCatalogTests` home) per the wind-wave 7b66d14d precedent — the seed's data-contract entries for the two test files already exist and stay). MECHANIC tests that are builder-shape/parse tests stay (`ElementalSpellsBuilderTests`, `ClosedVocabularyParseTests` untouched). The authored tree's lineage shape (twelve edges, canopy at the capstone) is covered by a NEW `IceLineageTreeCatalogTests` shipped-content contract in `test_spell_catalogs.py` (the fire `FireLineageTreeCatalogTests`/wind shape) — the lineage capability's registered-content convention, NOT a data echo.

### D11 — Verification contract (ice wave)

1. **Every test is behavior on synthetic state** — synthetic skills/buffs/entities, real settlement through the action/buff/clock/combat/session/preview stages; tests MUST fail on plausible bugs (freeze rung not locking, lock leaking past expiry, lock applied to non-holders, synergy double-pricing on two facts, synergy firing off a slow mount, footprint slow not mounted, footprint persisting after battlefield exit, wall defense ghost revived, capstone gate leak, retired `ice_wall` buff key resolving through a leftover). Source-text, data-echo and row-mirror assertions are prohibited.
2. **The catalog change's own proof** is a disposable offline engine scenario exercising each distinct D1 composition through real casts (slow −3 at 15 s on the real to-hit/flee/overwhelm consumers + expiry; each lock rung resolving `actions_per_turn: 0` for exactly its duration with adjacent entities unaffected and control-immunity honored as the shipped consumers honor it; 定身 locking through its own rule; synergy priced once on each stillness key incl. both facts at once and silence off slow/unrelated buffs; wall/mire footprint fact + slow + exit sweep + expiry; execution bypass vs high defense; devastation fraction on landed vs missed; two-root gates + branch points + two-parent canopy; retired-key rejection paths), plus the synthetic behavior module.
3. **Focused invocation** (tasks.md): `uv run --locked evennia test --settings test_settings.py --keepdb <modules>` with `MUD_TEST_SETTINGS=1` via the tool env, NEVER a shell prefix; plus `tools.observability_lint check`, `tools.test_data_lint check`, `tools.spec_traceability check`, `openspec validate ice-spell-catalog --strict`. No full local suite / browser / aggregate-coverage run; no command above ten minutes.
4. **Traceability:** canonical IDs via `uv run --locked python -m tools.spec_traceability list`; `@covers_requirement` literal IDs only; the retired 冰-element ID `skill-registry::skill-registry-contains-the-full-冰-element-spell-set` leaves the ledger with its requirement at the separately authorized main-sync; the new IDs `skill-registry::ice-spell-progression-composes-executable-physical-stillness-behavior` and `skill-lineage::the-ice-lineage-ships-as-the-authored-two-root-branching-tree-with-a-two-parent-canopy` annotate the new synthetic module / the lineage catalog test (defer-annotation-to-main-sync applies if an ID reads red before the sync).
5. **Shards:** the new non-browser test module joins exactly one shard in `.github/evennia-shards.json` (rules-a, beside `test_fire_burn_immolation`/`test_earth_terrain_guard`); this change owns the wave's only manifest edit; the ownership-contract test verifies.

## Risks / Trade-offs

- **`ice_slow` re-home (60→15 s, illusion → real −3) is the wave's cross-element live blast**: earth's three fissure rows keep consuming it — their targets now genuinely lose 3 agility for 15 s (the earth wave explicitly ratified 「ice's authority over the rung's row」; the duration shortening is ice pricing its own rung per the node table). Earth has no numeric pins on the row's settlement (verified: `test_earth_terrain_guard.py` asserts co-application presence on synthetic keys only) — expected green; if any earth pin breaks, it is a numeric echo of the retired illusion and gets the M4 load/apply/presence re-home, never a re-pin.
- **The `assertFalse(blocks_action)` → real lock inversion**: the inertness pin dies with the requirement; the lock is asserted behaviorally (actions resolve 0) in the new module, not by moving ice's keys into lightning's `BLOCKING_BUFF_KEYS` inventory (D3).
- **`ice_wall` display row flips meaning (beneficial → gone)**: the code disappears with the buff key; the registry node survives with new rows — the display set-equality test (`test_status_display.py`) is expected to re-home with the census, which is its designed behavior.
- **`ice_freeze_nightfall` shared by two 主宰 nodes** — refreshing either refreshes the same rung (ratified: one shared duration, D6b). Alternative (per-node keys) rejected: it would invent a duration distinction the lore does not price.
- **Freeze durations 30/40/90 and 定身 30 are node-table-explicit; only the 主宰 rung is band-derived** (D6) — recorded, single-row revision if the lore owner prices otherwise; zero engine coupling.
- **All consumed primitives are shipped and already exercised live** (four lock rules, earth fissure/fired lava markers, wind agility rules, fire/wind canopies at HEAD) — if any fails fit at implementation it is a defect in the owning archived surface, fixed there, not re-proposed here.

## Migration Plan

Nothing migrates. Registry ice block swap + rulebook row re-home/new rows + seven modifier rules + display census + echo-test retirement + shard finalization; offline scenario proof; synthetic settlement tests. Main-spec sync, ledger and freeze-list edits stay in the separately authorized workflow.

## Open Questions

None blocking. The one tension resolved at authoring: the 匯合 bracket's literal reading (an extra eternal_ice_field prerequisite edge) was audited against the node-data table and rejected — the table is the authority, the convergence is the canopy's two parents (D2).
