## Why

State-dependent healing and contact rituals must reject consistently before costs, then reuse the existing resistance and pleasure pipeline. Build typed subject-scoped cast conditions and state magnitudes that any spell can declare.

## What Changes

- Add typed cast conditions/interaction policy and reusable state-magnitude declarations, evaluated through existing no-create condition/state readers.
- Extend preflight/final capability validation and the existing resistance gate; preserve standard sexual acts and produce one standard outcome for elemental interactions.
- Implement the typed stimulus handler using the existing interval/equipment rules and canonical writer; stage randomness and snapshot every indirectly changed surface.
- Update rejection presenters and both command reference documents for altered cast availability; verify text/OOB paths use the same backend contract.
- **BREAKING** where authored policy or removed light data replaces old behavior; no compatibility aliases, migrations, or interim fake effects.
- Verification uses synthetic behavior tests and targeted runtime scenarios, never light catalog key/count/value contracts.

## Capabilities

### New Capabilities
- `stateful-spell-casting`: State-dependent healing and contact rituals must reject consistently before costs, then reuse the existing resistance and pleasure pipeline. Build typed subject-scoped cast conditions and state magnitudes that any spell can declare.

### Modified Capabilities
- `skill-effect-model`: Add the typed stimulus declaration to the closed effect grammar.
- `action-resolution-pipeline`: The executable contract changes as specified in this proposal's delta.
- `sexual-resist-cast-wiring`: The executable contract changes as specified in this proposal's delta.

## Impact

Implementation surfaces: `world/skills/effects.py`, `world/skills/registry.py`, `world/rules/action.py`, `world/rules/spell_conditions.py`, `world/rules/sexual_act_effects.py`, `world/rules/sexual_transitions.py`, `world/rules/stored_sexual_reads.py`, `world/rules/cast_settlement.py`, `world/rules/combat_session.py`.

Dependencies: `light-effect-potency`. Estimated bounded implementation: 8 engineer-hours including focused tests and integration, no project-wide suite. This is one independently reviewable workday slice; it does not ship incomplete catalog entries.

This turn creates planning artifacts and authorized lore corrections only. Do not apply, archive, sync main specs, create feature branches or merge until the user requests it. The complete dependency/conflict schedule and shared interface ownership are in `../light-spell-catalog/design.md`.
