## 1. Preconditions

- [x] 1.1 Confirm `make-shop-drawer-frameless` and `make-quest-drawer-frameless` are applied and archived, then prove the services path is unreachable: `grep -rn "openServiceSubmenu" web/static/webclient/js/elosern` returns nothing, and `grep -rn "source: \"services\.\|'services\." web/webclient-app --include='*.js' --include='*.vue'` (excluding `dist/` and `tests/`) shows no push site. Stop and report if either finds a live producer.

## 2. Store and shell deletions

- [x] 2.1 `web/webclient-app/stores/elosern/interaction.js`: delete the `openServiceSubmenu` branch of `handleExplorationItem`, the whole `handleServiceItem` handler, the quantity-form capture at the top of `focusPress`, and the `ServiceMenu` import; update the file header comment. `web/webclient-app/stores/elosern/frames.js`: delete the `handleServiceItem` call in the router submit path and the hosted-drawer pop rule in the settle (keep the character sub-dock rule). Verify with `pnpm exec vitest run web/webclient-app/tests/store` (repository root) after 4.x.
- [x] 2.2 `web/webclient-app/stores/elosern/hud.js`: delete `quantityForm` / `openQuantityForm`, `serviceSurface` / `setServiceSurface`, `HOSTED_SERVICE_SOURCES`, `descriptorIsServiceFrame`, `currentFrameIsServiceFrame`, `SERVICE_SURFACE_DRAWERS`, `SERVICE_SURFACE_FOR_SOURCE`, the `ServiceMenu` import, and the hosted branch of `closeHudDrawer` (design D2: frameless early return, then the existing sub-dock clear + root re-home for `skill` / `lore` / `status`); drop the `popFrame` option and its callers' arguments (`composables/use-drawers.js`, `composables/use-dock.js`). Update the file header comment.
- [x] 2.3 `web/webclient-app/stores/elosern/creation.js`: delete the settle-time hosting block and the `setServiceSurface(null)` teardown call in `syncHudDrawer`; update the function comment. `stores/elosern/view.js`: delete the quantity-form reset. `stores/elosern.js`: delete the `quantityForm` and `currentFrameIsServiceFrame` exports.
- [x] 2.4 `web/webclient-app/stores/frame-resolvers.js`: delete the nine `services.*` entries, `servicesMenuSource`, `questRowAt`, the services section comment, and the `ServiceMenu` import.
- [x] 2.5 `web/webclient-app/composables/use-dock.js`: delete `servicesConfirm` and its return entry. `web/webclient-app/AppClient.vue`: delete the `.services-confirm` block, the `servicesConfirm` destructure, the `:quantity-form` binding on `ShopPanel`, and any `.services-confirm` CSS. `web/webclient-app/components/ShopPanel.vue`: delete the `quantityForm` prop and its watch, and update the header/`selectedKey` comments (selection now comes from row focus or click only).
- [x] 2.6 Delete `web/webclient-app/lib/service_menu.js` and `web/static/webclient/js/elosern/service_menu.js`. `grep -rn "service_menu\|ServiceMenu\|quantityForm\|serviceSurface\|handleServiceItem\|servicesConfirm\|currentFrameIsServiceFrame\|descriptorIsServiceFrame\|SERVICE_SURFACE" web/webclient-app web/static/webclient/js --include='*.js' --include='*.vue'` (excluding `dist/`) returns only the test files handled in section 4 before they are edited, and nothing after.

## 3. Node gate and data-lint lists

- [x] 3.1 Delete `web/static/webclient/js/tests/service_menu.test.js` and the `ServiceMenu` block of `hud_dock_menus.test.js`. Remove the file from `tools/test_data_lint_seed.json`, `tools/test_data_freeze.json`, and `tests/test_data_independence_js_webclient.py`. Run `node --test web/static/webclient/js/tests/*.test.js` and `uv run --locked python -m tools.test_data_lint check`; both green.
- [x] 3.2 Re-point `web/webclient/tests/test_node_suite_evidence.py::test_service_menu_node_suite_passes` (rename to describe the panel) at `protocol_services_a.test.js` and `protocol_services_b.test.js`, keeping its `webclient-service-menus::the-services-panel-is-an-exact-read-only-exploration-mode-panel` annotation. Run the Evennia shard that contains it green.

## 4. Vitest

- [x] 4.1 `web/webclient-app/tests/frame-resolvers.test.js`: replace the services-family cases with one table-driven case asserting every former `services.*` source resolves to `{ unresolvable: true, reason: null }`; remove the `ServiceMenu` import.
- [x] 4.2 `tests/store/command_echo_surfaces.test.js`: replace the stock-frame / quantity-form echo cases with `onShopBuy` / `onShopSell` dispatches (the `ShopPanel` emit shape) asserting the echo resolves `itemLabel` from the committed shop rows; remove the `ServiceMenu` import. `tests/store/digit_row_picks.test.js`: delete the quantity-form precedence case.
- [x] 4.3 `tests/store/declarative_surfaces.test.js`, `tests/store/frameless_bag.test.js`, `tests/store/hud_drawer.test.js`, `tests/app_client_frameless_bag.test.js`: remove the forced-hosting setups (`setServiceSurface`, `services.*` pushes, `ServiceMenu` imports) and any assertion on the deleted store members, keeping the frameless-drawer assertions. In `tests/dialogue_store.test.js`, `tests/dialogue_dock.test.js`, `tests/bridge/bridge.test.js`, and `tests/store/store_dispatch_focus.test.js`, replace the `"services"` sub-dock literal used as a generic active-sub-dock marker with `"character"` and reword comments that describe a live services sub-dock. Add one store test: `closeHudDrawer()` on the status drawer opened from the Character entry still clears the `character` sub-dock and re-homes the exploration root. Run `pnpm test` green.

## 5. Specs and traceability

- [ ] 5.1 Sync this change's deltas into the main specs, then edit the Purpose paragraphs of `openspec/specs/webclient-service-menus/spec.md` (drop "the keyboard service dock with bounded quantity forms and an abandon confirmation"; say the service surfaces render in frameless reference drawers with bounded quantity entries and an abandon confirmation) and `openspec/specs/webclient-frame-resolution/spec.md` (drop the services table and "drawer-follows-stack behavior").
- [ ] 5.2 Re-anchor `webclient-frame-resolution::the-resolver-table-completes-with-the-services-combat-and-creation-families` in `web/tests/browser/test_browser_combat_menu.py` and `test_browser_creation_reset_draft.py` to the new "…combat and creation families" ID (from `uv run --locked python -m tools.spec_traceability list`), and update the `services.board` comment in `test_browser_exploration_frame.py`. Run `uv run --locked python -m tools.spec_traceability check` green.

## 6. Validation

- [ ] 6.1 Run the Node gate, `pnpm test`, `pnpm run build`, `pnpm run build-storybook`, `pnpm run showcase-coverage` (repository root), `uv run --locked python -m tools.test_data_lint check`, `uv run --locked python -m tools.spec_traceability check`, and `uv run --locked evennia test --settings test_settings.py --keepdb tests.test_evennia_test_optimization_contract`; all green.
- [ ] 6.2 Run `openspec validate retire-service-keyboard-frames --strict` and `git diff --check`; both clean.
