# Tasks: limbo-one-way-city-gates

## 1. Registry module

- [x] 1.1 Create `world/maps/city_gates.py`: `@dataclass(frozen=True, slots=True) class CityGateDef` with `map_id: str`, `gate_xyz: tuple[int, int, str]`, `exit_key: str`, `exit_aliases: tuple[str, ...]`, and `CITY_GATE_REGISTRY: MappingProxyType` with exactly one row `capital_altoria` (`map_id="capital_altoria"`, `gate_xyz=(2, 0, "capital_altoria")`, `exit_key="南門"`, `exit_aliases=("王都", "城門")`); module docstring names it the sole authored source of 虛境 city gates (design doc §4).
- [x] 1.2 Confirm no other module duplicates a gate key/alias/coordinate (grep 「南門」/「王都」/「城門」 gate authoring outside the registry and `altoria_capital.py` room data).
- [x] 1.3 Create `world/rules/city_gates.py` (design D7): `reanchor_home_on_gate_arrival(character, destination)` — player-gated no-op for non-`PlayerCharacter` arrivals (mirror the internal no-op discipline of `record_arrival`/`follow_companions`); resolve the destination's grid coordinate and return unless it matches a `CITY_GATE_REGISTRY` row's `gate_xyz` (read-only import of the frozen maps-side module); return unless `character.home` IS the 虛境 room (locate by `LIMBO_KEY` lookup, never dbref); otherwise set `character.home = destination` exactly once. This is the capability's sole home writer (single-writer rule).

## 2. bootstrap.sync_grid rewrite

- [x] 2.1 In `world/maps/bootstrap.py`: import `CITY_GATE_REGISTRY` from `world.maps.city_gates`; delete the `EXIT_TO_CITY` and `EXIT_TO_LIMBO` constants and both current `_ensure_exit` bridge calls (`:265-266`).
- [x] 2.2 Keep the `LIMBO_KEY` lookup with its `bootstrap_grid_limbo_room_missing` warn-and-return; after it, loop `CITY_GATE_REGISTRY` rows: resolve `grid.get_room(row.gate_xyz).first()`; on `None` call `log_warn("bootstrap_grid_gate_missing", context={"map_id": row.map_id, "xyz": row.gate_xyz, "action": "skip_gate_row"})` and `continue`; otherwise `_ensure_exit(limbo, gate, key=row.exit_key, aliases=list(row.exit_aliases))` (existing idempotent converge helper unchanged).
- [x] 2.3 Add the prune pass after the row loop: find every persisted `Exit` whose destination is the 虛境 room (destination-indexed query — reverse exits live at city-side rooms, not in `limbo.exits`), delete each, logging `log_info("bootstrap_grid_exit_pruned", context={"exit_key": …, "exit_dbref": …, "source_room_key": …, "action": "enforce_one_way_gates"})` per deletion; converged databases delete nothing and log nothing.
- [x] 2.4 Import `log_info` alongside `log_warn` via the named-import facade (`from world.observability import log_info, log_warn`); update the module docstring ("bridge it to Limbo" → one-way registry gate wording); leave `SOUTH_GATE_XYZ`/`NORTH_GATE_XYZ` and all wilderness code untouched.
- [x] 2.5 In `typeclasses/exits.py::after_successful_movement`: after the existing success-path steps (at the tail — the same position where the sibling removes the `observe_room_entry` call; additive, non-overlapping hunk, coordinate per the proposal-Impact ownership note), add the deferred-import call `reanchor_home_on_gate_arrival(traversing_object, <resolved destination>)` so a successful gate traversal re-homes a 虛境 `home` (design D7). Position it strictly after the steps that may raise (`charge_movement`), so a failing settlement never reaches it; mention the seam in the helper docstring (the onboarding-sentence reword of that docstring itself belongs to the sibling's trim of the same function).

## 3. Layer-2 hard gate (虛境 entry)

- [x] 3.1 In `typeclasses/rooms.py`, add `class LimboRoom(ObjectParent, DefaultRoom)`: override `at_pre_object_receive(arriving_object, source_location, **kwargs)` — when the arriving object is a character (guard via the repo's character seam, e.g. `DefaultCharacter`/`PlayerCharacter` isinstance check done with a deferred import) and it is not superuser-governed (`getattr(arriving_object, "is_superuser", False)`, mirroring the admin convention at `typeclasses/exits.py:163-166`), send a zh-tw localized refusal via `arriving_object.msg(...)` and return `False`; every other arrival returns `super().at_pre_object_receive(...)`. Docstring cites design doc §4 (amended): layer-2 gate; path-complete because Evennia 6.1.0 `DefaultObject.move_to` calls the destination's `at_pre_object_receive` as an abort hook for every move path — exit traversal and teleport/`moveto` alike (`evennia/objects/objects.py` move-hook sequence).
- [x] 3.2 In `world/maps/bootstrap.py::sync_limbo()`: after locating/renaming/authoring the room, declaratively converge its typeclass — when the room's current typeclass is not `LimboRoom`, call `room.swap_typeclass("typeclasses.rooms.LimboRoom", clean_attributes=False, no_default=True)` (module-path string; no import cycle); an already-converged room is a no-op. Keep the existing key/alias/description authoring intact so convergence preserves the authored identity.

## 4. Tests (world/maps/tests/)

- [x] 4.1 Adjust `test_bootstrap.py::test_sync_grid_creates_thirteen_rooms_and_twenty_six_exits` and any other exit-count/return-exit assertions to the forward-only bridge (the reverse 「離開王都」 no longer exists after sync).
- [x] 4.2 Add forward-exit idempotency coverage: after `sync_grid()` the starting room has exactly one exit to `(2, 0, "capital_altoria")` keyed 「南門」 with aliases 「王都」/「城門」; a second run duplicates nothing; a pre-drifted exit (legacy English aliases) converges in place on the same object.
- [x] 4.3 Add prune coverage: seed an exit South Gate→虛境 (key 「離開王都」, alias 「回虛境」) before/after a first sync, run `sync_grid()`, assert the exit object is deleted, the `bootstrap_grid_exit_pruned` event fires once (patch/assert via the observability seam the existing tests use), and a second sync logs no prune event.
- [x] 4.4 Add registry-only-exits coverage: after `sync_grid()`, the 虛境 room's exit set equals the registry rows (exactly one, to the `capital_altoria` gate), i.e. no exit list drift.
- [x] 4.5 Add missing-gate-row coverage: with the South Gate room absent (or the registry rebound in-test to a second row whose gate room is missing), assert `bootstrap_grid_gate_missing` warns, that row creates no exit, the other row still converges, and `sync_grid()` does not raise.
- [x] 4.6 Rewrite `test_limbo_room.py`'s bridge tests (`test_bridge_exits_carry_zh_tw_keys_and_aliases_only`, `test_pre_existing_english_bridge_aliases_are_reconciled_in_place`, `test_resync_keeps_bridge_zh_tw_without_duplicates`) to the forward-only surface: forward key/aliases from the registry, no return exit, prune on seed.
- [x] 4.7 Add hard-gate refusal coverage in `test_limbo_room.py` (or a focused module per 4.8): after `sync_limbo()` the room is typed `LimboRoom`; a non-superuser character traversing a hand-built exit into 虛境 is refused — location unchanged, zh-tw refusal message received; a forced `move_to` (teleport-style) by/for the same character is likewise refused (test the real `move_to` seam, no aspirational claims); a superuser-controlled character's arrival succeeds.
- [x] 4.8 Add gate-convergence idempotency coverage: `sync_limbo()` run twice leaves the room `LimboRoom` with no second swap (assert via the guarded branch), and the `LIMBO_KEY` key, `limbo` alias, and `LIMBO_DESC` description survive both runs.
- [x] 4.9 Anchor every new/renamed requirement with `@covers_requirement` using ids from `uv run --locked python -m tools.spec_traceability list` (applied specs): the `limbo-one-way-gates` family (registry sole source, forward-exit convergence, prune, missing-row degrade, add-a-row, 虛境 admits no character, first-gate-traversal home re-anchor) plus the renamed `grid-room-sync`, `limbo-room` (bridge + `sync_limbo` typeclass convergence), and `sample-city-altoria` bridging requirements; run `uv run --locked python -m tools.spec_traceability check` expectations at integration time (main agent runs full validation).
- [x] 4.10 If any task above lands in a NEW module under `world/maps/tests/`, register it in exactly one shard of `.github/evennia-shards.json` — the `maps-webclient-imports-prompts-tests` shard (label `world.maps`); prefer extending `test_bootstrap.py`/`test_limbo_room.py` so no shard edit is needed.
- [x] 4.11 Add the full one-way-flow integration test (sequenced post-`remove-onboarding-tutorial` for realism — birth-in-虛境 must already be the shipped flow): fresh character born in 虛境 (`home` == 虛境 room) → traverse the 「南門」 gate → `CmdHome` → assert the character arrives in the South Gate gate-room, NEVER 虛境, and that no 虛境 entry occurs (the layer-2 gate would refuse it anyway). Land in `world/maps/tests/test_limbo_room.py` (shard-safe).
- [x] 4.12 Rebuild the `EXIT_TO_CITY` bridge fixture in `world/rules/tests/test_map_knowledge_integration.py::test_limbo_bridge_records_grid_node` (verified: imports `EXIT_TO_CITY` at :80, uses `EXIT_TO_CITY["key"]`/`["aliases"]` at :86-87): construct the hand-built bridge from the `CITY_GATE_REGISTRY` `capital_altoria` row (`row.exit_key` / `list(row.exit_aliases)`) — or use the synchronized real exit — preserving the `map-knowledge::arrival-recording-happens-only-at-existing-successful-arrival-seams` anchor and the arrival-seams coverage this test carries.
- [x] 4.13 Add home-policy unit coverage (extend `test_limbo_room.py`, shard-safe): first gate traversal by a character whose `home` is 虛境 sets `home` to the gate room; a second traversal of the same gate is inert once `home` is non-Limbo; a hand-set non-Limbo `home` survives a first traversal untouched; a traversal whose settlement fails/compensates leaves `home` at 虛境; a non-player object traversing the gate changes no home.

## 5. Consistency sweep

- [x] 5.1 Grep the repo for `EXIT_TO_CITY` / `EXIT_TO_LIMBO` / 「離開王都」 / 「回虛境」 outside tests being updated and the change/spec archives; ensure zero production references remain (clean cutover, no aliases).
- [x] 5.2 Confirm `typeclasses/rooms.py` changes are exactly the new `LimboRoom` class (no edits to `Room`/`GridRoom`/other typeclasses) and confirm `docs/game/commands.md` / `docs/game/command-reference.md` need no change (no player command surface changes; the removed exit key was world data, not a command).
- [x] 5.3 Sanity-run the observability lint expectation: new event ids `bootstrap_grid_gate_missing` and `bootstrap_grid_exit_pruned` are snake_case with `context` dicts; `tools/observability_freeze.json` handling follows the shrink-only rule (full `tools.observability_lint check` left to the integration owner).
