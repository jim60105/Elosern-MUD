## Why

The lightning skill tree (`docs/lore/skill-trees/lightning.md`, 14 nodes: two roots — 先制路線 靜電護罩 chain and 過載路線 電擊術 chain — branch points at lightning_flicker→thunder_combo and spark_shock, and the 神格 canopy 雷霆神格 where the lore's 匯合 lands as the two-parent capstone) is the node-data authority, but the registry still ships the dev-era 2026-08-12 ten-row lightning set with no prerequisites and no coefficients, and its two signature rows are INERT illusions: `lightning_static_ward` is the retired `bounds`-illusion shape (no settlement consumer), `lightning_extra_action`'s `bounds: {actions_per_turn, ceiling: 1}` has no > 1 consumer at all (turn-order-control ships the consumption), and 麻痺 rides the dev-era 30 s `paralysis` row instead of the node table's priced rungs. The wave's two behavior changes ship exactly the missing verbs — extra-action consumption, in-round reordering, the declared-chance lock, the three-strike cap — and this change authors the whole tree over shipped+wave grammar, mirroring the fire/ice single-change precedents for the data scope.

## What Changes

- Replace the ten dev-era lightning registry rows with the 14-node tree verbatim from the node table (keys, labels, targets, MP costs AS-IS — every cost sits inside its §3.1 band; prerequisites via `SkillPrerequisite(node, N)`; coefficients per §3.2 — 處決級 = `bypass_defense`, 毀滅級 = `max_hp_fraction: 0.10`; caps via the existing reverse-edge derivation — no stored cap field; `thunder_apotheosis` is the two-parent capstone at 審判雷霆 Lv.10 ＋ 神雷滅殺 Lv.10). Key census: TEN re-authored (all dev-era keys survive by name — `spark_shock`, `static_ward`, `chain_lightning`, `paralyzing_bolt`, `thunder_combo`, `lightning_strike`, `heavens_thunder`, `thunder_gods_haste`, `judgement_thunder`, `divine_lightning_slaughter`), FOUR new keys (`lightning_flicker`, `thunder_shatter_strike`, `thunder_prison`, `thunder_apotheosis`). The dev-era `_elemental_spells("lightning", …)` block dies wholesale (it is the builder's LAST caller — the builder itself and its mechanic test stay; the two individually-written SELF_ONLY rows survive as individually-written rows).
- Author every node's effect composition over shipped+wave vocabulary only: `damage:lightning:magic` coefficients (1.0 → 4.0); `thunder_combo`'s 「連續三次判定」 as the wave's only live `DamagePolicy(extra_strikes=2)` row (multi-strike-cap-widening grammar); `thunder_gods_haste`'s 「本回合獲得一次額外行動機會」 as the `lightning_extra_action` mount re-homed to a 12 s (two-round-window, design D4) empty-modifier buff row + the additive settlement `thunder_gods_haste_grants_action {buff_active → actions_per_turn: 1}` (baseline 1 → 2; turn-order-control grammar); the 麻痺 rungs `paralysis` (re-homed 30→20 s, 術師 band per the node table) and `paralysis_enhanced` (30 s, 大師 band) each locked by the shipped `paralysis_locks_actions`-shaped rule path — the `blocks_action`/`BLOCKING_BUFF_KEYS = {paralysis}` inventory stays lightning's untouched (the rule-table lock is what the round loop consumes; the marker path stays as shipped); static_ward's micro-rung as the fire `scorching_armor_ignite` precedent — a `physical_hit` + `buff_active: lightning_static_ward` reaction applying the `static_ward_retreat` marker row (`round_order: retreat_to_tail` + the narrow `chance: 15` lock rule `static_ward_micro_stun`), lightning_flicker raising the trigger via its own higher-`chance` rule row + its own `advance_to_head` mount for 「你的行動順序提前」, and `thunder_apotheosis`'s tail-push as ENEMIES-audience hit riders applying the `apotheosis_retreat` marker row (`turn-order-control` grammar); `paralyzing_bolt`/`thunder_prison` mount the 麻痺 rungs through ordinary `buff_apply:` components.
- Author the lightning rulebook rows (data-only, same surfaces): the re-homed `paralysis`/`lightning_static_ward`/`lightning_extra_action` mount rows + the new micro-rung/order/marker rows (per design D1/D2/D6); the new `combat_modifiers.yaml` settlement rows (the extra-action grant, the two chance-bearing micro-stun rules, per design D3); `world/rules/rulebook/state_reactions.yaml` gains the two ward rows (fire precedent).
- Retire the lightning catalog echo tests in-file: `LIGHTNING_SPELL_CATALOG` + `LightningSpellCatalogTests`' echo methods (field-equality, effect round-trip, key-set equality) in `world/skills/tests/test_spell_catalogs.py` (the `_elemental_spells` builder-shape mechanic test stays — the builder survives as tested grammar; `ClosedVocabularyParseTests` untouched), the `test_lightning_spell_tier_labels_match_the_catalog` tier-label echo in `world/skills/tests/test_cost_tiers.py`, the dev-era `lightning_static_ward`/`lightning_extra_action` numeric pins in `world/rules/tests/test_buffs.py` (re-homed to load/apply/presence assertions per the ratified M4 discipline), plus the display-census test re-home.
- Sync `world/rules/rulebook/status_display.yaml` with the key census (the shipped coverage check requires exactly one entry per displayable live buff key ∪ combat-modifier rule id; reaction rule ids carry no display row — the fire ignite precedent): keep/relabel the surviving rows, add every new buff key and combat-modifier rule id per the table's label/severity conventions.
- Docs/traceability integration owned here: shard-manifest final state (`.github/evennia-shards.json` — this change owns the wave's last manifest edit); `lightning.md` stays the authority and is untouched; `docs/lore/magic-system.md` §3's lightning row checked for contradiction only; `tests/test_command_docs.py` untouched (no command-surface change); `docs/development/adding-spells.md`'s SELF_ONLY example (`static_ward`/`thunder_gods_haste`) stays valid — both survive as individually-written SELF_ONLY rows.

## Capabilities

### New Capabilities
None.

### Modified Capabilities
- `skill-registry`: the duplicated ten-row 雷-element data-contract requirement (`skill-registry::skill-registry-contains-the-full-雷-element-spell-set`) is REMOVED and replaced by one behavioral requirement — lightning spell progression composes executable 回合 behavior (an extra-action grant settled through the widened count-consumption leg at the authored round duration; in-round order rewrites — self advance and strike-sourced and cast-sourced tail retreats — settled through the declarative fold at authored keys; the 麻痺 ladder locking actions through the shipped rule path at authored 20/30 s rungs on lightning-owned keys; a declared-chance micro-rung whose seeded roll is recorded in the skip event; a three-strike 多段 rung settled through the widened cap with its single-terminal-emission discipline; a 處決級 execution rung that ignores defense; 毀滅級 devastation rungs; a two-root branching lineage whose two主宰 routes converge ONLY at the two-parent 神格 canopy) with the tree shape, per-node effects, coefficients and AS-IS costs carried as data pinned at load/apply/presence, not per-field echo.
- `skill-lineage`: ADDED 「The lightning lineage ships as the authored two-root branching tree with a two-parent canopy」 (registered-content shipped contract mirroring the fire/wind/ice lineage requirements' shape: two roots, the two authored branch points, the two independent route chains converging ONLY at the 神格 canopy consuming both authored parents, mastery passives out-graph). No dev-era lightning lineage requirement exists — the wave ADDS, the wind/ice precedent.
- Consequences recorded without deltas (no behavior of a generic surface changes): freeform-eligibility — the pure-damage nodes stay eligible through the shipped ladder; every control/order/buff-shaped node was never scalable; the `_elemental_spells` builder loses its last caller but stays shipped grammar with its mechanic test intact.

## Impact

`world/skills/registry.py` (lightning block: `_elemental_spells` call replaced by individually-written `_spell` rows per the earth/fire/wind/ice shape — per-node `effect_policies`, prerequisites, multi-prereq capstone); `world/rules/rulebook/buffs.yaml` (dev-era rows re-homed, new mount/marker rows), `world/rules/rulebook/combat_modifiers.yaml` (new settlement rows; ZERO changes to existing rules), `world/rules/rulebook/state_reactions.yaml` (two ward rows), and `world/rules/rulebook/status_display.yaml` (display-metadata census sync); `world/skills/tests/test_spell_catalogs.py` (echo retirement + new synthetic lightning behavior registration), `world/skills/tests/test_cost_tiers.py` (tier echo retirement), `world/rules/tests/test_buffs.py` (M4 re-home), new `world/rules/tests/test_lightning_turn_behavior.py` registered in `.github/evennia-shards.json`.

This turn creates planning artifacts only. Do not apply, archive, sync main specs, create feature branches or merge until requested.

## Batch

- depends-on: turn-order-control
  (code conflicts: `world/rules/rulebook/combat_modifiers.yaml` — both append rows to the same table; turn-order-control ships ZERO live rows (grammar + load validation only), this change authors every live user; appends are textual-tail, supervisor orders them after the behavior merges. `world/skills/registry.py` — this change owns the lightning block alone.)
- depends-on: multi-strike-cap-widening
  (code conflicts: none — this change authors `thunder_combo`'s `DamagePolicy(extra_strikes=2)` data over that change's widened validation; `.github/evennia-shards.json` — three wave changes each append one test module; manifest edits sequence under supervisor, this change owns the final state.)
