## 1. Preparation (verify shipped state at the apply-time tip)

- [ ] 1.1 Confirm both predecessors' contracts are merged and inspect current source/exported references (codegraph/LSP) for the dev-era lightning registry block (`_elemental_spells("lightning", …)` + the two `_skill` SELF_ONLY rows), the dev-era buff keys (`lightning_static_ward`, `lightning_extra_action`, `paralysis`), the shipped `BLOCKING_BUFF_KEYS = {"paralysis"}` ownership, `paralysis_locks_actions`, the fire `physical_hit`/`apply_buff_to_source` reaction rows, the wave's shipped grammar (`round_order` load clause, `mark_order_op`, chance-bearing lock validation, count consumption, `extra_strikes ∈ {0,1,2}`), and the individually-written `_spell` block shape (ice/fire precedent).
- [ ] 1.2 Author the 14-node tree exactly per design.md D1 (keys/labels/descriptions/targets/MP costs AS-IS/prerequisites via `SkillPrerequisite(node, N)`/coefficients + per-node `effect_policies` per D3-D5/effect bindings), deleting the dev-era `_elemental_spells("lightning", …)` block wholesale without alias and adding the four new keys `lightning_flicker`/`thunder_shatter_strike`/`thunder_prison`/`thunder_apotheosis`; verify at registry load through the shipped lineage validator: two roots, `spark_shock`/`thunder_combo` each consumed by exactly two edges, the two branch-terminal leaves, the two-parent canopy last.
- [ ] 1.3 Author the lightning rulebook rows per D1/D2/D3 in `world/rules/rulebook/buffs.yaml`: `paralysis` re-homed (duration 30→20, shape otherwise unchanged); NEW `paralysis_enhanced` (30 s, empty modifiers, debuff); `lightning_static_ward` re-homed (bounds illusion → empty-modifier mount, 60 s, buff, refresh); NEW `lightning_flicker_ward` mount (60 s buff) + `flicker_advance` (12 s buff — D4's two-window timing, `round_order: advance_to_head`); NEW marker rows `static_ward_stun`/`flicker_stun` (60 s debuff, `round_order: retreat_to_tail`) and `apotheosis_retreat` (6 s debuff, `round_order: retreat_to_tail`); `lightning_extra_action` re-homed (bounds illusion → empty-modifier mount, duration 60→12, buff, refresh — D4's two-window timing).
- [ ] 1.4 Author the `combat_modifiers.yaml` lightning rows, each its own row appended beside the ice ladder with the wave's comment convention: `thunder_gods_haste_grants_action` (`{buff_active: lightning_extra_action}` → `{actions_per_turn: 2}`), `paralysis_enhanced_locks_actions` (`{buff_active: paralysis_enhanced}` → `{actions_per_turn: 0}`), `static_ward_micro_stun` (`{buff_active: static_ward_stun}` → `{actions_per_turn: 0, chance: 15}`), `flicker_micro_stun` (`{buff_active: flicker_stun}` → `{actions_per_turn: 0, chance: 30}`)  (the two-stuns co-existence resolves through the wave's maximum-chance fold, D3). ZERO changes to existing rules (`paralysis_locks_actions` verbatim behind the re-homed row).
- [ ] 1.5 Author the two `state_reactions.yaml` ward rows (fire shape): `static_ward_micro_mark` (`event: physical_hit`, `buff_active: lightning_static_ward` → `mark_order_op: static_ward_stun`) and `flicker_micro_mark` (`event: physical_hit`, `buff_active: lightning_flicker_ward` → `mark_order_op: flicker_stun`).
- [ ] 1.6 Author the effect compositions per D3-D5: `thunder_combo`'s `EffectPolicy(coefficient=2.0, damage=DamagePolicy(extra_strikes=2))`; `judgement_thunder`'s `bypass_defense=True, predicate=()`; the two 毀滅級 rungs' `max_hp_fraction=0.10`; `thunder_apotheosis`'s ENEMIES damage policy + ENEMIES `buff_apply:apotheosis_retreat` rider; `static_ward`/`lightning_flicker`/`thunder_gods_haste` SELF_ONLY `self_buff_apply` legs (flicker two components: detection mount + `flicker_advance`); `paralyzing_bolt`/`thunder_prison` damage+`buff_apply:` ladders — every per-effect `EffectPolicy` tuple in effect order.
- [ ] 1.7 Sync `world/rules/rulebook/status_display.yaml`: keep the two re-homed rows; add display rows for the new buff keys (`paralysis_enhanced`, `static_ward_stun`, `flicker_stun`, `flicker_advance`, `apotheosis_retreat`, the `lightning_flicker_ward` mount) and the new combat-modifier rule ids (`thunder_gods_haste_grants_action`, `paralysis_enhanced_locks_actions`, `static_ward_micro_stun`, `flicker_micro_stun`) at the table's label/severity conventions — the shipped coverage check requires exactly one entry per displayable rule/buff (reaction rule ids carry no display row; the fire ignite precedent).
- [ ] 1.8 Buff-key + registry-key census: no dangling `buff_apply:`/`mark_order_op:` target; `lightning_mastery` untouched; `player_presets.py`/quests/spawns re-checked for retired-shape references (the `paralysis` duration change is the wave's live cross-consumer edit — enumerate every applier); `world/rules/tests/test_buffs.py`'s dev-era `lightning_static_ward`/`lightning_extra_action` numeric pins stripped to load/apply/presence per M4 — the `paralysis` `assertTrue(blocks_action)` pin SURVIVES verbatim (lightning keeps its marker path).

## 2. Echo retirement

- [ ] 2.1 Retire in-file: `LIGHTNING_SPELL_CATALOG` + `LightningSpellCatalogTests`'s three echo methods in `world/skills/tests/test_spell_catalogs.py` (builder-shape mechanic test + `ClosedVocabularyParseTests` untouched), `test_lightning_spell_tier_labels_match_the_catalog` in `world/skills/tests/test_cost_tiers.py`, and the retired traceability annotations — per the defer-annotation-to-main-sync discipline.

## 2b. Rule-table correspondence (shipped mechanical check)

- [ ] 2b.1 Add one `test_rule_<id>` function per new lightning rule id to `test_combat_modifiers.py`'s correspondence suite (`thunder_gods_haste_grants_action`, `paralysis_enhanced_locks_actions`, `static_ward_micro_stun`, `flicker_micro_stun`) — the shipped mechanical check fails closed on any rule without its one-unit-test; the two chance rows assert their bundle's shape through `matched_combat_modifiers` (bundle-value presence, not settlement dice — settlement lives in the behavior module).

## 3. Behavior tests (synthetic-only; zero data-catalog tests)

- [ ] 3.1 New `world/rules/tests/test_lightning_turn_behavior.py` registered in `.github/evennia-shards.json` (this change owns the wave's final manifest state): presence/composition of the authored census (load/apply/presence, M4), plus the cross-grammar settlement seams the wave's own modules don't already pin — the round loop provisions two slots while the extra-action mount is live and one after lapse; the ward reaction marks only qualifying physical attackers and the chance-bearing lock's recorded roll decides skip/act under fixed dice; a still-unacted struck victim relocates to the tail while an already-acted one is untouched; `thunder_combo`'s three-roll sweep emits one terminal mark.
- [ ] 3.2 Run ONLY the touched modules — no project-wide suite.

## 4. Docs / integration

- [ ] 4.1 `lightning.md` untouched (authority); `docs/lore/magic-system.md` §3 lightning row contradiction check only; `docs/development/adding-spells.md`'s SELF_ONLY example still names live rows (`static_ward`/`thunder_gods_haste` survive); `tests/test_command_docs.py` untouched.
- [ ] 4.2 `openspec validate lightning-spell-catalog --strict` green.
