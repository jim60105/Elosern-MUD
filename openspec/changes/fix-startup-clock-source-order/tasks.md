## 1. Startup ordering

- [ ] 1.1 Reorder `at_server_start` in `server/conf/at_server_startstop.py` so `sync_service_interiors()`, `sync_quest_runtime()`, `sync_guild_economy()`, `sync_guard_npc()`, and `sync_npc_schedules()` all run after `sync_grid()` and before `restore_persisted_sessions()`, keeping `restore_persisted_sessions()` strictly before `sync_wilderness()` (fix-startup-session-restore-order D1) with the existing comment updated to document both constraints
- [ ] 1.2 Update `world/quests/tests/test_deadlines.py::test_server_start_calls_quest_sync_after_map_sync` to assert `sync_quest_runtime()` runs before `sync_wilderness()` (invert the old order assertion) while still after lore/map sync
- [ ] 1.3 Add source-order guards in `world/rules/tests/test_guild_economy_guards.py` asserting each of the five moved syncs (`sync_service_interiors()`, `sync_quest_runtime()`, `sync_guild_economy()`, `sync_guard_npc()`, `sync_npc_schedules()`) appears before `restore_persisted_sessions()`, and restoration before `sync_wilderness()`
- [ ] 1.4 Extend the behavioral invocation-order test (`test_startup_invokes_restore_before_wilderness_sync` twin) so the recorded call order proves all five moved syncs precede `restore_persisted_sessions`, which precedes `sync_wilderness`; annotate with `covers_requirement("player-combat-session::startup-combat-restoration-advances-time-only-after-every-deterministic-clock-source-is-registered")` (the ID exists after task 3.1)
- [ ] 1.5 Update the "啟動順序" section of `docs/gm/operations.md` to describe the new order (world/lore → grid → service interiors → quest runtime → guild economy → guard → NPC schedules → session restoration → wilderness)

## 2. Regression tests

- [ ] 2.1 Add a cold-start probe (EvenniaTest, in `world/rules/tests/test_npc_schedule_runtime.py` or a sibling): clock tick starts at 0, an NPC is assigned a schedule effective at tick 0 with a `state` entry due at tick 3, a well-formed hostile one-round session is persisted and its recorded enemy deleted, then the deterministic startup sequence runs with every clock-source sync registered before `restore_persisted_sessions()`; assert the clock advanced to tick 6 and `npc.db.schedule_state` holds the entry's state. The due-tick-3 event payload cannot be read from the restore call (it discards `settle_session` events), so assert the event only through a spy on `settle_combat_result` following the `test_wilderness_population.py` spy pattern. Annotate with `covers_requirement("npc-schedule-runtime::the-npc-schedules-clock-source-is-registered-before-startup-combat-recovery-advances-time")` (the ID exists after task 3.1)
- [ ] 2.2 Add a quest-deadline probe (EvenniaTest, in `world/quests/tests/test_deadlines.py`): persist a well-formed hostile session whose `rounds_elapsed` is at least 600 (the minimum `deadline_tick` is one hour = 3600 s; `_round_cap` does not gate the invalid-settlement restore path), delete the recorded enemy, and run restoration with `sync_quest_runtime()` registered first; assert the active quest with `deadline_tick = 3600` fails with reason `deadline_expired`. Annotate with `covers_requirement("player-combat-session::startup-combat-restoration-advances-time-only-after-every-deterministic-clock-source-is-registered")` (the ID exists after task 3.1)
- [ ] 2.3 Confirm `world/maps/tests/test_wilderness_population.py::test_restart_settles_committed_victory_before_reconciliation` (monster referenced by a committed session survives reconciliation) stays green with the new order

## 3. Verification

- [ ] 3.1 Sync both delta specs into main specs (`openspec/specs/npc-schedule-runtime/spec.md`, `openspec/specs/player-combat-session/spec.md`) via the openspec sync workflow so the new requirement IDs enter the `spec_traceability` index before the check runs
- [ ] 3.2 Run the affected package suites: `world.maps.tests`, `world.quests.tests.test_deadlines`, `world.rules.tests.test_guild_economy_guards`, `world.rules.tests.test_npc_schedule_runtime`, `world.rules.tests.test_npc_schedules`, `world.rules.tests.test_combat_session`
- [ ] 3.3 Run `openspec validate --change fix-startup-clock-source-order --strict` and `uv run --locked python -m tools.spec_traceability check`
- [ ] 3.4 Run the full Evennia suite before handoff
