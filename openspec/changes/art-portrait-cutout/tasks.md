## 1. Dependencies

- [ ] 1.1 `uv add rembg onnxruntime` (CPU wheels — never `onnxruntime-gpu`). Confirm the resolution succeeds against Python 3.13 and the existing `pillow>=12.3.0` pin; if `rembg` pins an incompatible Pillow, stop and report before writing any code (design: risks).
- [ ] 1.2 `uv sync --locked` and confirm `python -c "import rembg"` works in the project environment. Record the resolved `rembg` version — tasks 3.4 and 3.5 depend on it.
- [ ] 1.3 Confirm no `onnxruntime-gpu`, `nvidia-*`, or CUDA-linked package entered `uv.lock` as a transitive dependency.

## 2. Settings

- [ ] 2.1 Add the `ART_REMBG_*` block to `server/conf/settings.py` next to the `ART_SD_*` output-format block: `ART_REMBG_ENABLED` (`_env_bool`, default `False`), `ART_REMBG_MODEL` (`_env_choice` over `("bria-rmbg", "isnet-anime", "isnet-general-use", "u2net", "u2netp")`, default `"bria-rmbg"`), `ART_REMBG_DOWNLOAD_ENABLED` (`_env_bool`, default `True`), `ART_REMBG_ALLOWANCE_SECONDS` (`_env_int_bounded`, default `120`, `low=10`, `high=1800`), `ART_REMBG_THREADS` (`_env_int_bounded`, default `0`, `low=0`, `high=256`).
- [ ] 2.2 Add the two code-only settings with the comment stating why each is NOT environment-overridable: `ART_REMBG_BACKEND = "world.art.cutout.RembgCutoutBackend"` (import-executing seam, same rationale as `ART_SD_CLIENT`) and `ART_REMBG_MODEL_DIR = os.path.join(GAME_DIR, "server", ".rembg")` (persistent-volume path, same rationale as `ART_STORE_ROOT`).
- [ ] 2.3 In the `ART_REMBG_MODEL` comment, state the licensing fact plainly: `bria-rmbg` wraps BRIA-licensed RMBG-2.0 weights and commercial use requires a licence from BRIA; `isnet-anime` is the permissive drop-in alternative.
- [ ] 2.4 Add the alpha-hostile boot check AFTER the `secret_settings` import, beside the derived `ART_SD_OUTPUT_EXTENSION`: `ART_REMBG_ENABLED` true with an effective `ART_SD_OUTPUT_FORMAT == "jpeg"` raises `ImproperlyConfigured` naming both settings and the reason. Placing it after the secret import is load-bearing — a `secret_settings.py` format override must be caught by the same check.
- [ ] 2.5 Add the five env-backed names to `_ENV_OVERRIDES` in `server/conf/test_settings.py` (`test_test_settings_override_names_match_the_settings_ast` fails otherwise). Do NOT add `ART_REMBG_BACKEND` or `ART_REMBG_MODEL_DIR` — they read no environment.

## 3. The background-removal seam

- [ ] 3.1 Create `world/art/cutout.py` with a module docstring stating the boundary: local CPU post-process on transport PNG bytes, applied between generation and encoding, for portrait subject kinds only. Define `CutoutError(Exception)` carrying `.code`, mirroring `SDError`.
- [ ] 3.2 Define `CUTOUT_SUBJECT_KINDS = frozenset({ArtSubjectKind.CHARACTER, ArtSubjectKind.MONSTER})` and `applies_to(kind) -> bool` as an explicit membership test — never `kind is not ArtSubjectKind.SCENE`.
- [ ] 3.3 Implement `resolve_cutout_backend()` (dotted-path import of `ART_REMBG_BACKEND`, mirroring `resolve_sd_client()`) and the module-level `remove_background(png_bytes) -> bytes` entry point. `remove_background` maps EVERY escaping exception to a `CutoutError`: resolution/import/session/model problems to `art_cutout_unavailable`, removal problems to `art_cutout_error`. Nothing unbounded may leave this function.
- [ ] 3.3a Validate the backend's return value in `remove_background` before returning it: non-`bytes`, empty, or missing the PNG magic raises `art_cutout_error`. Without this an unvalidated value reaches `encode` and settles as `sd_format_error`, pointing the operator at the format settings for a backend fault.
- [ ] 3.4 Implement `RembgCutoutBackend`. Import `rembg` and `onnxruntime` lazily inside the first call, never at module import. Before the import, create `ART_REMBG_MODEL_DIR` if absent and set BOTH `U2NET_HOME` and `REMBG_HOME` to it; verify against the version pinned in 1.2 which name that release actually reads and note it in a code comment (setting both is intentional belt-and-braces, not a guess left unchecked).
- [ ] 3.5 In the same backend, honour `ART_REMBG_DOWNLOAD_ENABLED=false` by checking for the configured model's on-disk artifact under `ART_REMBG_MODEL_DIR` BEFORE importing `rembg` and raising `art_cutout_unavailable` when absent — no import, no network, no wait. Confirm the artifact filename convention against the pinned version from 1.2.
- [ ] 3.6 Build at most one session per configured model per process: a module-level cache with the lock guarding CONSTRUCTION only (check, lock, re-check) and never the inference call, providers pinned to `["CPUExecutionProvider"]`. Apply `ART_REMBG_THREADS` when non-zero — first check the version pinned in 1.2: if `rembg.new_session()` accepts a caller-supplied `SessionOptions`, use `intra_op_num_threads`; if it builds its own and reads `OMP_NUM_THREADS` (the 2.x behaviour), set that variable alongside the model-home variables from 3.4 instead. Record which path applies in a code comment; do not assume the signature.
- [ ] 3.7 Create `world/art/fake_cutout.py::FakeCutoutBackend` mirroring `FakeSDWebUIClient`: records every call, `fail_every_call` / `add_failure` script `CutoutError`s, and the success path returns a real PNG whose alpha is zeroed over a fixed region. It must never return its input unchanged (an alpha assertion against a passthrough double is a tautology) and must import neither `rembg` nor `onnxruntime`.
- [ ] 3.8 Keep `world/art/cutout.py` free of any `world.observability` call: it raises, the worker logs (design D12), so no failure is reported twice.

## 4. Worker integration

- [ ] 4.1 In `world/art/worker.py::_settle_one`, insert the stage between `client.generate(...)` and `encode(...)`: when `settings.ART_REMBG_ENABLED` and `cutout.applies_to(subject.kind)`, replace `image.data` with `cutout.remove_background(image.data)` for the encode call. Leave the `GeneratedImage` provenance fields untouched — the metadata still describes the generation (design D11).
- [ ] 4.2 Add one `except CutoutError` arm before the generic `except Exception`, settling the classic path `FAILED` with `error.code` and the gallery path through the existing `_gallery_failure(error.code)`. Verify by reading that the prior-output retention and the "no job left `in_progress`" guarantees hold on the new arm exactly as they do for `SDError`.
- [ ] 4.3 Emit `art_cutout_done` (info: `job`, `subject`, `image_id`, `model`, duration) and `art_cutout_failed` (warn with `exc=`: same keys plus `code`) through named `world.observability` imports in `worker.py`. Emit neither for a skipped subject.
- [ ] 4.4 Extend `_lease_timeout()`: `per_item = ART_SD_TIMEOUT_SECONDS + _CONVERSION_ALLOWANCE_SECONDS + (ART_REMBG_ALLOWANCE_SECONDS if ART_REMBG_ENABLED else 0)`. Update the function docstring to name the third term and to record why the one-time model download sits outside the bound (the claim-token rule makes an overrun safe).
- [ ] 4.5 (design D6a) Make the classic terminal-failure settle claim-token guarded: `world/art/queue.py::settle` and `_settle_by_key` gain a required `generation_token` argument and reject a token mismatch as a no-op returning `None`, exactly like `settle_generated`; `_run_and_settle_batch` and `_fail_batch` pass the claim-time token snapshot the worker already captures before blocking work. Update every existing caller (worker failure paths, `_fail_batch`, and any test calling `settle` directly) in the same commit — no compatibility shim.

## 5. Container, environment inventory, and documentation

- [ ] 5.1 `Containerfile`: add `install -d -m 775 -o root -g 0 /app/server/.rembg` to the app-layout stage and `/app/server/.rembg` to the final stage's `VOLUME` list.
- [ ] 5.2 `compose.yaml`: add the `evennia-rembg:/app/server/.rembg` mount and declare the named volume. Do not add an `ART_REMBG_*` interpolation line — these knobs arrive through `env_file: .env` like the rest of the `ART_*` family.
- [ ] 5.3 `.env.example`: add the five env-backed `ART_REMBG_*` entries with type, default, and the licensing note on `ART_REMBG_MODEL`. `test_env_example_documents_every_env_backed_override` and `test_env_example_advertises_no_dead_variables` both fail without this, and adding `ART_REMBG_BACKEND` or `ART_REMBG_MODEL_DIR` here would fail the dead-variable check.
- [ ] 5.4 `docs/development/settings-and-environment.md`: add the five knobs to the inventory table with type, default, and validation rule, plus a short subsection on the model cache directory, the download policy, and the jpeg-vs-alpha boot refusal. `test_guide_documents_every_env_backed_setting_and_is_linked` fails otherwise.
- [ ] 5.5 `docs/gm/prompts.md`: extend the `ART_SD_*` operator table with the removal knobs, the one-line opt-in recipe, and the honest costs — ~1 GB model on first use, ~10 s of CPU per portrait, and roughly 1–1.5 GB of PERMANENT resident memory added to the Evennia server process once the session is built (an order of magnitude less with `isnet-anime`). Also state that scenes are never cut out, that seed-synchronized art keeps its background, and that `ART_REMBG_DOWNLOAD_ENABLED=false` plus a pre-seeded volume is the no-runtime-network configuration.
- [ ] 5.6 Update the inventory literals in `server/conf/tests/test_env_overrides.py` — `ENV_BACKED`, `DEFAULT_REPR`, `VALID_OVERRIDES`, `INVALID_VALUES`, `BOOL_SETTINGS` as applicable, plus the AST set read by `test_settings_ast_reads_exactly_the_env_backed_inventory` — for the five new env-backed `ART_REMBG_*` names and the two code-only names.

## 6. Tests

- [ ] 6.1 New module `world/art/tests/test_cutout.py` (unit, `unittest.TestCase` where possible): seam resolution and its `art_cutout_unavailable` mapping; `remove_background` bounding an arbitrary exception; the return-value validation from 3.3a (non-`bytes`, empty, and non-PNG each raising `art_cutout_error`); `applies_to` membership; the `ArtSubjectKind` exhaustiveness contract test that fails when a new kind is added without a decision; the fake's alpha behaviour.
- [ ] 6.2 In the same module, cover `RembgCutoutBackend` with the ONNX/`rembg` session factory patched (never a real model): providers are exactly `["CPUExecutionProvider"]`; the session is built once for three consecutive calls; `ART_REMBG_THREADS=4` reaches the session options and `0` does not; both model-home environment names equal `ART_REMBG_MODEL_DIR` and the directory exists; `ART_REMBG_DOWNLOAD_ENABLED=false` with no cached artifact raises `art_cutout_unavailable` with no `rembg` import and no network call. Laziness is proven as a PAIR: (a) importing `world.art.cutout` does not import `rembg` (assert via `sys.modules`), and (b) with the `rembg` import patched to raise, the first real removal attempt raises `CutoutError("art_cutout_unavailable")` — the module-import test alone would pass even if first-use handling were wrong.
- [ ] 6.3 Extend `world/art/tests/test_worker.py` with the enabled-stage integration cases, injecting the fake through `override_settings(ART_REMBG_ENABLED=True, ART_REMBG_BACKEND="world.art.fake_cutout.FakeCutoutBackend")`: character portrait stored transparent; monster portrait stored transparent; gallery character job and gallery monster job each append one card whose file is transparent; scene drain records zero backend calls and produces bytes byte-for-byte equal to the disabled run; disabled-stage drain records zero calls for every kind. **Every transparency assertion must drive the sd fake with an OPAQUE multi-pixel PNG fixture, not `fake_sd_client.DEFAULT_PNG`** — that constant is a 1x1 already-transparent image, so an alpha assertion against it passes with the stage disabled and proves nothing. Add the opaque fixture next to `DEFAULT_PNG` (or build it in the test module) and assert the stored bytes differ from the same job's disabled-stage output.
- [ ] 6.4 Extend `world/art/tests/test_worker.py` with the failure cases: `art_cutout_unavailable` from an unresolvable backend path; `art_cutout_error` from a scripted backend failure; an arbitrary non-`CutoutError` exception still settling a bounded cutout code; a failing gallery cutout appending no card and recording the code on the gallery record; a mixed batch (scene + failing portrait + succeeding portrait) settling all three terminally; prior valid output retained and no artifact written on every failure path; the sd client called exactly once per failing job.
- [ ] 6.4a Regression for the D6a token guard (extend `world/art/tests/test_queue.py` and/or `test_worker.py`): claim a classic portrait, reclaim it (lease expiry or forced requeue) so a NEW claim carries a fresh token, then settle the OLD claim's terminal `art_cutout_error` with its stale token — the settle must be a no-op leaving the new claim untouched. Plus the direct unit case: `settle(..., generation_token=<stale>)` returns `None` on an `in_progress` record whose current token differs.
- [ ] 6.5 Add the stage-order test: with `encode` observed, an enabled portrait job passes the backend's bytes (not the client's) to `encode`, and exactly one artifact is published.
- [ ] 6.6 Add the lease-bound tests: disabled equals `ART_SCHEDULER_LIMIT × (timeout + conversion allowance) + margin` exactly; enabled is larger by exactly `ART_SCHEDULER_LIMIT × ART_REMBG_ALLOWANCE_SECONDS`.
- [ ] 6.7 Extend `world/art/tests/test_formats.py` with the alpha round-trip cases for `png`, `webp`, and `avif` in both metadata modes, plus the opaque-source regression that asserts unchanged output for all four formats.
- [ ] 6.8 Extend `world/art/tests/test_art_observability.py` with the event cases: one `art_cutout_done` on success carrying job/subject/image_id/model/duration; one `art_cutout_failed` on failure carrying the bounded code and a `tb:` segment; no cutout event for a skipped scene or a disabled run. Patch the caller module's binding, never `world.observability.*`.
- [ ] 6.9 Extend `server/conf/tests/test_art_settings.py` and `server/conf/tests/test_env_overrides.py`: the five documented defaults; typed coercion including the case-insensitive model choice; out-of-range/unknown-value boot failures for all five; `ART_REMBG_BACKEND` and `ART_REMBG_MODEL_DIR` ignoring the environment; the jpeg-plus-enabled boot failure via environment AND via a `secret_settings.py` format override; `png`/`webp`/`avif` with the stage enabled booting normally; jpeg alone still booting.
- [ ] 6.10 Confirm `world.art.tests.test_cutout` needs NO shard-manifest edit — the shard-4 `world.art` prefix already owns it. Verify with `tests.test_evennia_test_optimization_contract`; edit `.github/evennia-shards.json` only if that contract test actually demands an explicit label.
- [ ] 6.11 Annotate with `@covers_requirement` ONLY requirements whose canonical IDs already exist in `uv run --locked python -m tools.spec_traceability list` (the already-main capabilities this change modifies: `art-queue-worker`, `art-output-format-pipeline`, `settings-environment-overrides`, `container-image`). The NEW `art-portrait-cutout` capability has no main spec until archive-sync, so its tests are written substantively complete now WITHOUT annotations; the archive workflow's follow-up (sync the delta into `openspec/specs/`, then annotate the newly canonical IDs) owns their traceability. Do NOT invent future IDs — unknown literals are rejected by the check.

## 7. Verification

- [ ] 7.1 `openspec validate art-portrait-cutout --strict` passes.
- [ ] 7.2 `uv run --locked python -m tools.spec_traceability check` reports zero uncovered requirements.
  Under 6.11's annotation split, the four already-main capabilities' requirements this change touches must show annotations or their pre-existing covering tests, and no UNKNOWN-id annotation may appear; the new `art-portrait-cutout` capability is not in the main-spec index until archive-sync, exactly as the traceability document prescribes for active changes.
- [ ] 7.3 `uv run --locked python -m tools.observability_lint check` passes with `tools/observability_freeze.json` still empty — no new entry.
- [ ] 7.4 The `world.art` and `commands-server-typeclasses` Evennia shards pass, plus `tests.test_evennia_test_optimization_contract`.
- [ ] 7.5 With `ART_REMBG_ENABLED` at its default, confirm the whole art suite is byte-identical in behaviour to before this change: no test loads `rembg`, no test touches the network, and no existing lease or format assertion changed.
- [ ] 7.6 `podman compose build` succeeds and `podman compose config` shows the `/app/server/.rembg` volume; confirm the built image contains no `.onnx` artifact.
- [ ] 7.7 Manual acceptance on the developer box only (never in CI): with `ART_REMBG_ENABLED=true` and the real backend, `@art requeue` one character portrait and one monster portrait, confirm the stored files carry transparency and that `@art status` and the `art_cutout_done` event agree; then confirm a scene requeue is untouched.
