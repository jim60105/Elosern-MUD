"""Data-contract test: skill registry content contract
Skill-registry, category-classification, and content tests.

Package split of the original flat module; each slice module groups the
shipped classes by concern (the registry contract suite, the content-
completion and divine-mystery families, the category classification and
flee-declaration pins, and the out-of-combat availability policy). The
category/mastery vocabularies and the pinned false-flag key set live in
``_support`` (not a collected test module).
"""
