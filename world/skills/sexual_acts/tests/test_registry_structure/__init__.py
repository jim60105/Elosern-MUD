"""Data-contract test: sexual act registry structure contract
Whole-registry structural invariants for the sexual act catalogue.

Package split of the original flat module; each slice module groups the
shipped classes by concern (the SexualActDef contract and pair-event
validation, the act-family battery, the whole-registry assembly and
effect/channel structure, and the divine eighth-row, ownership-guard and
claimant suites). The rulebook-path probes, the check helpers and the
pinned vocabularies live in ``_support`` (not a collected test module).
"""
