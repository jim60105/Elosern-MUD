"""Immutable settlement-related identity registries (settlement-shops design §3).

This package holds registry-backed identity for goods bundles (assortments).
Place and settlement records arrive in ``settlement-place-registry``; until
then the only member is the assortment layer.
"""