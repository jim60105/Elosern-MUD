"""Freeform casting (element-mastery-freeform-casting) behavior tests.

Package split of the original flat module; each slice module groups the
shipped classes by concern (scale-table lookups, the resolver gate and
ActionRequest scale contract, scaled resolution, preview/session facade, and
the text-command surface). The synthetic mastery/skill rows and scope helpers
live in ``_support`` (not a collected test module).
"""
