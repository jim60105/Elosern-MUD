"""Frozen no-create status read model tests (status read model).

Package split of the original flat module; each slice module groups the
shipped classes by concern (the status read model, the character read model,
and the skill-key grouping/level-reference comparisons). The file-local skill
taxonomy rows and live-registry probes live in ``_support`` (not a collected
test module).
"""
