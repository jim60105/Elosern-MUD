"""Defeat-aftermath violation tests (defeat-aftermath-violation): the guarded
violation settlement over the shared aftermath battlefield — state-derived
roll sourcing, the threshold gate, the per-section violation rulebook, the
attempt loop, the companion pool and digest, event-log ordering, the flag-off
path, and rollback replay.

Package split of the original flat module; each slice module groups the
shipped classes by concern. The shared ``ViolationBase`` /
``EventSourceIsolation`` bases and log probes live in ``_support`` (not a
collected test module).
"""
