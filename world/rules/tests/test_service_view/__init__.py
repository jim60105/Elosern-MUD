"""Service view tests (webclient-service-menus): the frozen services read
model exercised against deterministic fakes.

Package split of the original flat module; each slice module groups the
shipped classes by concern (host/summary resolution, board filtering and
quest rendering, shop/inventory surfaces, and the corrupt-record edge
battery). Shared fakes, fixtures, and the isolation base live in
``_support`` (not a collected test module).
"""
