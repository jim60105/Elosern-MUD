"""Canonical global settlement quantum for deterministic time advancement."""

#: Canonical duration of one settlement step in world seconds (clock gcd over all
#: buff tick_intervals and sexual decay interval_seconds).
SETTLEMENT_QUANTUM_SECONDS: int = 10
