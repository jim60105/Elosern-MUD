"""Pending character command boundary tests (creation gate, wizard, concept).

Package split of the original flat module; each slice module groups the
shipped classes by concern (the pending gate, the interactive wizard flow,
the generative concept command, and the prefilled-default prompts). The
queued-deferLater harness, prompt stubs, and shared fixture helpers live in
``_support`` (not a collected test module).
"""
