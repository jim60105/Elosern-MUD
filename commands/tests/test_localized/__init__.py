"""Localized zh-tw default command tests (localize-limbo-zhtw).

Package split of the original flat module; each slice module groups the
shipped classes by concern (the give/delivery seam, the command surface and
quickbar pinning, the character- and account-side commands, and the XYZ-grid
and help surfaces). The kit item keys live in ``_support`` (not a collected
test module).
"""
